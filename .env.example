# Override the default skill list consumed by scripts/check-prompt-parity.sh.
# Whitespace-separated; comma-separated values are NOT split. The hardcoded
# default tracks the 14 skein-managed skills, so override only for a subset.
MANAGED_SKILLS="conduct content-draft content-review deep-review dev-plan fan-out grill plan-view release review-gauntlet review-plan rfc-finder spec-compliance update-docs"
