# Override the default skill list consumed by scripts/check-prompt-parity.sh
# and scripts/bundle-appliers.sh. Whitespace-separated; comma-separated values
# are NOT split. The hardcoded default in each script tracks the 11 skein-managed
# skills, so override only when working on a subset.
MANAGED_SKILLS="conduct content-draft content-review deep-review dev-plan fan-out plan-view review-plan rfc-finder spec-compliance update-docs"
