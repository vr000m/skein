# Task: skein:release — progressive disclosure, script extraction, audit split, test anchor fragility

**Status**: Draft
**Component**: meta
**Assigned to**: Claude
**Priority**: Medium
**Branch**: TBD
**Created**: 2026-09-17
**Review Gates**: full

## Objective

Reduce `plugins/skein/skills/release/SKILL.md` (243,730 bytes / 456 lines, per `wc -lc` — round 5 fix: the plan previously stated three different figures for this file, "244K, 454 lines" here and "~247KB" later in this same paragraph, and "~102K of ~247K" in round 2's Findings; all now read from one measurement) — roughly a third of the plugin's Claude-mirror skill-corpus byte total, though skills contribute only name+description until invoked, so the real cost is this file's bytes paid per `/release` invocation, not a standing context tax) from a monolithic always-loaded prose spec into a structure that matches every other complex skein skill (`deep-review`, `review-plan`, `review-gauntlet`, all of which ship a generated `scripts/` bundle and keep `SKILL.md` as the orchestration layer, not the mechanism). This is a structural refactor: behavior changes are limited to the two explicit exceptions named in Requirements below (A2.5's additional `--infer-template` entry point, and the test suite's assertion *targets* moving alongside the prose they check) — the untemplated `/release` cut (the common case, including every skein release to date) must not regress, and every templated-branch behavior currently documented in prose must be preserved, whether verbatim or via a named, reviewed equivalent.

## Context

`/code-review xhigh --fix` against `feature/release-repo-template` (2026-09-17, findings recorded in `docs/dev_plans/20260914-feature-release-repo-template.md`) surfaced 15 findings, nearly all of them spec self-contradictions or coverage gaps *within* the template subsystem added by that plan. Reviewing the pattern across findings (#2, #5, #6, #9, #11, #14 in particular) shows the underlying cause isn't isolated prose bugs — it's that deterministic validation logic (template read, jq gate sequence, marker resolution, commit-precondition checks) is re-specified in English at roughly 5 call sites across 2 mirrors (~10 near-duplicate copies), with no single source of truth an editor or reviewer can check for consistency. Every other complex skein skill avoids this by extracting the deterministic parts into `scripts/`, leaving `SKILL.md` to orchestrate.

Three further structural issues surfaced in follow-up review (2026-09-17, this session):

1. **No progressive disclosure.** The template subsystem (schema, jq gates, Audit Mode's dual-anchor scheme, A2.5 inference) is only relevant on the templated branch, yet lives inline in `SKILL.md` and is loaded on every invocation — including the untemplated case, which is the norm today (skein's own repo carries no template).
2. **A2.5 circularly coupled to A2.** Audit Mode's template-inference step (A2.5) proposes a template from release history but must reason about which candidates A2 could/couldn't classify to avoid proposing a canonical template against a repo that deliberately removed one — coupling a read-only classifier to a generative inference step inside the same mode.
3. **Parity tests anchor on prose headings.** `tests/parity/test_release_skill_contract.py`'s region helpers locate sections via `text.index("### Step 1: ...")`-style literal heading lookups; an editorial heading rename raises `ValueError` instead of a meaningful test failure, making the test suite fragile to exactly the kind of copy-editing this refactor will do.

This plan is scoped to structure only. It does not attempt to resolve the 14 unfixed content findings from the xhigh review (marker match-count precedence, Step 6 recompose ordering, jq-pin authorization for new call sites, CRLF/Unicode gate gaps, etc.) — those are logic bugs in the current prose and are out of scope here, though several should get materially easier to fix once the duplicated prose collapses into a single script. This includes findings #9 and #11 specifically (Audit Mode's marker-absent fallback): even if this refactor's A2/A2.5 split makes a fix to #9/#11 obvious, it is **not** folded into this branch — log it as a separate, standalone plan/finding instead, so this plan's own behavior-invariance requirement stays a meaningful check rather than a moving target. See "Related Work" below for the explicit link back to the xhigh findings.

**Grilled decisions (2026-09-17, review-plan Step 6.4, all accepted as recommended):**

1. **Script distribution model** (superseded 2026-09-18, review-plan round 2 — see below): ~~bundled, not hand-authored per mirror...~~ Round 2's architecture, sequencing, and assumptions lenses independently found this unworkable: `BUNDLE_SKILLS` is not a generic per-skill script-bundling list, it is specifically the **auto-fix applier pipeline roster** (`scripts/bundle-appliers.sh`, `scripts/check-sync.sh`, `tests/parity/test-applier-bundle-parity.sh` all assume every registered skill has an applier and consume `BUNDLE_SHARED`'s auto-fix-only files); registering `release` there either hard-fails under `set -euo pipefail` (`bundle_applier_for`'s `*)` arm returns 1 for an unknown skill) or, once an applier arm is stubbed in, ships six inoperative auto-fix files (`reconcile-findings.sh`, `audit-auto-fix-eligibility.sh`, `plan-scope-detect.sh`, `auto-fix-allowlist.json`, `lib/auto-fix-common.sh`, `lens-budget.sh`) into release's mirrors, violating `bundle-map.sh`'s own stated invariant ("bundled stays synonymous with operative"). The decision's rationale was also factually wrong: `plugins/*/skills/review-gauntlet/lib/` **is** an existing hand-authored-per-mirror precedent (6 files, byte-parity-enforced via `GAUNTLET_LIB_PARITY_FILES` in `tests/parity/test-applier-bundle-parity.sh:138`, documented in `AGENTS.md`'s own "### review-gauntlet `lib/` (authored, mirror-parity-enforced)" subsection — which this plan's Related Work section cites two lines below the now-corrected claim). `plugins/*/skills/fan-out/fan-out.sh` is a second hand-authored-per-mirror precedent.

   **Redecided (2026-09-18, review-plan round 2 re-grill, accepted as recommended): hand-authored per-mirror, `review-gauntlet/lib/` pattern.** New scripts are authored separately in `plugins/skein/skills/release/scripts/` and `plugins/skein-codex/skills/release/scripts/` (Codex mirror first via `codex:rescue`, Claude mirror aligned in the same commit, per `AGENTS.md`'s general mirror convention). Byte-parity is enforced via a new `RELEASE_LIB_PARITY_FILES` array in `tests/parity/test-applier-bundle-parity.sh`, modeled on the **full** `GAUNTLET_LIB_PARITY_FILES` precedent — not just its byte-identity loop (lines ~138-147, this plan's earlier, incomplete citation) but also its **enumeration half** (lines ~151-210), which exists precisely because a hand-maintained array silently exempts any future script someone forgets to register from mirror parity forever (round 3 fix: citing only the byte-identity loop reproduces the exact hole the enumeration half was written to close — see Requirements/Phase 2 below for the corrected task). Including the documented anchor-divergent-exclusion carve-out for any script that legitimately resolves `${CLAUDE_PLUGIN_ROOT}` vs `$SKILL_DIR` (matching `gauntlet-common.sh`'s treatment). No `BUNDLE_SKILLS` registration, no `bundle-appliers.sh`/`check-sync.sh`/auto-fix-pipeline involvement — release's scripts carry no auto-fix mode, so nothing in that pipeline applies to them.
6. **Path-anchor bridging** (added 2026-09-19, review-plan round 3 grill, accepted as recommended; template corrected 2026-09-19, round 4): round 2's `SKILL.md`-prose sentinel-token idea (`__RELEASE_SCRIPT_ROOT__`) does not work — no mechanism in either harness expands an arbitrary token in skill prose, and round 3's architecture and assumptions lenses independently found the premise for inventing it was also wrong: `normalize_release_workflow()` only forbids prefix-match/free-form substitution, and already permits exact-full-line divergence pairs. **Redecided: each mirror's `SKILL.md` prose carries its own real anchor** (`${CLAUDE_PLUGIN_ROOT}` on Claude, `$SKILL_DIR` on Codex) at every new script-invocation call site, and `normalize_release_workflow()` gains one new divergence pair per call site — the same shape as its existing sanctioned pairs, not a new mechanism class. **Round 4 correction of the template itself**: the existing machinery is seven cardinality gates guarding four contracts, of which only two (invocation-mode, execution-model) are the symmetric exact-full-line pair shape being followed — not "six pairs" as round 3 stated (a third miscount, after round 2's "four" and round 3's own intermediate "five"); each sanctioned pair spans four regions of `scripts/check-prompt-parity.sh` (shell constants, `awk -v` list, normalizer branches, count-call-plus-gate), not one function edit. See Requirements/Architecture/Phase 1/Phase 2 below for the corrected mechanism.
2. **A2.5 stays default-on** inside `/release audit` — the new `--infer-template` entry point is *additional*, not a replacement, so plain `/release audit` keeps emitting the inference proposal exactly as today. No behavior change on this axis.
3. **A2→A2.5 interface**: A2's classification output stays the interface, **amended round 7 by decision 12 below**: after Phase 2 the extracted script emits structured JSON (classification fields only) and `SKILL.md` renders the human-readable text from it; A2.5 parses those JSON fields. No new persistence artifact (exact fields named in Phase 3 below).
4. **xhigh findings #9/#11 stay out of scope** for this plan, full stop — no follow-up-commit clause, even if the A2/A2.5 split makes a fix obvious mid-implementation.
5. **Phase sequencing**: a new Phase 1.5 lands the stable-anchor markers *before* any prose moves (trivially green), so Phases 2–3 move prose against already-stable anchors instead of racing Phase 4 to fix what they just broke.
7. **A2 characterization test / fixture-diff "before" state (added 2026-09-19, review-plan round 4 grill, accepted as recommended; scope completed 2026-09-19, round 5 fix).** Round 3 moved the A2 characterization test to Phase 1.5 to fix a sequencing defect, but round 4 found this created two new defects: (a) the test has no single comparable subject across the Phase 1.5 → Phase 2 boundary — pre-extraction A2 output is model-read prose, post-extraction is script stdout, different artifacts; (b) round 3's redefinition of the fixture-diff task (diff extracted-script stdout instead of LLM prose) means Phase 2 is the phase that *creates* those scripts, so there is no "before" script-stdout state to diff against. **Redecided: Phase 1.5 captures A2's classification *value*, not raw prose/transcript, as a committed golden fixture** (e.g. `tests/release/golden/a2-marker-absent.json`, recording the classification for the marker-absent, no-current-template case). Phase 1.5's own Findings entry records this capture as a **manual, human-verified step** (there is no pytest/bash harness that can execute the prose-driven, pre-extraction LLM path — round 5 fix: round 4's "Phase 1.5's own test asserts..." language wrongly implied an automated check existed for this side of the comparison; only the post-extraction, script-stdout side is machine-asserted). **Round 5 fix — decision 7's scope was previously A2-only, leaving every other extracted script (jq-gate validation, commit-precondition check, marker resolution) with no before-state**: Phase 1.5 commits one golden fixture **per extracted script, per fixture-repo case** (templated + untemplated), not just the A2 marker-absent case; the full golden set is what supplies Phase 2's/Phase 3's fixture-diff "before" state for every script, not only A2's. See Phase 1.5/Phase 2/Phase 3 below.
8. **A2.5 reference-file placement (added 2026-09-19, review-plan round 5 grill, accepted as recommended).** Phase 3 originally moved A2.5's inference prose into the same `references/template-subsystem.md` file as the template schema and jq-gate documentation, gated on the templated-branch trigger (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release) — but A2.5 must also run via the standalone `--infer-template` entry point on repos with **no** current template (that is A2.5's entire purpose per Context: proposing a template from release history when one was deliberately removed), so the templated-branch gate would leave `--infer-template` on an untemplated repo with no reference file to load. **Redecided: A2.5's inference prose splits into its own file, `references/audit-inference.md`, loaded whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of whether a template currently exists.** `references/template-subsystem.md` keeps only the template schema, jq-gate documentation, and dual-anchor marker resolution, and stays templated-branch-gated. This matches the `content-review` precedent's actual shape (two reference files, each with its own trigger) that round 3 already established as the real model, rather than round 3/4's continued one-file assumption. See Requirements/Architecture/Phase 3 below.
9. **Script input seam and fixture construction (added 2026-09-20, review-plan round 6 grill, accepted as recommended).** Round 5 specified fixtures as committed scratch git repos with commit/tag state that scripts run against; round 6 found that is unrealizable twice over: git stores a nested `.git` as a gitlink (repo convention is `git init` in mktemp at test time), and A2's real inputs are GitHub-side (`gh release list`/`gh release view --json body`, `git ls-remote --tags` peeled SHAs, release bodies carrying `release-template-sha` markers), none of which a local repo has. **Redecided: the extracted scripts are pure over injected local inputs** (release body text, release list JSON, peeled-commit SHA passed as files/argv/stdin); `SKILL.md` prose keeps the `gh`/`git ls-remote` fetches and pipes results in, and Phase 2's mapping table states which A2.5 fields come from script stdout vs the fetch — fetch results are script *inputs*, never outputs, so script stdout is the sole source of A2's *classification* fields, and A2.5's field-contract assertion covers only those. Fixtures are static input files under `tests/release/fixtures/` (a release body as a file, a release-list JSON, SHAs); where git state is unavoidable a deterministic builder in `tests/release/lib.sh` does `git init` in mktemp with pinned identity/dates. Pinned executables cross into scripts as pre-resolved absolute paths via documented env vars (`RELEASE_JQ`, `RELEASE_GIT` — `gh` is never used by a script, since fetches stay in `SKILL.md` prose), which the script re-checks (absolute, exists, executable) rather than re-resolving (`SKILL.md` Step 1b forbids an executable-selector environment as a *resolution* source). `RELEASE_JQ` is required only on code paths that invoke jq: Step 1b pins jq only on the templated branch, so the untemplated case of `read-release-template.sh` must succeed with it unset, fail-closed otherwise; the pytest suite's jq-gate subprocess tests override the same variables.
10. **Golden schema (added 2026-09-20, round 6 grill, accepted as recommended).** Phase 1.5 goldens pin a **value-level JSON projection** (the classification/decision fields), recorded manually from the pre-extraction prose path; Phase 2/3 assert a defined `jq -S` projection of each script's JSON stdout (decision 12) equals the golden — Phase 1.5 fixes the projection's key set, including `exit_code`, and Phase 2 asserts key-set equality, so a Phase 2 subset projection cannot weaken the golden — so output formatting may differ but decisions may not. This removes the requirement that Phase 1 pre-define byte-level stdout for scripts that do not yet exist. Goldens live in `tests/release/golden/`; the comparisons are cases in a Phase 2 bash suite under `tests/release/` wired into `ci:`.
11. **Region-length baseline rules (added 2026-09-20, round 6 grill, accepted as recommended).** `.release-region-length-baseline.tsv` stays append-only; Phase 2 and Phase 3 each append one row per region they shrink or relocate in the same commit; Phase 4 asserts only the **last row per `(region,file)`** against the 10%/20-byte tolerance, and a region relocated to a new `file` supersedes its earlier `(region, SKILL.md)` row. `exempt` (a reason string) is reserved for regions deliberately emptied; shrinks and relocations are recorded by appended rows, never exemptions. Phase 4's tolerance is measured against the **last row's** `byte_length` (each phase's append records the accepted post-change length), so unrecorded drift after the last append fails. The baseline side of each ancestry pin is a machine-readable `# captured-at: <sha>` header (tsv) or first-line comment (test-id baseline); the boundary side (Phase 1.5's and Phase 2's first commits) is recorded by each phase's last commit in `tests/parity/.release-baseline-meta.json` (`phase15_first_commit`, `phase2_first_commit`), which Phase 4 reads — not this plan's prose.
12. **A2 script emits JSON (added 2026-09-20, review-plan round 7 grill, accepted as recommended).** Decision 3's "human-readable text" interface conflicted with decision 10's `jq -S` projection, which presupposes JSON stdout. **Redecided: extracted scripts emit structured JSON (classification fields only); `SKILL.md` renders the human-readable text from it and A2.5 parses the JSON fields.** The A2→A2.5 field-contract assertion moves to Phase 3 (where `references/audit-inference.md` is created), reading the field list from a documented section of that file; Phase 2 documents the A2 script's JSON field schema in its mapping table.

## Related Work

- `docs/dev_plans/20260914-feature-release-repo-template.md` — the plan that introduced the template subsystem this refactor restructures. Its `/code-review xhigh --fix` run (2026-09-17) findings #1–#12, #14–#15 (finding #13 already fixed) are the concrete symptom list motivating this plan; see its Findings/changelog section for the full list. This plan does not re-litigate those findings' resolutions — it changes where the logic they point at lives, and Phases 2–3 below should close several of them as a side effect (tracked per-phase, not claimed as a batch fix). Findings #9 and #11 are explicitly excluded from that side-effect closure per the grilled decision above.
- `AGENTS.md` — mirror-editing convention is documented across its "### Path-resolution idiom (harness-divergent)" and "### review-gauntlet `lib/` (authored, mirror-parity-enforced)" subsections (there is no single top-level `## Mirrors` heading in `AGENTS.md` itself; the project's `.claude/CLAUDE.md` does carry one). Both govern the Codex-mirror-first edit order this plan follows throughout. `AGENTS.md` states no scoped rule limiting `codex:rescue` to "prose and path-anchor edits only" — that phrasing (removed from Requirements below, round 2 fix) overstated what the doc says; the actual, general rule is simply Codex-mirror-first-then-Claude-aligned for every edit to the Codex mirror, scripts included.
- `plugins/*/skills/review-gauntlet/lib/`, `tests/parity/test-applier-bundle-parity.sh` (`GAUNTLET_LIB_PARITY_FILES`) — the hand-authored-per-mirror precedent this plan's revised Phase 2 follows for `release`'s new scripts (redecided grilled decision 1, round 2). `scripts/lib/bundle-map.sh`, `scripts/bundle-appliers.sh`, `scripts/check-sync.sh` (the auto-fix-applier bundle machinery) are explicitly **not** touched by this plan — round 2 review established that machinery is scoped to skills with an auto-fix mode, which `release` does not have.

## Requirements

- `/release <version>` and `/release audit` behavior is unchanged for every untemplated repo (including skein's own repo) before and after this refactor, and the templated branch's documented behavior is preserved — either verbatim or via a named, reviewed equivalent when an assertion's *source of text* necessarily moves (SKILL.md prose → `scripts/` behavior or `references/` content). This is a structural move, not a behavior change, with two explicit, deliberate exceptions: (a) `/release audit --infer-template` is a new additional entry point (A2.5 stays default-on inside plain `/release audit` — grilled decision 2); (b) the parity suite's assertion *targets* relocate alongside the prose they check (Phase 2/3 each ship a mapping table, see below) — the suite's *coverage* must not narrow, proven by a superset-of-test-ids check scoped to the pytest prose-contract file (Phase 4; see the round-2 fix on that check's scope below), not byte-for-byte prose equality.
- Each phase that moves an assertion's source of text (Phase 2, Phase 3) ships a **mapping table** as part of its own deliverable: old assertion (test name + line) → new assertion (script behavior test, or `references/`-content test). No assertion is deleted without a named replacement in the same commit. **Retarget**, for this table's purposes, means: the pytest test function's *name* and *test id* stay in `test_release_skill_contract.py` and keep asserting observable `/release` behavior (CLI output, file state), but its body's mechanism for producing/checking that behavior may call into the extracted script instead of re-deriving the check from `SKILL.md` prose text. A row is satisfied only when the new assertion still fails if the underlying behavior regresses. Per **round-3 fix** (this "still fails if behavior regresses" clause was flagged as unobservable prose, allowing a passthrough like `assert SCRIPT.exists()` to satisfy it): each mapping-table row MUST additionally record **evidence of the retargeted test actually failing** under a deliberate one-line mutation of the extracted script (e.g. flip a jq gate's comparison operator, capture the failing pytest output, then revert) — this evidence is committed alongside the mapping table itself, not merely asserted in prose.
- **Pytest-id baseline (round 3 fix — was ambiguous where/how this lives).** The pre-Phase-1.5 test-id set (captured in Phase 1, compared in Phase 4) is committed as a **data file**, not embedded in this plan's prose `## Findings` section: `tests/parity/.release-test-id-baseline.txt` (sorted, one pytest test-id per line, regenerated by a named `just` recipe, e.g. `just release-baseline-refresh`), with the plan's Findings section recording only the commit SHA it was captured at and a pointer to the file — never the list itself. Phase 1's Findings entry also records `git rev-parse HEAD` at capture time; Phase 4's superset check additionally asserts that commit — read from a machine-readable first-line `# captured-at: <sha>` comment in the baseline file itself, not this plan's prose (round 6 fix, grilled decision 11) — is a strict ancestor of Phase 1.5's first commit, so a `conduct --resume` or an out-of-order re-run of Phase 1 cannot silently rebaseline onto the already-mutated suite.
- **Fixture-repo diff, redefined (round 3 fix; before-state and repo-definition completed round 5).** The original "diff Step 4's composed confirmation output byte-for-byte before/after" task is not executable as specified: `/release` is a model-driven prose skill, and Step 4's confirmation text is LLM-generated and non-deterministic run to run — a byte-identical comparison can never reliably pass or fail for the reason intended. The fixture-diff task instead diffs the **extracted scripts' stdout and exit codes** (deterministic bash, not model output) against Phase 1.5's committed golden fixtures — **not** a live "before this phase's changes" capture (round 5 fix: Phase 2 is the phase that *first creates* the extracted scripts, so no live before-state can exist at that point; round 4's grilled decision 7 already established the golden value as the before-state, but round 3's original wording of the fixture-diff task itself was never updated to match, and round 4's own scope only captured a golden for the single A2 case — round 5 extends this to one golden per extracted script, per fixture-repo case, so every script has a real before-state to diff against). **The representative templated and untemplated fixture cases (static input files under `tests/release/fixtures/`, grilled decision 9) are named as a Phase 1.5 deliverable** (round 5 fix — no phase previously defined, created, or listed these repos, despite Phase 2/3 and Review Focus all referencing "a representative templated + untemplated fixture repo" as if it already existed). Verifying the *prose* composition around that output stays the pytest contract suite's job (via the mapping-table retargeting above), not the fixture-diff task's. Phase 1.5, Phase 2, and Phase 3 each name a concrete command for this (see their task lists below).
- **`references/` files carry no path-anchor lines (added round 5 — architecture and Contradiction Pass findings).** Every new script-invocation line this refactor adds — the ones carrying `${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR` real anchors (grilled decision 6) — stays in `SKILL.md` prose, never in `references/template-subsystem.md` or `references/audit-inference.md` (grilled decision 8). Phase 3 moves only anchor-free descriptive/schema/inference prose into `references/`; this is what keeps the `references/` byte-parity arm (modeled on the existing `content-review/references` plain `diff -r` byte-identity block) satisfiable, since a `references/` file carrying a divergent per-mirror anchor line would fail that byte-identity check by construction, and `normalize_release_workflow()` is invoked only against the two `SKILL.md` paths — it does not, and is not being extended to, cover `references/` content.
- Deterministic template-read/validate/jq-gate logic currently re-specified in prose at the ~5 call sites identified in the xhigh review is extracted into scripts following the shape of `plugins/skein/skills/review-gauntlet/lib/` (a hand-authored, mirror-parity-enforced `lib/` — not `plugins/skein/skills/deep-review/scripts/lib/persist-common.sh`, which was this plan's earlier, incorrect citation for the pattern; the correct precedent files are enumerated in Phase 2 below). Per **redecided grilled decision 1** (round 2, above), these are authored separately in `plugins/skein/skills/release/scripts/` and `plugins/skein-codex/skills/release/scripts/`, byte-parity-enforced via a new `RELEASE_LIB_PARITY_FILES` array in `tests/parity/test-applier-bundle-parity.sh`. `codex:rescue` edits the Codex-mirror `SKILL.md` prose first (general mirror convention, `AGENTS.md`) — this now applies equally to the Codex-mirror `scripts/`, since scripts are hand-authored per mirror, not bundle-copied from a single canonical source.
- **The template subsystem splits into two `references/` files, each with its own trigger (round 5 fix — grilled decision 8, superseding round 2/3/4's single-file assumption).** `references/template-subsystem.md` (schema definition, jq validation gate sequence *documentation* — the scripts now carry the logic — and Audit Mode's dual-anchor marker resolution) loads only on the templated branch (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release). `references/audit-inference.md` (A2.5's inference prose) loads whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of whether a template currently exists — A2.5 must be reachable on untemplated repos, since proposing a template against a repo that deliberately removed one is its entire purpose. Both follow the existing `content-review/references/` precedent, which round 3 established actually ships **two** files with a branching load rule (not one), so this two-file split matches that precedent's real shape rather than round 2/3/4's continued one-file model. There is no harness-enforced lazy-load mechanism for either file, so this is a model-discretion contract, not a hard guarantee: the untemplated path is verified to *reference* `references/template-subsystem.md` exactly once, textually gated behind the templated-branch check (see Review Focus below) — this is the load-bearing claim, not an absolute "must not pay context cost" guarantee, which no phase could actually gate on (round 2 fix). `references/audit-inference.md` has its own, separate exactly-once-referenced-on-audit-mode assertion (Phase 3).
- Audit Mode's A2.5 template-inference is split from A2's read-only drift classification into its own subcommand/step (`/release audit --infer-template`) so A2 never depends on A2.5's proposal state, closing the circular-coupling risk (A2.5 proposing a canonical template against a repo that deliberately removed one). A2 remains a pure read-only classifier with **unchanged emission** (grilled decision 3): A2.5 parses the same fields from A2's output that a human reads today. Per round-2 fix as amended by decision 12 (round 7), this interface is pinned precisely: after Phase 2 extracts A2's matching logic into a script, that script's JSON stdout **is** "A2's existing output" for A2.5's purposes (`SKILL.md` renders the human-readable text from it) — the script's stdout contract (defined in Phase 2's mapping table) is the sole source of A2's *classification* fields A2.5 parses (fetch results are script inputs, not outputs), so there is exactly one artifact named "A2's output" from Phase 2 onward, not two. A2.5 stays default-on inside plain `/release audit` (grilled decision 2); `--infer-template` is an additional standalone entry point for re-running inference alone, not a replacement for the default behavior.
- `tests/parity/test_release_skill_contract.py`'s region-boundary helpers are re-anchored on stable, machine-readable markers (e.g. `<!-- skein:step N -->`-style HTML comments, invisible in rendered prose) instead of literal `### Step N: <title>` heading text **and** the other literal body-text anchors the suite currently uses (e.g. `"1. Read \`CHANGELOG.md\`"`, `"3. **Releases (list only)**"` — not all fragile anchors are headings). Round 2 found the actual scope is far larger than first estimated: the suite has ~142 `.index(`/`.find(` literal-text anchor call sites spread across ~60 `test_*` function bodies (round 3 clarification: the suite has 119 `test_*` functions total; 60 of them contain at least one literal-text anchor — "~60" was always the anchor-bearing subset, not a miscount of the 119 total, contra one round-3 lens's flag), not ~15 region-helper functions — Phase 1.5's scope is corrected below to match. `README.md` and `docs/dev_plans/20260712-feature-release-skill.md` also carry literal anchors the suite reads and are added to Phase 1.5's scope (round 2 fix). **Round 3 correction**: `README.md`'s anchor is not a `.index()`-style literal-text lookup at all — `test_release_skill_contract.py:316-329` selects the row via a `startswith("| release |")` table-row prefix match, then asserts exact cell equality; the HTML-comment-marker treatment above doesn't apply to a markdown table row. `README.md`'s anchor is instead stabilized by giving the `release` row a stable leading cell value the prefix match already depends on (documented as unchanged, not re-anchored) and adding it to the same rename-regression test's file list so a future column edit still fails loudly rather than silently. `docs/dev_plans/20260712-feature-release-skill.md`'s anchor is prose (compatible with the HTML-comment treatment) so it gets the standard marker treatment. This combined work is what closes the opaque-`ValueError` failure mode Phase 1.5 exists to prevent, ahead of Phase 3's `README.md` edit. Both mirrors carry the same markers. Per grilled decision 5, this markers-first work lands in **Phase 1.5**, before Phase 2 moves any prose — not in Phase 4, which now covers only the rename-resilience regression test and the coverage-narrowing check.
- **Mirror-atomicity, not phase-atomicity (round 4 fix — this line previously conflated the two).** `AGENTS.md` requires only that a Codex-mirror edit and its Claude-mirror alignment land in the same commit (mirror-atomicity): whenever a commit touches the Codex `SKILL.md`/`scripts/` half, the corresponding Claude `SKILL.md`/`scripts/` half, any `check-prompt-parity.sh`/lint-glob/pre-commit-config changes that edit needs, and the retargeted test assertions for that edit land in the same commit, with no intermediate commit between the Codex half and the Claude half. It does **not** require that each `### Phase N:` section be exactly one commit (phase-atomicity) — a phase, especially Phase 1.5 given its now much larger scope (round 2/3/4 corrections), may land as multiple mirror-atomic commits. `scripts/check-prompt-parity.sh` must keep passing at the end of every commit (via `just ci`, see below), including against the new `scripts/` and `references/` files. Per **round-4 fix** (correcting round 3's own miscount, superseding round 2's sentinel-token approach, redecided grilled decision 6 above): `normalize_release_workflow()`'s header forbids prefix-match/free-form-substitution rules ("deliberately forbidden here"), but **does** permit exact-full-line divergence pairs. Its actual machinery is **seven** cardinality gates (`check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1`, one `-ne 0`) guarding **four** contracts, of which only **two** (invocation-mode, execution-model) are the symmetric exact-full-line placeholder-pair shape — not "six pairs" as round 3 said; the other two are one-sided line deletions with no counterpart. Each sanctioned pair spans **four regions** of the file (these four ranges cover the whole divergence machinery, not each pair individually — round 5 fix, per the assumptions lens: the two symmetric pairs specifically occupy narrower sub-ranges within these — `:280-286` of the constants, `:316-319` of the `awk -v` list, `:346-361` of the branches, and `:388-395`+`:412-431` of the count-calls-plus-gates; the wider ranges below are supersets, so an implementer following them lands in the right place): shell constants (`:276-286`), the `awk -v` list (`:313-319`), normalizer branches (`:342-361`), and the `count_release_contract_line` calls plus `-ne 1` gates (`:382-431`). Each mirror's `SKILL.md` prose carries its own real anchor (`${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR`) at every new script-invocation call site — no sentinel token, which round 3 found has no expansion mechanism on either harness — and Phase 2 adds one new divergence pair per call site to `normalize_release_workflow()`, matching the 2-symmetric-pair/4-region template above, each still pinned to a per-mirror count of 1 (not compared for equality between mirrors — a same-count-on-both-sides check would pass vacuously if both mirrors dropped the same anchor). This is release's `SKILL.md`'s first path-anchor seam. Round 3 also corrected an overclaim (release is not the repo's *only* byte-compared mirror pair — `rubric.md`, per-skill prompt pairs, `content-review/references`, and `review-gauntlet/lib/` are all byte-compared too), but round 3's narrower replacement claim ("release is the only pair compared via *normalized* full-file diff") is **also false**, per round 4: `fan-out`'s `normalize_fanout_prompt()`/`prompt_files_match()` (`check-prompt-parity.sh:165-190`) is a second normalized-full-file-diff mirror pair, and it uses the exact free-form-substitution class `normalize_release_workflow()`'s header calls "forbidden" — proving that restriction is function-local to `normalize_release_workflow()`, not repo-wide. The actually load-bearing point is narrower: *within `normalize_release_workflow()` specifically*, only exact-full-line pairs are sanctioned, and this plan's new anchors must follow that function-local rule. Phase 3 extends `check-prompt-parity.sh` with a `references/` byte-parity arm modeled on the existing `content-review/references` block — noting (round 3 fix) that block's actual precedent ships **two** references files with a branching load rule and a second skill reading them cross-skill, not the one-file/one-trigger model this plan assumes; Phase 3's two "referenced exactly once" assertions (one per `references/` file) are scoped to `release`'s own two-file case; the `content-review` precedent references each file twice (SKILL.md body plus a listing), so "exactly once" is a deliberate, disclosed departure, not a copy of the precedent (round 6 fix).
- New scripts (`plugins/skein/skills/release/scripts/*.sh` and the Codex-mirror equivalent, hand-authored per redecided grilled decision 1) are added to the `justfile`'s `lint-scripts` recipe globs and `.pre-commit-config.yaml`'s matching `files:` patterns in the same commit that introduces them. Script-level unit test coverage is authored as **bash test suites under `tests/<suite>/`, each with its own `justfile` recipe** — matching this repo's actual convention for canonical-script unit tests (`tests/reconciliation/test-reconciler-unit.sh`, `tests/lenses/test-persist-lens-result.sh`, `tests/gauntlet/test-finding-key.sh`) — **not** folded into `test_release_skill_contract.py` or counted toward its pytest test-id set (round 2 fix: this repo has no generic pytest-based script-unit-test convention, and folding them in would make Phase 4's coverage-narrowing check either non-comparable or silently scoped to the prose file only; see Phase 4). **Round 3 fix**: `justfile:164`'s `ci:` recipe is a hand-maintained dependency list (`ci: lint check-sync … pytest-tests`), not a glob — `lint-scripts`' globs lint the new suite but never *run* it. The new suite's `justfile` recipe MUST be appended to `ci:`'s dependency list in the same commit that introduces the suite (Phase 2), not merely created and left for Phase 4 to notice is missing. Additionally, the `RELEASE_LIB_PARITY_FILES` array added to `tests/parity/test-applier-bundle-parity.sh` (per redecided grilled decision 1 above) is modeled on the **full** `GAUNTLET_LIB_PARITY_FILES` precedent, not just its byte-identity loop: the precedent's enumeration half (`tests/parity/test-applier-bundle-parity.sh:151-210`) walks the union of both mirrors' `lib/` directories and fails on any basename not registered in the array — this is what stops a future script from silently escaping mirror-parity checking forever. Phase 2 reproduces both halves, not just the byte-identity loop this plan's earlier drafts cited alone.
- **`-ne 1` cardinality pins require call-site-qualified distinct line text (round 5 fix — round 4's disposition claimed this was fixed but no phase task actually addressed it).** `count_release_contract_line` counts whitespace-trimmed exact full-line matches file-wide; a `-ne 1`/`-ne 0` gate fails the moment two call sites in the same `SKILL.md` emit the identical anchor line. Since this plan's premise (Context) is roughly 5 call sites collapsing onto shared scripts, at least two call sites would naturally emit the same invocation line unless each is given distinct, call-site-qualified text (e.g. embedding the script's purpose or a short discriminator in the invocation line itself, not just the bare `${CLAUDE_PLUGIN_ROOT}/skills/release/scripts/<name>.sh` (Codex: `"$SKILL_DIR"/scripts/<name>.sh`) form). Phase 1's inventory records each call site's exact, distinct line text; Phase 2's divergence-pair task uses that exact text, never a templated/interpolated line that could collide across sites.
- **Fail-open acknowledgment (round 5 addition).** Each new cardinality gate this refactor adds widens an existing fail-open property of `normalize_release_workflow()`: per `check-prompt-parity.sh:450-452`, any single tripped gate skips the entire normalized full-file mirror diff, not just the failing contract's check. This is accepted as existing, disclosed behavior inherited from the function as it stands today (not something this plan can fix without redesigning the function, which is out of scope) — noted here so a reviewer doesn't mistake the new gates as introducing this property, rather than merely adding two more triggers for a pre-existing one.
- `just ci` (backgrounded per this repo's testing convention) is each phase's actual Validation cmd — not the narrower `pytest tests/parity/test_release_skill_contract.py` + `check-prompt-parity.sh` pairing alone, which is a strict subset and would let a `check-sync`/lint/other-pytest-file regression go unnoticed. The targeted pytest run stays as each phase's fast inner-loop Test command.

## Review Focus

- **No behavior drift**: the refactor must not change what a templated or untemplated `/release` run does (except the two named exceptions above), only where the logic lives and how much of it loads per invocation. Phase 2 and Phase 3 each include a task that diffs the extracted scripts' stdout/exit codes (not the LLM-composed prose around them — round 3 fix, see Requirements above) against Phase 1.5's goldens on the templated + untemplated fixture cases, with a named command in each phase's task list.
- **Progressive disclosure boundary correctness**: verify the untemplated path genuinely never reads `references/template-subsystem.md`, and that `references/audit-inference.md` is read only when Audit Mode actually runs (round 5 fix — two files, two triggers, per grilled decision 8). Since no harness-enforced lazy-load mechanism exists (only the `content-review` prose-instruction precedent — see Requirements above), this is verified via two static assertions (exactly one reference to each file, each textually inside its own trigger region of `SKILL.md`) plus one manual transcript check per file, not an automated harness-level guarantee.
- **Mirror parity**: `scripts/check-prompt-parity.sh` and `test_release_skill_contract.py` both continue to assert what they assert today or a named, reviewed equivalent (per the mapping-table requirement above); confirm the new test-anchor mechanism (Phase 1.5) and the retargeted assertions (Phase 2/3) don't silently narrow what's covered while making the suite pass — Phase 4's coverage-narrowing check (superset-of-test-ids over `test_release_skill_contract.py` only, per the round-2 fix above; the new bash script-unit suites are validated separately by their own `just` recipes, not folded into this comparison) is the actual gate for this. This requires a captured pre-Phase-1.5 baseline test-id set (added to Phase 1, round 2 fix) since Phase 4 cannot compare against a baseline nothing recorded.
- **A2/A2.5 split correctness**: after the split, confirm A2's classification for a marker-absent, no-current-template repo doesn't change (this is exactly finding #9/#11's territory from the xhigh review, and per the grilled decision above, any fix to that territory is explicitly out of scope here — this Review Focus item is about *not regressing* it, not fixing it). Phase 1.5 captures the golden pre-split classification value for this exact case (round 3/4/5 fix — moved here from Phase 3, extended to a golden-fixture comparison — see grilled decision 7 and Phase 1.5/2/3 below), so the split's effect on it is measurable against a committed baseline rather than asserted from memory.

## Architecture & Call Flow

This refactor touches six independently-executing components (round 2 adds component 6, the drift-guard plane, previously omitted); their contracts:

1. **Claude/Codex `SKILL.md` mirrors** (`plugins/skein/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/SKILL.md`) — the orchestration layer. Post-refactor, each invokes the extracted scripts (below) for deterministic work and reads `references/template-subsystem.md` only when the templated branch is reached and `references/audit-inference.md` only when Audit Mode runs (round 6 fix — stale single-file wording). Both mirrors currently carry zero `${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR` path-anchor lines (verified); the new script-invocation call sites this refactor adds are the *first* per-harness path-addressing seam in this pair, addressed via real per-mirror anchors plus one new exact-full-line divergence pair per call site in `normalize_release_workflow()` (round 3 redecided grilled decision 6 — superseding round 2's sentinel-token idea, which had no expansion mechanism).
2. **Hand-authored per-mirror scripts** (`plugins/skein/skills/release/scripts/*.sh`, `plugins/skein-codex/skills/release/scripts/*.sh` — redecided grilled decision 1, round 2) — pure over injected local inputs (files/argv/stdin — grilled decision 9) with exit code and stdout out; the pinned `jq`/`git`/`gh` executables arrive via documented env vars, fail-closed; authored separately per mirror (Codex first via `codex:rescue`, Claude aligned same commit) and byte-parity-checked via `RELEASE_LIB_PARITY_FILES` in `tests/parity/test-applier-bundle-parity.sh` (component 6). Each script's contract (arguments, exit codes, stdout shape) is documented in Phase 2 as part of the mapping table, so `SKILL.md` prose has a concrete interface to call rather than re-deriving the logic.
3. **`references/template-subsystem.md` and `references/audit-inference.md`** (round 5 fix — two files, not one; grilled decision 8) — `template-subsystem.md` (schema, jq-gate documentation, dual-anchor marker resolution) is read by `SKILL.md` only on the templated branch (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release). `audit-inference.md` (A2.5's inference prose) is read whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of template presence — so `--infer-template` on an untemplated repo still has a reference file to load. Both trigger conditions and both readers are named explicitly in Phase 3; both are model-discretion reads (see Requirements above), not harness-enforced gates. Neither file carries a path-anchor line (Requirements above) — every script-invocation call site with a real per-mirror anchor stays in `SKILL.md`.
4. **A2 → A2.5 handoff** — after Phase 2 extracts A2's matching logic into a script, that script's JSON stdout (documented in Phase 2's mapping table; decision 12) is "A2's existing output" for A2.5's purposes — the single pinned medium for A2's classification fields (round 2 fix; see Requirements above). No new persistence artifact.
5. **Test planes, kept separate** (round 2 fix) — `tests/parity/test_release_skill_contract.py` continues to assert observable `/release` behavior (pytest, prose-contract), retargeted per the mapping tables but not expanded to cover script internals. New **script-level unit coverage lives in bash suites under `tests/<suite>/`**, each with its own `justfile` recipe, matching this repo's existing convention (`tests/reconciliation/`, `tests/lenses/`, `tests/gauntlet/`) — not folded into the pytest file or its test-id set.
6. **Drift-guard plane** (round 2 addition — previously omitted from this section despite Phase 2 editing files in it) — `tests/parity/test-applier-bundle-parity.sh`'s new `RELEASE_LIB_PARITY_FILES` array is release's mirror-parity check for component 2, modeled on `GAUNTLET_LIB_PARITY_FILES`. `scripts/lib/bundle-map.sh`, `scripts/bundle-appliers.sh`, `scripts/check-sync.sh` (the auto-fix-applier bundle/drift machinery) are explicitly out of scope for `release` — that machinery governs skills with an auto-fix mode, which `release` does not have (redecided grilled decision 1).

## Implementation Checklist

### Phase 1: Explore precedent and inventory the ~10 duplicate-logic call sites

**Impl files:** `docs/dev_plans/20260917-refactor-release-skill-structure.md` (Findings section — the inventory itself is a committed deliverable, not left in agent context only), `tests/parity/.release-test-id-baseline.txt` (new, the captured baseline data file), `justfile` (new `release-baseline-refresh` recipe) — round 5 fix, these two artifacts were created by this phase's own tasks below but omitted from this list
**Test files:** none
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py --collect-only -q` (round 5 fix — this phase's baseline-capture task actually runs this; `n/a` contradicted that)
**Validation cmd:** `just ci` (backgrounded — round 5 fix, per the repo-wide Requirements rule above; `n/a` was inconsistent with this phase creating and running a new `justfile` recipe)
**Goal:** a concrete, line-numbered inventory of every call site re-specifying template-read/jq-gate/marker-resolution logic in both mirrors, committed to this plan's Findings section so a dropped session or `conduct --resume` doesn't lose it, plus the confirmed harness mechanism for lazy-loaded `references/` files (the `content-review/references/content-guidelines.md` precedent, already cited above — confirm no stronger mechanism exists before assuming this one), plus the pre-Phase-1.5 pytest test-id baseline Phase 4 needs (round 2 fix — Phase 4 previously had no baseline to compare against).

- Grep both mirrors for every prose reference to the jq gate sequence, template schema validation, and marker (`release-template-sha`) resolution; tabulate call site, line range, and which of the xhigh findings (if any) it corresponds to. Commit this table into this plan's `## Findings` section. **For each call site, also record the exact, call-site-distinct invocation line text Phase 2 will write** (round 5 fix — `normalize_release_workflow()`'s `-ne 1`/`-ne 0` cardinality gates fail the moment two call sites emit the identical full line; a bare `${CLAUDE_PLUGIN_ROOT}/skills/release/scripts/<name>.sh` (Codex: `"$SKILL_DIR"/scripts/<name>.sh`) invocation repeated at multiple call sites would collide, so each site's line must carry a distinct discriminator, e.g. naming the calling step) — this table entry is what Phase 2's divergence-pair task and path-anchor writes use verbatim.
- Confirm the `content-review/references/` precedent is in fact the only conditional/lazy-load mechanism in this codebase (no `scripts/`, `tests/`, or justfile gate observes or enforces it) — record this explicitly so Phase 3's verification approach isn't silently assuming a stronger guarantee than exists.
- Confirm `scripts/check-prompt-parity.sh`'s current scope: it walks `rubric.md`, `*-prompt.md` (maxdepth 1), and per-skill normalizer rules — it does not yet cover `scripts/` or arbitrary `references/` subtrees for `release`. Confirm the `content-review/references` block (lines ~563-577) is the copyable precedent for Phase 3's extension. **Round 4 correction** (this bullet still described the superseded round-2 sentinel-token approach verbatim, contradicting redecided grilled decision 6 — four lenses independently caught this stale instruction surviving into round 3's own task list): `normalize_release_workflow()`'s existing divergence machinery is **seven** cardinality gates (`scripts/check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1` and one `-ne 0`) guarding **four** contracts, of which only **two** are the symmetric exact-full-line placeholder-pair shape (invocation-mode, execution-model) Phase 2's new path anchors will follow; the other two are one-sided line deletions with no counterpart and are not the template. Each sanctioned pair spans **four regions** of the file, not one function: the verbatim-line shell constants (`:276-286`), the `awk -v` substitution list (`:313-319`), the normalizer branches (`:342-361`), and the `count_release_contract_line` calls plus their `-ne 1` gates (`:382-431`). Confirm this exact shape before Phase 2 — it replaces round 2's sentinel-token instruction and round 3's "six pairs, one function" miscount, both wrong.
- Confirm the corrected script-distribution precedent (redecided grilled decision 1, round 2): `plugins/*/skills/review-gauntlet/lib/`'s file set and `GAUNTLET_LIB_PARITY_FILES` (`tests/parity/test-applier-bundle-parity.sh:138-147`, the byte-identity loop) **and** its enumeration half (`:151-210`, the union-over-both-mirrors guard against a silently-unregistered future script — round 3 fix, see Requirements above), including the anchor-divergent-exclusion carve-out for `gauntlet-common.sh`, as the exact template for Phase 2's `RELEASE_LIB_PARITY_FILES`. This replaces the original inventory task of confirming `scripts/lib/bundle-map.sh`'s `BUNDLE_SKILLS`/`bundle_applier_for` shape, which is no longer relevant — `release` does not register there.
- Confirm `normalize_release_workflow()`'s exact existing cardinality-contract count and shape (round 4 fix — the plan previously miscounted this as "four", then "five", then round 3's own "six pairs"): **seven** cardinality gates (`check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1`, one `-ne 0`) guarding **four** contracts, of which only **two** (invocation-mode, execution-model) are symmetric exact-full-line placeholder pairs, each spanning four file regions (constants, `awk -v` list, normalizer branches, count-call-plus-gate). This 2-symmetric-pair/4-region shape — not "six pairs, one function" — is the template Phase 2's new path-anchor divergence pair(s) follow (redecided grilled decision 6).
- Run `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py --collect-only -q` against the pre-Phase-1.5 tree; commit the resulting sorted test-id list, filtered to `::` lines (`--collect-only -q` also prints a blank line and an `N tests collected in Xs` summary that would break the superset check) and prefixed with a `# captured-at: <sha>` header line the check skips (round 6 fix) to `tests/parity/.release-test-id-baseline.txt` (a data file, not this plan's prose — round 3 fix) via a new `just release-baseline-refresh` recipe, and record the capture commit's `git rev-parse HEAD` plus a pointer to the file in this plan's Findings section (round 2/3 fix — Phase 4's superset check needs both a machine-readable baseline and a tree-state pin so a re-run can't silently rebaseline onto the mutated suite).
- Grep `README.md` and `docs/dev_plans/20260712-feature-release-skill.md` for literal-text anchors the suite also locates (round 2 finding); confirm which mechanism applies to each — `README.md`'s is a table-row prefix/cell-equality match, not a `.index()` literal lookup (round 3 finding, see Requirements above) — and add the applicable treatment to Phase 1.5's scope below.
- Confirm the exact count of `test_*` functions (119 total) vs. the subset containing at least one literal-text anchor (60) in `tests/parity/test_release_skill_contract.py`, so Phase 1.5's scope bullet states both numbers unambiguously (119 is exact; the anchor-bearing count depends on the counting rule — a round-6 recount gave 63 functions / 140 literal `.index(`/`.find(` sites — so state the rule used and treat those figures as approximate) (round 3 fix — a round-3 lens misread "~60" as a miscount of the 119 total).

### Phase 1.5: Land stable test anchors before any prose moves

**Impl files:** `plugins/skein/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/SKILL.md`, `tests/parity/test_release_skill_contract.py`, `README.md`, `docs/dev_plans/20260712-feature-release-skill.md` (round 2 addition — both carry literal anchors the suite also reads; see Phase 1), `tests/release/golden/*.json` (new — one golden fixture per extracted script per fixture case, round 5 fix), `tests/release/fixtures/` (new — static injected-input files per case, grilled decision 9), `tests/release/lib.sh` (new — deterministic git-state builder, only where git state is unavoidable), `justfile` (`release-baseline-check` recipe, registered in `ci:` and `parity-tests`), `tests/parity/.release-baseline-meta.json` (new — boundary SHAs), `tests/parity/.release-region-length-baseline.tsv` (new), `docs/dev_plans/20260917-refactor-release-skill-structure.md` (Findings section — mapping table, first-commit-SHA record, round 5 fix: these were previously omitted from the Impl-files list despite being this phase's own deliverables)
**Test files:** `tests/parity/test_release_skill_contract.py`
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci` (backgrounded)
**Goal:** stable, machine-readable anchors exist at every region boundary the suite currently locates via literal heading or body text, in both mirrors, with zero prose movement — trivially green, so Phases 2–3 move prose against already-stable anchors instead of racing to fix what they just broke (grilled decision 5). Round 3 fix: this phase now also ships its own mapping table and in-phase coverage gate (it is the largest test-mutating phase and was previously the only one without either), and captures the A2 characterization test moved here from Phase 3 so it reflects genuinely pre-extraction behavior.

- Add `<!-- skein:step-N -->`-style anchors at each region boundary currently reached via `text.index("### Step N: ...")` or literal body-text lookups (e.g. `"1. Read \`CHANGELOG.md\`"`, `"3. **Releases (list only)**"`, `"2. Resolve and validate"`) — both the heading anchors and the body-text anchors the assumptions lens flagged as not-headings.
- **Scope correction (round 2):** the suite has ~142 `.index(`/`.find(` literal-text anchor call sites spread across ~60 `test_*` function bodies, not ~15 region-helper functions as originally estimated — only 4 helper functions are `_`-prefixed shared helpers today; the rest are inline literal lookups inside individual test bodies. This phase's actual task is: (a) promote every inline literal-text lookup into a small number of shared, anchor-keyed region helpers (this *creates* the ~15-helper structure the original estimate assumed already existed), then (b) retarget all ~60 call sites onto those helpers. Budget this phase accordingly — it is the largest phase in this plan by line count touched, not a small mechanical pass.
- Inconsistent heading spellings noted in review (some region helpers use truncated vs. full heading text) are normalized as part of this pass.
- Add the HTML-comment marker treatment to the literal-text anchor in `docs/dev_plans/20260712-feature-release-skill.md` (round 2 addition, see Phase 1 — this file's anchor is prose, compatible with the standard treatment). For `README.md` (round 3 correction — its anchor is a `startswith("| release |")` table-row prefix match plus exact-cell equality, not a `.index()` literal lookup): leave the row's leading cell value unchanged (it is the prefix match's own anchor) and add `README.md` to the rename-resilience regression test's file list below, so a future column edit to the `release` row still fails loudly instead of silently passing.
- Add an anchor for the "templated-branch region" of `SKILL.md` that Phase 3's progressive-disclosure static assertion (Review Focus) needs — round 3 fix: no phase previously created this anchor, so that assertion had no region to check "exactly once, textually inside" against. This phase adds the region boundary anchor; Phase 3 adds the assertion that reads it.
- **Also add the Audit Mode region anchor here** (round 6 fix — Phase 3's `references/audit-inference.md` exactly-once assertion needs an Audit Mode region anchor; adding it in Phase 3, after this phase froze the byte-length baseline and rename test, would split a recorded region and leave the anchor uncovered by the rename test. Anchors are inert, so add it now and record its region in the baseline).
- **Create the representative templated and untemplated fixture inputs** (round 5 fix, redone round 6 — grilled decision 9: round 5 specified committed scratch git repos, which git cannot store as nested `.git` and which cannot carry the GitHub-side inputs A2 actually consumes): commit **static input files** under `tests/release/fixtures/templated/` (a `.release-template.json`, a release-list JSON, at least one marker-bearing release body as a text file, and the peeled-commit SHAs) and `tests/release/fixtures/untemplated/` (skein's own repo shape — no template file, no markers). Where a script genuinely needs git state, `tests/release/lib.sh` builds it with `git init` in a `mktemp -d` using pinned author/committer identity and dates, matching the repo's fixture convention (`tests/lenses/`, `tests/reconciliation/`, `tests/parity/test-anchor-path-with-spaces.sh`). No `tests/**/.git` is ever committed.
- **Move the A2 characterization test here from Phase 3, and extend its scope to every extracted script** (round 3 fix — sequencing and Contradiction Pass findings: Phase 2 ports A2's matching logic into a script one commit before Phase 3's characterization test was previously scheduled to capture a "pre-split" baseline, so that baseline was already post-mutation; round 5 fix — round 4's golden-fixture scope covered only the A2 marker-absent case, leaving every other extracted script with no before-state for Phase 2/3's fixture-diff). **Capture each extracted script's pre-extraction classification/output *value*, not raw prose/transcript, as one committed golden fixture per script per fixture-repo case** (round 4 fix — grilled decision 7 — extended round 5): run the marker-absent, no-current-template audit path (and the equivalent pre-extraction path for the jq-gate validation and commit-precondition checks) against the pre-Phase-2 (and pre-Phase-1.5-prose-move, since this phase moves no logic) `SKILL.md`, on both fixture cases above — the prose path does live `gh`/`git ls-remote` fetches, so the human performing this manual capture supplies the fixture files (release list, release body, peeled SHA) as the mocked fetch results, so golden and Phase 2 comparison run over identical inputs; each golden carries a fixed key set including `exit_code` (grilled decision 10) and a script/case pair a golden does not apply to is listed `n/a` in the mapping table, and commit each result as `tests/release/golden/<script>-<case>.json`, each a **value-level JSON projection** of the decision fields (grilled decision 10; a `jq -S`-normalised object, not raw stdout, since the scripts and their stdout shape do not exist until Phase 2) (e.g. `tests/release/golden/a2-marker-absent.json`, `tests/release/golden/jq-gate-templated.json`, `tests/release/golden/jq-gate-untemplated.json`). **This capture is recorded in this phase's Findings entry as a manual, human-verified step, not an automated pytest/bash assertion** (round 5 fix — round 4's "add a test in this phase asserting the prose-driven path produces that golden value" named no runner, because the prose-driven path is an LLM reading `SKILL.md`, which no pytest/bash harness in this repo can execute; the automated half of this comparison lives entirely in Phase 2/3, which assert the extracted script's stdout against the committed golden value). Phase 2's Validation gate then asserts each extracted script's stdout produces the *same* golden value for its case, on both fixture repos — this is what supplies Phase 2's/Phase 3's fixture-diff "before" state for every script, not only A2's.
- **Record each anchor-bounded region's byte length, with a file column and an exemption column** (round 4 fix — Phase 4's per-region byte-length assertion needs a real Phase 1.5 recording task, and `.release-test-id-baseline.txt`'s pytest-id-only format can't hold these rows; round 5 fix — the file needs a `file` column so Phase 4 can resolve a region after Phase 3 relocates it into `references/`, and an `exempt` column with a machine-readable reason string so Phase 4's tolerance check can read shrink-exemptions directly instead of parsing Phase 2/3's prose mapping tables) into a new committed data file, `tests/parity/.release-region-length-baseline.tsv` (columns: `region`, `file`, `byte_length`, `exempt` — empty string when not exempt), with a `# captured-at: <sha>` header (and a Findings pointer) — Phase 4 asserts against this file and its recorded commit is a strict ancestor of Phase 2's first commit and of Phase 4's tree, mirroring the pytest-id baseline's anti-rebaseline pin above. **Phase 2 and Phase 3 append rows for regions they shrink or relocate, and Phase 4 reads the last row per `(region,file)`; Phase 3 only appends new rows for regions it relocates into `references/` (with the new file's path in the `file` column); it never edits an existing Phase-1.5-recorded row in place** (round 5 fix — a cross-finding conflict between "append" and "edit" proposals; append is the safe direction since it preserves Phase 1.5's original capture as the audit trail).
- **Record this phase's first commit SHA** (round 5 fix — Phase 4's superset-of-test-ids ancestry check needs Phase 1.5's first commit as the boundary point, but no phase previously recorded it) in `tests/parity/.release-baseline-meta.json` as `phase15_first_commit` (written by this phase's last commit, machine-readable — grilled decision 11), and pointed to from this phase's Findings entry, alongside the mapping table and golden-fixture capture-commit records above.
- This phase's own scope now includes the round-2/round-3 corrected sizing (bullet above) — because it both creates and populates the ~15 region-helper functions, ships its **own mapping table** (old inline `.index()`/`.find()` call site → new anchor-keyed helper call) and its own in-phase coverage gate (round 3 fix — Phase 1.5 was previously the largest test-mutating phase with neither, an omission the Requirements mapping-table rule was scoped past by only naming "Phase 2, Phase 3"). **This phase's mapping-table mutation evidence is anchor-scoped, not script-scoped** (round 5 fix — Requirements' mutation-evidence rule is worded over "the extracted script," which does not exist yet in this no-logic-moves phase): each row's evidence is a deliberate one-line mutation of the anchor comment itself (e.g. rename `<!-- skein:step-3 -->` to `<!-- skein:step-3x -->` in a scratch copy), captured failing pytest output, then reverted — proving the retargeted call site actually depends on the anchor rather than passing vacuously via a stale fallback. The coverage gate is a concrete command created **in this phase** (`just release-baseline-check`, registered in `ci:` and `parity-tests` in this phase — round 7 fix, comparing the filtered `--collect-only -q` output against `tests/parity/.release-test-id-baseline.txt`; Phase 4 adds only the ancestry pin — round 6 fix), run here too: at the end of this phase, `--collect-only -q`'s output must be a superset of `tests/parity/.release-test-id-baseline.txt`, checked immediately rather than left solely to Phase 4.
- Add a scratch-copy rename regression test, parameterized over all three anchored files (`SKILL.md` scratch copy, `README.md`, `docs/dev_plans/20260712-feature-release-skill.md` — round 3 fix, was scoped to `SKILL.md` alone): rename a heading (or, for `README.md`, edit a non-anchor cell) in a scratch copy, assert the suite still locates the region correctly via the anchor (not the now-stale heading text). For `README.md` the observable is a named `AssertionError` from the exact cell-equality check (`test_release_skill_contract.py:316-329` — an edited cell fails by design), not a silent pass and not a bare `StopIteration`; specify and assert that (round 6 fix).
- No jq-gate, schema, or template logic moves in this phase — `SKILL.md` prose content is otherwise byte-identical before/after except for the inserted anchor comments and any heading-spelling normalization from the bullet above; the latter is a deliberate, disclosed exception to "byte-identical except anchors," not an oversight (round 2 fix — the prior draft claimed a stronger invariant than this phase's own heading-normalization task delivers).
- **Internal task order for this phase (round 5 fix — this phase's outputs are order-dependent but no order was pinned, and it may span multiple mirror-atomic commits per the Requirements atomicity split):** (1) fixture cases (static inputs + `tests/release/lib.sh` builder), (2) anchors + heading normalization, (3) golden-fixture capture (needs the pre-anchor-move `SKILL.md` for the "before" side, so capture before or in the same commit as the anchor insertion — anchors don't move logic, so ordering relative to (2) is flexible, but must precede Phase 2), (4) byte-length baseline recording (needs the anchors from (2) to exist), (5) mapping table + in-phase coverage gate + scratch-copy rename regression test (needs (2)-(4) complete to test against). Each mirror-atomic commit within this ordering still carries its own Codex-then-Claude pairing per Requirements above.

### Phase 2: Extract scripts/ (deterministic logic)

**Impl files:** `plugins/skein/skills/release/scripts/read-release-template.sh` (+ siblings per Phase 1's inventory), `plugins/skein-codex/skills/release/scripts/read-release-template.sh` (+ siblings, hand-authored per redecided grilled decision 1 — round 2), `tests/parity/test-applier-bundle-parity.sh` (new `RELEASE_LIB_PARITY_FILES` array, modeled on `GAUNTLET_LIB_PARITY_FILES`), `scripts/check-prompt-parity.sh` (new exact-full-line divergence pair(s) in `normalize_release_workflow()`, per-call-site, per redecided grilled decision 6 — not a sentinel-token mechanism, round 2's superseded idea), `justfile` (`lint-scripts` globs), `.pre-commit-config.yaml` (matching `files:` patterns), `tests/parity/.release-region-length-baseline.tsv` (append rows for shrunk regions), `tests/parity/test-prompt-parity-extended.sh` (seed extension for the new gates), `tests/release/` (new bash suite and golden comparisons), `plugins/skein/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/SKILL.md`
**Test files:** `tests/parity/test_release_skill_contract.py` (retargeted per the mapping table below), new script-level unit tests as bash suites under `tests/<suite>/` (round 2 fix — not pytest, see Requirements/Architecture above)
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci` (backgrounded)
**Goal:** every call site from the Phase 1 inventory calls the same hand-authored, mirror-parity-checked script instead of re-deriving the logic in prose; `SKILL.md` shrinks by roughly the amount of prose extracted; behavior is unchanged; the suite's coverage of the extracted logic moves to script-level tests with an explicit mapping table, not silently dropped.

- Port the jq gate sequence (empty/shape/enum/unknown-key/duplicate-key checks, per `SKILL.md`'s existing `## Canonical Format` schema section) into `read-release-template.sh`, following `plugins/skein/skills/review-gauntlet/lib/gate-bounded.sh`'s validation-helper pattern (round 2 fix — the plan's prior citation, `deep-review/scripts/persist-common.sh`'s `persist_validate_json_shape`, does not exist at that path; the actual file is `plugins/skein/skills/deep-review/scripts/lib/persist-common.sh`, but `review-gauntlet/lib/` is the correct structural precedent to follow now that scripts are hand-authored per mirror, not bundled). Assert this script's projected JSON stdout and exit code for both the templated and untemplated fixture case equal Phase 1.5's committed `tests/release/golden/jq-gate-<case>.json` values exactly (round 5 fix — extending grilled decision 7's golden-value comparison, previously scoped to A2 only, to every extracted script).
- Port the commit-precondition check (tracked, matches HEAD) and marker resolution (dual-anchor scheme, Audit Mode's A2 matching) into sibling scripts, authored separately in each mirror's `scripts/` directory. Immediately after, assert the extracted script's projected JSON stdout for the marker-absent, no-current-template case equals Phase 1.5's committed `tests/release/golden/a2-marker-absent.json` value exactly (round 4 fix — grilled decision 7: this replaces "re-run Phase 1.5's test," since the artifact shape changes from prose-transcript to script-stdout across this boundary; the golden value, captured in Phase 1.5, is what stays constant and also supplies this phase's fixture-diff "before" state, below), and likewise assert the commit-precondition script's projected stdout against its own Phase 1.5 golden value for both fixture cases.
- Add `tests/parity/test-applier-bundle-parity.sh`'s `RELEASE_LIB_PARITY_FILES` array (the new scripts' filenames) and **all four** mechanisms of the `GAUNTLET_LIB_PARITY_FILES` precedent (round 4 fix — round 3's "both halves" was itself an incomplete copy: only 2 of 4 actual precedent mechanisms were captured): the byte-identity loop (modeled on lines ~138-147), the enumeration half (modeled on lines ~151-210, which walks the union of both mirrors' `scripts/` directories and fails on any basename not registered in the array), the precedent's `PARITY_GAUNTLET_LIB_ROOT` test-override fixture (lets tests point the parity check at a scratch `lib/`/`scripts/` tree instead of the real one, so parity-check behavior itself is testable), and its **G9 self-test** (a test that deliberately adds an unregistered file to the scratch tree and asserts the enumeration guard actually fails — proving the guard isn't vacuous). If any new script legitimately resolves `${CLAUDE_PLUGIN_ROOT}` vs `$SKILL_DIR`, exclude it via the same documented anchor-divergent-exclusion pattern used for `gauntlet-common.sh`. **Extend the existing self-tests' recursion-skip guards to include the new `RELEASE_LIB_PARITY_ROOT` override variable alongside the existing ones, `PARITY_GAUNTLET_LIB_ROOT` and `PARITY_BUNDLE_REF_ROOT` (round 6 fix — the plan previously named a nonexistent `GAUNTLET_LIB_PARITY_ROOT`; the actual variable is `PARITY_GAUNTLET_LIB_ROOT`, guarded alone at `:221` and with the others at `:314`)** (round 5 fix — the precedent's own convention is that every override variable appears in every self-test's skip guard, e.g. `PARITY_GAUNTLET_LIB_ROOT` alone at `test-applier-bundle-parity.sh:221` and with `PARITY_BUNDLE_REF_ROOT` at `:314`; omitting the new variable from those existing guards would re-spawn the existing fixture runs when release's own fixture is exercised). **Note on section 7's bundle-reference grep (round 6 fix): it matches only the Claude-form `skills/<skill>/scripts/<x>.sh` anchor, so Codex `"$SKILL_DIR"/scripts/<x>.sh` lines evade it — §7 passes as long as both mirrors carry the file but is not a Codex-side guard; the byte-parity arm above is the Codex-side guard.** **Confirm whether the bundle-reference enumeration check (section 7 of the same file) captures release's new `SKILL.md` script-invocation lines**, and if so, add the necessary registration or documented exemption in this commit — its failure message must not instruct the exact remedy grilled decision 1 forbids (registering `release` in `BUNDLE_SKILLS`).
- Write each mirror's real path anchor (`${CLAUDE_PLUGIN_ROOT}` on Claude, `$SKILL_DIR` on Codex) directly into `SKILL.md` prose at each new script-invocation call site, and add one new exact-full-line divergence pair per call site to `normalize_release_workflow()` in `scripts/check-prompt-parity.sh` — the same 2-symmetric-pair/4-region shape as its existing sanctioned pairs (round 4 correction: not "six pairs" as round 3 said — see Context grilled decision 6), each pinned to a per-mirror occurrence count of exactly 1. **Also extend `tests/parity/test-prompt-parity-extended.sh`'s `seed_release_pair_with_documented_divergence` (line ~129, used by 9 tests including `test_release_documented_harness_differences_are_normalized`, which requires exit 0) with the new anchor lines, and add self-test cases per new gate: passes with the anchor present once, fails on a missing anchor, fails on a duplicated anchor** (round 6 fix — the new `-ne 1` gates set `PARITY_DIFF=1` and would otherwise break the seeded synthetic pair, with no self-test of the gates themselves) (round 3 redecided grilled decision 6, superseding round 2's sentinel-token approach, which had no expansion mechanism on either harness).
- Rewrite each Phase-1-inventoried call site in both `SKILL.md`s to invoke the relevant script and interpret its output/exit code, removing the duplicated prose.
- Write the Phase 2 mapping table (old assertion name/line on `SKILL.md` prose → new script-behavior test) as this phase's own deliverable, in this plan's Findings section, **with evidence of each retargeted test failing under a deliberate mutation of the extracted script** (round 3 fix, see Requirements above); retarget each listed assertion in `tests/parity/test_release_skill_contract.py` in the same commit, per the "retarget" definition in Requirements above.
- Add script-level unit test coverage for every new script as a bash suite under `tests/<suite>/` (e.g. `tests/release/test-read-release-template.sh`) with its own `justfile` recipe, matching `tests/reconciliation/`/`tests/lenses/`/`tests/gauntlet/`'s existing convention — **and append that recipe to `justfile`'s `ci:` dependency list in this same commit** (round 3 fix — `ci:` is a hand-maintained list, not a glob; `lint-scripts`' globs lint the suite but never run it, so without this the phase's own `just ci` Validation cmd goes green having never executed the new coverage).
- **Golden comparison, against Phase 1.5's golden values, not a live before/after capture** (round 5 fix; reworked round 6 — grilled decisions 9/10: no live before-state can exist since this phase first creates the scripts, and the goldens are value-level projections). These comparisons are **cases in a Phase 2 bash suite under `tests/release/`**, wired into `ci:` per the bullet above, so `just ci` is the plane that runs them and Phase 3's re-run is simply `just ci`. For each extracted script and each case under `tests/release/fixtures/{templated,untemplated}/`: feed the static inputs to the script, then assert the `jq -S` projection of its stdout, and its exit code, equal the corresponding `tests/release/golden/<script>-<case>.json` value exactly. Each script's projection is documented in this phase's mapping table.
- Add new scripts to `justfile`'s `lint-scripts` globs and `.pre-commit-config.yaml`'s matching `files:` patterns in this same commit.
- Codex mirror `SKILL.md` and `scripts/` edits go through `codex:rescue` first per `AGENTS.md`'s general mirror convention; Claude mirror aligned in the same commit — this now applies to the scripts as well as the `SKILL.md` prose, since scripts are hand-authored per mirror (redecided grilled decision 1, round 2), not bundle-copied from a single canonical source.
- **Append `.release-region-length-baseline.tsv` rows for every region this phase shrinks** (round 6 fix — Phase 2 removes duplicated prose from anchored regions well past the 10%/20-byte tolerance and previously had no task recording it; Phase 4 asserts only the last row per `(region,file)`, grilled decision 11), in the same commit as each shrink; set `exempt` only for a region deliberately emptied.
- **Record this phase's first commit SHA** as `phase2_first_commit` in `tests/parity/.release-baseline-meta.json` (written by this phase's last commit; add that file to this phase's Impl files) — Phase 4's `.tsv` ancestry pin targets it (round 7 fix — no phase previously recorded it).
- **Document the A2 script's JSON field schema in this phase's mapping table** (round 7 fix — decision 12; the A2→A2.5 field-contract *assertion* moves to Phase 3, because `references/audit-inference.md`, the file that names the fields A2.5 parses, is created there and this phase cannot assert against a file that does not exist yet): the schema lists each classification field the script emits, separating fields sourced from script stdout from fetch-side inputs.

### Phase 3: Progressive disclosure split + A2/A2.5 separation

**Impl files:** `plugins/skein/skills/release/references/template-subsystem.md`, `plugins/skein/skills/release/references/audit-inference.md` (new, round 5 — grilled decision 8), `plugins/skein/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/references/template-subsystem.md`, `plugins/skein-codex/skills/release/references/audit-inference.md` (new), `plugins/skein-codex/skills/release/SKILL.md`, `scripts/check-prompt-parity.sh` (`references/` byte-parity arm, two files), `tests/parity/.release-region-length-baseline.tsv` (append rows for relocated regions), `tests/release/` (A2→A2.5 field-contract assertion), `docs/skills_architecture/20260522-design-claude-skills-architecture.md` (invocation-mode catalogue entry for `--infer-template`), release skill's frontmatter/README subcommand listing
**Test files:** `tests/parity/test_release_skill_contract.py` (retargeted per the mapping table below; the marker-absent/no-current-template golden was captured in Phase 1.5 (manual, value-level — round 7 fix, previously described as a moved automated test); this phase re-runs Phase 2's golden comparison via `just ci`)
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci` (backgrounded)
**Goal:** an untemplated `/release <version>` cut is verified, via the static assertion and manual transcript check below (not an absolute harness-enforced guarantee — round 5 fix, matching the hedge already stated in Requirements/Review Focus), to not read `references/template-subsystem.md`; A2.5 is reachable both as part of `/release audit`'s default run (grilled decision 2) and standalone via `--infer-template` on templated **and untemplated** repos alike, since its inference prose now lives in the separately-triggered `references/audit-inference.md` (grilled decision 8, round 5) rather than being gated on the templated branch; A2.5 consumes A2's unchanged existing output (grilled decision 3) rather than a new artifact.

- Move the template schema definition, jq-gate-sequence *documentation* (the scripts now carry the logic; this is the remaining prose description), and Audit Mode's dual-anchor marker resolution into `references/template-subsystem.md` (both mirrors), read by `SKILL.md` only on the templated branch per the `content-review` precedent named in Phase 1. **Move A2.5's inference prose into a separate `references/audit-inference.md` (both mirrors, round 5 fix — grilled decision 8), read by `SKILL.md` whenever Audit Mode runs `--infer-template` or default `/release audit`**, independent of template presence — this closes the contradiction where A2.5's prose would otherwise be unreachable from an untemplated repo's `--infer-template` invocation. Neither `references/` file carries a path-anchor line (Requirements above); the script-invocation call sites with real per-mirror anchors stay in `SKILL.md`, so `scripts/check-prompt-parity.sh`'s `references/` byte-parity arm (below) can stay a plain byte-identity check.
- Add `/release audit --infer-template` as an **additional** standalone entry point that re-runs A2.5 alone — A2.5 keeps running by default inside plain `/release audit` (grilled decision 2). Name the exact fields A2.5 parses from the Phase-2-extracted A2 matching script's stdout (grilled decision 3, pinned per the round-2 Requirements fix — this is now a script-stdout contract, not raw `SKILL.md` prose) as part of this task's deliverable.
- Update the doc/frontmatter/README entries for the new `--infer-template` flag (`docs/skills_architecture/20260522-design-claude-skills-architecture.md` line ~36 prose and the README `release` row). Round 6 fix: `test_invocation_mode_count_matches_release_catalogue` (`test_release_skill_contract.py:871-880`) asserts only skill-count strings ("2 of 14/15 skills … clear both") and pins no subcommand or flag list, so `--infer-template` changes nothing it checks — add a named test in `tests/parity/test_release_skill_contract.py` that pins the `release` subcommand/flag list as listed in `SKILL.md` frontmatter/usage and the README `release` row (assert `--infer-template` appears in both), so the new entry point cannot pass vacuously.
- Assert the marker-absent, no-current-template audit path still equals Phase 1.5's `tests/release/golden/a2-marker-absent.json` value (projected JSON stdout) after this phase's A2/A2.5 split (round 4 fix — grilled decision 7: comparison is against the golden value, not a re-run of Phase 1.5's own test mechanism). Per Context above, do not fix xhigh findings #9/#11 in this phase even if the split makes a fix obvious — log it separately.
- **Assert the A2→A2.5 field contract** (moved here from Phase 2, round 7 fix): a bash-suite assertion that every field name the documented A2.5 field-list section of `references/audit-inference.md` says A2.5 parses appears in the A2 script's projected JSON stdout and goldens (the assertion parses that section's field list; a rename on either side fails), with a mapping-table row carrying mutation evidence (rename a field in the script, capture the failure, revert).
- Add the progressive-disclosure boundary check named in Review Focus: **two** static assertions (round 5 fix — one file became two) — `references/template-subsystem.md` is referenced exactly once, textually inside the templated-branch region anchor Phase 1.5 added (round 3 fix — that anchor now exists for this assertion to check against); `references/audit-inference.md` is referenced exactly once, textually inside the Audit Mode region anchor added in Phase 1.5 (round 6 fix — previously added here, after the byte-length baseline froze) — plus a manual transcript check for each, recorded in this plan's Findings section. Note (round 3 fix, now literally satisfied rather than merely noted): the `content-review/references` precedent this models really does ship two references files with a branching load rule — this plan's two-file split now matches that shape directly, rather than approximating a single-file case scoped narrower than the precedent.
- Extend `scripts/check-prompt-parity.sh` with a `references/` byte-parity arm — **two arms, one per new file** (round 5 fix) — modeled on the existing `content-review/references` block (lines ~563-577), so both new `references/` files are mirror-drift-guarded from the moment they're introduced — they are not covered by any existing check otherwise. Since neither file carries a path anchor (per the task above), a plain `diff -r` byte-identity check suffices for both, with no normalizer needed.
- **Carry forward Phase 1.5's region-helper anchors for content moving into `references/`, by appending new rows, never editing Phase 1.5's** (round 4 addition, append-vs-edit resolved round 5 — a cross-finding conflict between two lens proposals): when this phase moves anchored `SKILL.md` content into `references/template-subsystem.md` or `references/audit-inference.md`, re-point the corresponding anchor-keyed test helpers at the new `references/` file location, and **append** a new row to `.release-region-length-baseline.tsv` for each relocated region (same `region` name, new `file` value, current byte length, empty `exempt` unless also shrunk) rather than mutating the row Phase 1.5 recorded — Phase 1.5's original row stays as the audit trail of the pre-move state. Record the retarget in this phase's mapping table like any other relocated assertion.
- Write the Phase 3 mapping table (old assertion name/line on `SKILL.md` prose → new `references/`-content test) as this phase's own deliverable, with evidence of each retargeted test failing under a deliberate content mutation (round 3 fix, per Requirements above); retarget each listed assertion in `tests/parity/test_release_skill_contract.py` in the same commit.
- **This phase does not touch the extracted scripts** (round 5 fix — round 3's fixture-diff task, copied verbatim from Phase 2, was vacuous here: Phase 3 is a pure prose/references move plus the A2.5 entry-point addition, and touches no script file per the Impl-files list above). Instead, this phase's no-behavior-drift evidence is: (a) the mapping-table mutation evidence above, and (b) re-running Phase 2's fixture-diff commands unchanged post-Phase-3 and confirming they still pass — since this phase changes where prose lives, not what any script does, a passing re-run of Phase 2's existing golden-value comparisons is sufficient; no new fixture-diff command is needed.
- Codex mirror `SKILL.md` and `references/` prose edits go through `codex:rescue` first per `AGENTS.md`; Claude mirror aligned in the same commit; no intermediate commit between the two halves.

### Phase 4: Coverage-narrowing check and rename-resilience regression

**Impl files:** `tests/parity/test_release_skill_contract.py`, `justfile` (`parity-tests` registration, round 6 fix)
**Test files:** `tests/parity/test_release_skill_contract.py`
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci` (backgrounded)
**Goal:** prove the suite's *coverage* did not narrow across Phases 1.5–3, using a check compatible with the refactor's own goal of shrinking `SKILL.md` (a byte-length tolerance would fail by construction on intentional shrinkage, so this phase uses a coverage-preservation check instead).

- **Extend** (Phase 1.5 created `just release-baseline-check`; this phase adds only the ancestry pin) the superset-of-test-ids check scoped to `test_release_skill_contract.py` only (round 2 fix — the new script-level unit tests are bash suites, which emit no pytest ids, so they cannot be spanned by this check without breaking the repo's bash-suite convention; see Requirements/Architecture above): the post-refactor collected pytest test-id set must be a superset of `tests/parity/.release-test-id-baseline.txt` (round 3 fix — a committed data file, not a list embedded in Phase 1's Findings prose; see Requirements above), and the baseline's capture commit (read from its `# captured-at: <sha>` header, not this plan's Findings prose; a shallow clone cannot resolve ancestry, so the check skips with a named warning there rather than failing) must be a strict ancestor of `phase15_first_commit` read from `tests/parity/.release-baseline-meta.json` — proving no prose-contract assertion was dropped without a named replacement, and that the baseline itself wasn't silently recaptured onto an already-mutated tree.
- Add a bash-suite **enumeration** check, modeled on `tests/parity/test-applier-bundle-parity.sh`'s enumeration half (round 3 fix — "confirm every suite is registered and passing" previously named no mechanism): walk `tests/release/test-*.sh` (excluding the `tests/release/lib.sh` builder, which is a helper, not a suite), and for each suite assert a `justfile` recipe invokes it and that recipe is a `ci:` dependency (two-hop: suite → recipe → `ci:`, since `ci:` lists recipes, not files), failing loudly on any unregistered suite rather than silently skipping it. **Register this enumeration check in the hand-maintained `parity-tests` recipe (`justfile:27-40`) and list `justfile` among this phase's Impl files** (round 6 fix — the check is bash-shaped but Phase 4's Impl files named only the pytest file, the same hole round 3 closed for `ci:`). This is the script-coverage half of the proof, now actually enforced rather than merely confirmed by inspection.
- Add a per-region byte-length assertion (round 3 fix — the Goal's "non-empty, not silently shrunk to a vacuous substring match" claim previously had no implementing task; **round 4 fix**: `.release-test-id-baseline.txt` is a sorted pytest-test-id list and cannot hold byte-length rows, and no Phase 1.5 task recorded them; **round 5 fix**: the tolerance and exemption mechanism were both still undefined even after round 4's separate-file fix). Phase 1.5 records each anchor-bounded region's byte length into a **separate** committed data file, `tests/parity/.release-region-length-baseline.tsv` (columns: `region`, `file`, `byte_length`, `exempt` — round 5 fix, added `file` and `exempt` columns), with a `# captured-at: <sha>` header. This phase asserts, for the **last row per `(region,file)`** whose `exempt` column is empty, the current byte length of `region` in `file` is non-empty and within **10% or 20 bytes (whichever is larger) of that last row's `byte_length`** (round 5 fix — a concrete tolerance, not "a documented tolerance" with no number); rows with a non-empty `exempt` value are skipped, read directly from the `.tsv` (round 5 fix — not parsed out of Phase 2/3's prose mapping tables, which have no exemption field). This phase also asserts the `.tsv`'s `# captured-at:` commit is a strict ancestor of `phase2_first_commit` (from `tests/parity/.release-baseline-meta.json`) — the same anti-rebaseline pin as the pytest-id baseline above (round 6 fix: the `.tsv` capture commit is a strict ancestor of **Phase 2's first commit and Phase 4's tree** — not of Phase 1.5's first commit, which is impossible since the `.tsv` is recorded in Phase 1.5's step (4), after steps (1)-(2) commit; and the assertion reads the **last row per `(region,file)`** since Phase 2/3 append rows for shrunk/relocated regions — grilled decision 11), since Phase 3 appends rows to this same file and an out-of-order re-run must not silently recapture it.
- The Phase 1.5 scratch-copy rename regression test already proves anchor robustness in isolation; this phase's coverage check is the complementary proof that the anchors still bound the *same logical regions* after Phases 2–3's real prose movement.
- Confirm this phase's own checks pass cleanly given Phases 1.5–3 have already landed the anchor and prose changes they depend on — by construction, since Phase 1.5 now runs first, this phase adds no new anchor-stability risk, only the coverage-narrowing proof.

<!-- reviewed: 2026-09-20 @ 3a1fd9f6bc2684c3b80975a04c19a7fc7b465ec1 -->

## Findings

(populated during implementation/review — do not pre-fill; Phase 1's committed inventory and Phase 2/3's mapping tables land here as they're produced)

### review-plan disposition (2026-09-17)

Full 44-finding review-plan report (13 Critical, 22 Important, 9 Minor, 12 Contradiction) is persisted at `.review-plan/latest-claude.json` from that run. Disposition:

- **5 genuine design decisions**, grilled and accepted as recommended (see "Grilled decisions" under Context above): script distribution model (bundled), A2.5 default-on, A2→A2.5 interface (existing output, unchanged), xhigh #9/#11 out of scope, Phase 1.5 sequencing insertion.
- **All other findings** (Missing Task, Testing Gap, Risk, Constraint, Ambiguity, Sequencing, Nonexistent Reference, and the remaining Contradiction findings that were downstream of the 5 decisions above) had a single clear fix and are incorporated directly into the Requirements/Review Focus/Architecture & Call Flow sections and the phase checklists above — not left as open findings.

### review-plan disposition, round 2 (2026-09-18)

Re-review of the revised plan (run_id `20260918T143631Z`, plan_hash at review time `0dd0ea149ebc9056bcda40f156138463c6815357`), persisted at `.review-plan/latest-claude.json`. 44 findings (8 Critical, 21 Important, 15 Minor). Disposition:

- **Re-grilled decision 1** (script distribution model): four of the 8 Critical findings (2 Contradiction, 1 Constraint, 1 Hidden dependency, 1 Environmental Fact) all converged on the same root cause — `BUNDLE_SKILLS` is the auto-fix-applier pipeline roster, not a generic bundler, and decision 1's "no hand-authored-per-mirror precedent" rationale was false (`review-gauntlet/lib/` and `fan-out.sh` both are precedents). Re-grilled via `AskUserQuestion`; accepted recommendation: switch to hand-authored per-mirror `scripts/`, `review-gauntlet/lib/` pattern with a new `RELEASE_LIB_PARITY_FILES` byte-parity check — see the revised Context/Requirements/Architecture/Phase 2 sections above.
- **1 Critical Nonexistent Reference**: `deep-review/scripts/persist-common.sh` doesn't exist at the cited path (`deep-review/scripts/lib/persist-common.sh` is correct, but no longer the operative precedent once decision 1 was redecided) — fixed by pointing Phase 2 at `review-gauntlet/lib/` instead.
- **2 Critical findings on the superset-of-test-ids check** (Testability + Testing Gap): the check was unenforceable as specified — no captured baseline, ambiguous "retarget" semantics, and (per two Important-severity Contradiction findings) an impossible ask to span both pytest and the bash script-suite convention at once. Fixed directly (standard fix, not grill-eligible — a testing-mechanism definition gap, not an architecture judgment call): "retarget" is now defined in Requirements, a baseline-capture task is added to Phase 1, and the coverage check is scoped to the pytest prose-contract file only, with bash-suite registration as a separate proof.
- **Remaining Important/Minor findings** (component-list omissions in Architecture & Call Flow, the `normalize_release_workflow()` forbidden-substitution-class conflict, Phase 1.5's ~15-helper vs ~142-call-site/~60-test-body underscoping, the A2→A2.5 medium ambiguity post-Phase-2, the unfounded `codex:rescue` scoping claim, the absolute "must not pay context cost at all" Requirements contradiction, and the missing README.md/20260712-plan anchor files) each had a single clear fix and are incorporated directly into the sections and phases above — not left open.
- **Not fixed, logged as accepted framing (Minor, informational):** the architecture lens noted the progressive-disclosure split moves ~20% of `SKILL.md`'s bytes (the template-only regions), leaving the dominant untemplated-path prose (~102K of ~247K) untouched, so the post-refactor file (~195K estimated) stays roughly 2x the next-largest skein skill rather than fully matching the Objective's "matches every other complex skein skill" framing. This plan does not expand scope to also compress untemplated-path prose — noted here as a disclosed gap between the Objective's framing and this plan's actual reach, not a defect requiring a phase.

### review-plan disposition, round 3 (2026-09-19)

Re-review of the round-2-revised plan (run_id `20260918T171659Z`, plan_hash at review time `2632cb37f2c3c7c413a70f7dd2604ea88ae48626`), persisted at `.review-plan/latest-claude.json`. 46 findings (9 Critical, 26 Important, 11 Minor) after the sequential Contradiction Pass. Disposition:

- **Re-grilled decision 6** (new — path-anchor bridging mechanism): round 2's sentinel-token idea (`__RELEASE_SCRIPT_ROOT__`) turned out to have no expansion mechanism on either harness, and the premise for inventing it (that `normalize_release_workflow()` forbids all path-anchor divergence handling) was itself wrong — it only forbids prefix-match/free-form substitution, and already permits exact-full-line divergence pairs. Re-grilled via `AskUserQuestion`; accepted recommendation: each mirror's `SKILL.md` prose carries its own real anchor, and `normalize_release_workflow()` gains one new exact-full-line divergence pair per call site — the same shape as its existing six sanctioned pairs (also corrected: the plan previously miscounted this set as "four" and separately "five"). See the revised Context/Requirements/Architecture/Phase 2 sections above.
- **Standard fixes applied directly** (all judged mechanism/definition gaps, not architecture judgment calls): the `RELEASE_LIB_PARITY_FILES` citation was missing the `GAUNTLET_LIB_PARITY_FILES` precedent's enumeration half (now both halves are in scope); new bash script-unit suites were never wired into `justfile`'s `ci:` dependency list (now an explicit Phase 2 task, plus a Phase 4 enumeration check as backstop); the A2 characterization test was scheduled a phase too late relative to Phase 2's extraction (moved to Phase 1.5, with Phase 2/3 re-running it as a regression check); the "no-behavior-drift" fixture-diff task specified an impossible byte-identical comparison of non-deterministic LLM-generated prose (redefined to diff the extracted scripts' deterministic stdout/exit codes instead); the pytest-id baseline had no machine-readable format or tree-state pin (now a committed data file plus a recorded capture-commit ancestry check); the "retarget" definition's load-bearing clause was unobservable prose (now requires committed evidence of a deliberate-mutation test failure per mapping-table row); `README.md`'s anchor mechanism was misidentified as a `.index()` literal lookup when it's actually a table-row prefix/cell-equality match (corrected, given its own treatment); the "templated-branch region" anchor Phase 3's static assertion depends on was never created by any phase (now added in Phase 1.5); Phase 1.5 was the largest test-mutating phase with no mapping table or in-phase coverage gate (now has both); Phase 4's "same logical regions, non-empty" claim had no implementing task (now a per-region byte-length assertion); and three factual overclaims were corrected (release is not the repo's *only* byte-compared mirror pair, only its only *normalized*-comparison pair; the content-review `references/` precedent actually ships two files with a branching load rule, not one; "~60 test bodies" was a correct anchor-bearing subset of 119 total functions, not a miscount needing to be "119").
- **Rejected finding**: the codebase-claims lens's claim that Phase 1.5's scope should be corrected from "~60" to "119" was itself incorrect — the assumptions lens and the Contradiction Pass both verified 60 is the count of anchor-*bearing* test bodies out of 119 total, a different (and correctly stated) quantity. The plan now states both numbers explicitly to remove the ambiguity that caused the misread, without changing the substance.

### review-plan disposition, round 4 (2026-09-19)

Re-review of the round-3-revised plan (run_id `20260919T091715Z`, plan_hash at review time `0b954ac7620136f7193fca959bf64426cd6dd2f5`), persisted at `.review-plan/latest-claude.json`. 66 findings (18 Critical) after the sequential Contradiction Pass, converging on roughly 7 distinct root issues (many Critical findings were independent lenses catching the same defect). Disposition:

- **Re-grilled decision 7** (new — A2 characterization test / fixture-diff "before" state): round 3's fix of moving the characterization test to Phase 1.5 created a new defect — no comparable artifact across the Phase 1.5 → Phase 2 boundary (prose vs. script stdout), and round 3's redefined fixture-diff task had no "before" state since Phase 2 is what creates the scripts being diffed. Re-grilled via `AskUserQuestion`; accepted recommendation: Phase 1.5 records A2's classification *value* as a committed golden fixture; Phase 1.5's test asserts the prose path produces it; Phase 2's test asserts the extracted script's stdout produces the same value, which also supplies Phase 2's fixture-diff "before" state. See the revised Context/Phase 1.5/Phase 2/Phase 3 sections above.
- **Root cause: round 3's own "fix" of the divergence-pair template was itself a third miscount.** Four lenses plus the Contradiction Pass independently found `normalize_release_workflow()`'s actual machinery is seven cardinality gates guarding four contracts, of which only two are the symmetric exact-full-line pair shape (not "six pairs, one function" as round 3 said, after round 2's "four" and round 3's intermediate "five") — each sanctioned pair spans four file regions, not one function edit. This was a standard mechanism-description fix (not grill-eligible — a verifiable code-shape claim, not an architecture judgment call), applied throughout Context/Requirements/Architecture/Phase 1/Phase 2 above. A related overclaim was also corrected: round 3's "release is the only *normalized*-comparison mirror pair" is false — `fan-out`'s prompt-pair normalizer is a second one, using the exact free-form-substitution class `normalize_release_workflow()`'s header calls forbidden (proving that restriction is function-local, not repo-wide).
- **Standard fixes applied directly** (mechanism/definition gaps, not architecture judgment calls): mirror-atomicity (required by `AGENTS.md`) was conflated with phase-atomicity (not required) in Requirements — split apart, noting Phase 1.5's now-large scope as the reason this matters; the `RELEASE_LIB_PARITY_FILES` citation was still missing 2 of 4 actual `GAUNTLET_LIB_PARITY_FILES` precedent mechanisms (`PARITY_GAUNTLET_LIB_ROOT` test-override fixture, G9 self-test proving the enumeration guard isn't vacuous — now all four are in scope); Phase 4's per-region byte-length assertion had no real recording mechanism (`.release-test-id-baseline.txt` can't hold byte-length rows) — Phase 1.5 now records to a separate `tests/parity/.release-region-length-baseline.tsv`; no phase re-anchored Phase 1.5's region helpers when Phase 3 moves their content into `references/` — added as a Phase 3 task.
- **Remaining Important/Minor findings** (the `-ne 1` pin's same-invocation-line-twice collision risk and its full-mirror-diff-skip side effect, the `ci:` recipe's two-hop bash-suite-registration resolution path, mutation-evidence schema looseness, mapping-table-completeness gate wording) each had a single clear fix and are incorporated directly into the phase checklists above — not left open.
- **Observed pattern, flagged to the user rather than silently repeated a fifth time**: every round so far (2, 3, 4) has found new Critical issues in the immediately-preceding round's own fixes, most often the same divergence-pair count being re-miscounted. Recommend a round 5 before treating this plan as converged, given round 4's fixes touch the same mechanism round 3 got wrong.

### review-plan disposition, round 5 (2026-09-19)

Re-review of the round-4-revised plan (run_id `20260919T134438Z`, plan_hash at review time `8f5c1800599412418d8fa3a4937ce279a28a9c77`), persisted at `.review-plan/latest-claude.json`. 48 findings (14 Critical, 20 Important, 14 Minor) after the sequential Contradiction Pass. Disposition:

- **The divergence-pair mechanism claim is now confirmed correct — first round where it survives verification.** All five lenses independently re-verified `normalize_release_workflow()`'s shape (7 gates / 4 contracts / 2 symmetric pairs / 4 regions, per round 4's Context decision 6) directly against `scripts/check-prompt-parity.sh`, and the `fan-out` second-normalized-pair claim, and every other previously-contested line-number/count claim (119/60/142 test-anchor counts, `GAUNTLET_LIB_PARITY_FILES`'s four mechanisms including `PARITY_GAUNTLET_LIB_ROOT` and G9). Zero findings this round dispute any of that. The assumptions lens flagged one Minor imprecision (the four file-region ranges are machinery-wide, not pair-specific — the two symmetric pairs occupy narrower sub-ranges) and one Minor inconsistency (three different `SKILL.md` byte/line-count figures in the plan's own prose) — both noted, not yet fixed (low-risk, superset ranges).
- **Root cause: round 4's disposition claimed several fixes were "incorporated directly into the phase checklists" that were never actually written into any checklist bullet — only described at the Context/Requirements level.** This is a different failure mode than rounds 2-4's repeated re-miscounting: the *content* of round 4's decisions (golden-value comparison, the `-ne 1` collision risk, the `.tsv` recording mechanism) was correct, but the disposition's claim that it had been propagated into every phase's task list was false for at least four items (Phase 2's fixture-diff bullet still specified the discredited before/after live capture; the `-ne 1` collision fix was never given a concrete mechanism in any phase task; the byte-length exemption format was still undefined; the mutation-evidence schema was never adapted for Phase 1.5's anchor-only retargeting). All four are fixed directly this round (standard fixes — verifiable propagation gaps, not architecture judgment calls), throughout Phase 1/1.5/2/3/4 above.
- **Re-grilled decision 8** (new — A2.5 reference-file placement): Phase 3 originally placed A2.5's inference prose inside the same templated-branch-gated `references/template-subsystem.md` as the template schema, but A2.5 must also run via `--infer-template` on untemplated repos — the templated-branch gate would leave that entry point with no reference file to load on exactly the repos it's most needed for. Re-grilled via `AskUserQuestion`; accepted recommendation: split A2.5's prose into its own `references/audit-inference.md`, gated on Audit Mode running at all rather than on template presence — matching the `content-review` precedent's real two-file/branching-trigger shape. See the revised Context/Requirements/Architecture/Phase 3 sections above.
- **New architectural finding, fixed directly (standard fix — a Requirements-level invariant, not itself a judgment call once decision 8 resolved the file split): `references/` files must carry no path-anchor lines.** Phase 3's planned `references/` byte-parity arm is plain byte-identity (modeled on `content-review/references`, no normalizer); if a `references/` file carried one of Phase 2's real per-mirror anchors, that byte-identity check would fail by construction, and removing the line from `SKILL.md` would also drop Phase 2's per-call-site divergence-pair count from 1 to 0, breaking that gate too. Resolved by stating the invariant explicitly: script-invocation lines with real anchors always stay in `SKILL.md`; only anchor-free descriptive/schema/inference prose moves to `references/`.
- **Fixture-diff scope completed (standard fix): round 4's golden-value approach (decision 7) covered only the single A2 marker-absent case, leaving every other extracted script (jq-gate validation, commit-precondition check) with no before-state for Phase 2/3's byte-diff task — the exact defect class decision 7 was supposed to close, recurring in the part of its scope round 4 didn't extend.** Fixed by committing one golden fixture per extracted script per fixture-repo case in Phase 1.5, and rewriting Phase 2's fixture-diff bullet (which had never been updated from round 3's original "capture before this phase's changes" wording — an impossible ask once Phase 2 is understood as the phase that creates the scripts) to diff against those golden values instead of a live before/after capture. The representative templated/untemplated fixture repos themselves — referenced by Review Focus and Phase 2/3 since round 1 but never created by any phase — are now a named Phase 1.5 deliverable.
- **Remaining Important findings, fixed directly** (mechanism/definition gaps): Phase 1.5's internal task order pinned (fixture repos → anchors → golden capture → byte-length recording → mapping table/coverage gate/rename test); Phase 1's contradictory `n/a` Test/Validation commands corrected to match its own justfile-recipe-creating task; Phase 1/1.5's Impl-files lists extended to include the golden fixtures, fixture repos, baseline files, and mapping-table/first-commit-SHA Findings entries those phases' own tasks create; the region-length baseline given a `file` column (so Phase 4 can resolve a relocated region) and an anti-rebaseline ancestry pin matching the pytest-id baseline's; Phase 3's append-vs-edit conflict on that baseline resolved as append-only; Phase 3's vacuous fixture-diff task (it touches no script) replaced with "re-run Phase 2's existing golden comparisons and confirm they still pass"; the fail-open "any tripped gate skips the entire normalized diff" property disclosed as accepted existing behavior, not something this plan introduces or must fix; the bundle-reference enumeration check (section 7) and the G9/`PARITY_GAUNTLET_LIB_ROOT` self-tests' recursion-skip guards both given explicit Phase 2 tasks so release's new lines don't silently trip either mechanism.
- **Not yet independently re-verified**: this round's fixes have not themselves been through a fresh lens pass. Given round 5 found zero new disputes of round 4's core mechanism claim (a first), but did find a new failure mode (claimed-vs-actual checklist propagation) affecting roughly a third of round 4's disposition, a round 6 is recommended before treating this plan as converged — though the bar for round 6 should be narrower: confirm this round's checklist edits actually landed as described, rather than re-deriving the underlying mechanism facts from scratch again.

### review-plan disposition, round 6 (2026-09-20)

Re-review of the round-5-revised plan against post-#43 `main` (run_id `20260920T081434Z`, plan_hash at review time `c98f4fd45361aed776895bf6b40f5ae1b2aace1f`), persisted at `.review-plan/latest-claude.json`. 36 findings (2 Critical, 17 Important, 17 Minor) after the Contradiction Pass (10 findings). All round-5 mechanism claims (7 gates/4 contracts/2 pairs/4 regions, GAUNTLET_LIB precedent, line numbers and byte counts) re-verified against current `main` with no drift; every defect was in the fixture/golden/baseline test machinery round 5 introduced. Disposition:

- **Grilled decision 9** (new): scripts are pure over injected local inputs; fixtures are static files plus a deterministic `tests/release/lib.sh` builder — committed nested git repos are not storable and could never carry A2's GitHub-side inputs. Pinned executables cross via env vars.
- **Grilled decision 10** (new): goldens are value-level JSON projections in `tests/release/golden/`, compared in a Phase 2 bash suite wired into `ci:`.
- **Grilled decision 11** (new): `.release-region-length-baseline.tsv` stays append-only, Phase 2/3 append rows, Phase 4 reads the last row per `(region,file)`; ancestry pins read `# captured-at:` headers, and the tsv pin targets Phase 2's first commit (the earlier Phase-1.5-first-commit rule was unsatisfiable).
- **Standard fixes applied directly**: Claude anchor form corrected to `${CLAUDE_PLUGIN_ROOT}/skills/release/scripts/<name>.sh` (Codex `"$SKILL_DIR"/scripts/<name>.sh`); `--collect-only -q` filter to `::` lines; nonexistent `GAUNTLET_LIB_PARITY_ROOT` corrected to `PARITY_GAUNTLET_LIB_ROOT` with all three guards; §7's Claude-only grep disclosed; Phase 2 now extends `test-prompt-parity-extended.sh`'s seed and self-tests the new gates; Phase 2 appends `.tsv` rows and asserts the A2→A2.5 field contract; stale no-op `pytest`→`uv run` task deleted; Audit Mode anchor moved to Phase 1.5; Phase 1.5 coverage gate named (`just release-baseline-check`); README rename-regression observable specified; `--infer-template` catalogue test retargeted to a test that pins the subcommand list; Phase 4 registers its enumeration check in `parity-tests`; stale single-file wording removed; anchor-count figures marked approximate.
- **Not yet independently re-verified**: these fixes have not been through a fresh lens pass. Rounds 2-6 each found defects in the preceding round's own fixes; recommend a narrow round 7 confirming propagation only before conduct.

### review-plan disposition, round 7 (2026-09-20)

Narrow confirmation round on the round-6 edits (run_id `20260920T181224Z`, plan_hash at review time `ed7ff6323651ec9cfaebd9774ec3176daf9f1a76`), persisted at `.review-plan/latest-claude.json`. 30 findings (0 Critical, 14 Important, 16 Minor) after the Contradiction Pass (7 findings); the codebase-claims lens verified 38 references with none wrong (its records were written on its behalf from its clean reply — no per-section progress). All defects were propagation gaps or seams in round 6's own additions. Disposition:

- **Grilled decision 12** (new): extracted scripts emit structured JSON (classification fields only); `SKILL.md` renders human text; decision 3 amended. This resolves the JSON-vs-human-text tension with decision 10's `jq -S` projection and the A2.5 field-source contradiction (fetch results are inputs, not outputs).
- **Standard fixes applied directly**: A2→A2.5 field-contract assertion moved from Phase 2 to Phase 3 (where `references/audit-inference.md` is created); boundary SHAs recorded machine-readably in `tests/parity/.release-baseline-meta.json` (`phase15_first_commit`, `phase2_first_commit`), with Phase 2 gaining the task; `.tsv` tolerance measured against the last row, `exempt` narrowed to deliberately-emptied regions; `release-baseline-check` registered in `ci:`/`parity-tests` in Phase 1.5; Phase 4 enumeration narrowed to `tests/release/test-*.sh` with the two-hop suite→recipe→`ci:` check and `justfile:27-40`; Phase 3 Impl files gain the `.tsv` and the `tests/release/` assertion; golden key set (incl. `exit_code`) fixed in Phase 1.5; manual golden-capture procedure specified (human supplies fixture files as mocked fetch results); `RELEASE_JQ` required only where jq runs, `gh` dropped from the script-side env set; subcommand-list-pinning test given a target file; stale raw-stdout, "fixture repo", moved-characterization-test, and superseded ancestry wording removed.
- **Not yet independently re-verified**: as with every prior round, these fixes have not been through a fresh lens pass. Rounds 2-7 each found defects in the previous round's fixes, though round 7's were all Important/Minor propagation, with no Critical findings and no mechanism-level errors — the trend is converging.
