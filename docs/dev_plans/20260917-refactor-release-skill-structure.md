# Task: skein:release — progressive disclosure, script extraction, audit split, test anchor fragility

**Status**: Draft
**Component**: meta
**Assigned to**: Claude
**Priority**: Medium
**Branch**: TBD
**Created**: 2026-09-17
**Review Gates**: full

## Objective

Reduce `plugins/skein/skills/release/SKILL.md` (243,730 bytes / 456 lines, per `wc -lc` — round 5 fix: the plan previously stated three different figures for this file, "244K, 454 lines" here and "~247KB" later in this same paragraph, and "~102K of ~247K" in round 2's Findings; the Objective's own figures all now read from one measurement, while round 2's disposition figures ("~102K of ~247K", "~195K estimated") stand as historical estimates taken against the older reading, not restatements of it) — roughly a third of the plugin's Claude-mirror skill-corpus byte total, though skills contribute only name+description until invoked, so the real cost is this file's bytes paid per `/release` invocation, not a standing context tax) from a monolithic always-loaded prose spec into a structure that matches every other complex skein skill (`deep-review`, `review-plan`, `review-gauntlet`, all of which ship a generated `scripts/` bundle and keep `SKILL.md` as the orchestration layer, not the mechanism). This is a structural refactor: behavior changes are limited to the three explicit exceptions named in Requirements below (A2.5's additional `--infer-template` entry point, that entry point bypassing A2.5's template-absent gate per grilled decision 16, and the test suite's assertion *targets* moving alongside the prose they check) — the untemplated `/release` cut (the common case, including every skein release to date) must not regress, and every templated-branch behavior currently documented in prose must be preserved, whether verbatim or via a named, reviewed equivalent.

## Context

`/code-review xhigh --fix` against `feature/release-repo-template` (2026-09-17, findings recorded in `docs/dev_plans/20260914-feature-release-repo-template.md`) surfaced 15 findings, nearly all of them spec self-contradictions or coverage gaps *within* the template subsystem added by that plan. Reviewing the pattern across findings (#2, #5, #6, #9, #11, #14 in particular) shows the underlying cause isn't isolated prose bugs — it's that deterministic validation logic (template read, jq gate sequence, marker resolution, commit-precondition checks) is re-specified in English at roughly 5 call sites across 2 mirrors (~10 near-duplicate copies), with no single source of truth an editor or reviewer can check for consistency. Every other complex skein skill avoids this by extracting the deterministic parts into `scripts/`, leaving `SKILL.md` to orchestrate.

Three further structural issues surfaced in follow-up review (2026-09-17, this session):

1. **No progressive disclosure.** The template subsystem (schema, jq gates, Audit Mode's dual-anchor scheme, A2.5 inference) is only relevant on the templated branch, yet lives inline in `SKILL.md` and is loaded on every invocation — including the untemplated case, which is the norm today (skein's own repo carries no template).
2. **A2.5 depends on A2's classification-source field.** Audit Mode's template-inference step (A2.5) proposes a template from release history, but its candidate set is defined over A2's *resolved classification source* — a one-directional A2→A2.5 data dependency binding a generative inference step to a read-only classifier inside the same mode. Proposing a template against a repo that deliberately removed one is A2.5's **purpose**, not the risk (grilled decision 16); A2.5's only guard is that it must not propose a canonical shape A2 could not classify.
3. **Parity tests anchor on prose headings.** `tests/parity/test_release_skill_contract.py`'s region helpers locate sections via `text.index("### Step 1: ...")`-style literal heading lookups; an editorial heading rename raises `ValueError` instead of a meaningful test failure, making the test suite fragile to exactly the kind of copy-editing this refactor will do.

This plan is scoped to structure only. It does not attempt to resolve the 14 unfixed content findings from the xhigh review (marker match-count precedence, Step 6 recompose ordering, jq-pin authorization for new call sites, CRLF/Unicode gate gaps, etc.) — those are logic bugs in the current prose and are out of scope here, though several should get materially easier to fix once the duplicated prose collapses into a single script. This includes findings #9 and #11 specifically (Audit Mode's marker-absent fallback): even if this refactor's A2/A2.5 split makes a fix to #9/#11 obvious, it is **not** folded into this branch — log it as a separate, standalone plan/finding instead, so this plan's own behavior-invariance requirement stays a meaningful check rather than a moving target. See "Related Work" below for the explicit link back to the xhigh findings.

**Grilled decisions (2026-09-17, review-plan Step 6.4, all accepted as recommended):**

1. **Script distribution model** (superseded 2026-09-18, review-plan round 2 — see below): ~~bundled, not hand-authored per mirror...~~ Round 2's architecture, sequencing, and assumptions lenses independently found this unworkable: `BUNDLE_SKILLS` is not a generic per-skill script-bundling list, it is specifically the **auto-fix applier pipeline roster** (`scripts/bundle-appliers.sh`, `scripts/check-sync.sh`, `tests/parity/test-applier-bundle-parity.sh` all assume every registered skill has an applier and consume `BUNDLE_SHARED`'s auto-fix-only files); registering `release` there either hard-fails under `set -euo pipefail` (`bundle_applier_for`'s `*)` arm returns 1 for an unknown skill) or, once an applier arm is stubbed in, ships six inoperative auto-fix files (`reconcile-findings.sh`, `audit-auto-fix-eligibility.sh`, `plan-scope-detect.sh`, `auto-fix-allowlist.json`, `lib/auto-fix-common.sh`, `lens-budget.sh`) into release's mirrors, violating `bundle-map.sh`'s own stated invariant ("bundled stays synonymous with operative"). The decision's rationale was also factually wrong: `plugins/*/skills/review-gauntlet/lib/` **is** an existing hand-authored-per-mirror precedent (6 files, byte-parity-enforced via `GAUNTLET_LIB_PARITY_FILES` in `tests/parity/test-applier-bundle-parity.sh:138`, documented in `AGENTS.md`'s own "### review-gauntlet `lib/` (authored, mirror-parity-enforced)" subsection — which this plan's Related Work section cites two lines below the now-corrected claim). `plugins/*/skills/fan-out/fan-out.sh` is a second hand-authored-per-mirror precedent.

   **Redecided (2026-09-18, review-plan round 2 re-grill, accepted as recommended): hand-authored per-mirror, `review-gauntlet/lib/` pattern.** New scripts are authored separately in `plugins/skein/skills/release/lib/` and `plugins/skein-codex/skills/release/lib/` (**superseded by decision 23**: the Claude half lands in Phase 2 and the Codex half is authored by `codex:rescue` in Phase 3.5, mirroring the finished Claude half byte-for-byte modulo path anchors, rather than Codex-first inside each Phase 2 commit). Byte-parity is enforced via a new `RELEASE_LIB_PARITY_FILES` array in `tests/parity/test-applier-bundle-parity.sh`, modeled on the **full** `GAUNTLET_LIB_PARITY_FILES` precedent — not just its byte-identity loop (lines ~138-147, this plan's earlier, incomplete citation) but also its **enumeration half** (lines ~151-210), which exists precisely because a hand-maintained array silently exempts any future script someone forgets to register from mirror parity forever (round 3 fix: citing only the byte-identity loop reproduces the exact hole the enumeration half was written to close — see Requirements/Phase 2 below for the corrected task). Including the documented anchor-divergent-exclusion carve-out for any script that legitimately resolves `${CLAUDE_PLUGIN_ROOT}` vs `$SKILL_DIR` (matching `gauntlet-common.sh`'s treatment). No `BUNDLE_SKILLS` registration, no `bundle-appliers.sh`/`check-sync.sh`/auto-fix-pipeline involvement — release's `lib/` scripts carry no auto-fix mode, so nothing in that pipeline applies to them.
6. **Path-anchor bridging** (added 2026-09-19, review-plan round 3 grill, accepted as recommended; template corrected 2026-09-19, round 4): round 2's `SKILL.md`-prose sentinel-token idea (`__RELEASE_SCRIPT_ROOT__`) does not work — no mechanism in either harness expands an arbitrary token in skill prose, and round 3's architecture and assumptions lenses independently found the premise for inventing it was also wrong: `normalize_release_workflow()` only forbids prefix-match/free-form substitution, and already permits exact-full-line divergence pairs. **Redecided: each mirror's `SKILL.md` prose carries its own real anchor** (`${CLAUDE_PLUGIN_ROOT}` on Claude, `$SKILL_DIR` on Codex) at every new script-invocation call site, and `normalize_release_workflow()` gains one new divergence pair per call site — the same shape as its existing sanctioned pairs, not a new mechanism class. **Round 4 correction of the template itself**: the existing machinery is seven cardinality gates guarding four contracts, of which only two (invocation-mode, execution-model) are the symmetric exact-full-line pair shape being followed — not "six pairs" as round 3 stated (a third miscount, after round 2's "four" and round 3's own intermediate "five"); each sanctioned pair spans four regions of `scripts/check-prompt-parity.sh` (shell constants, `awk -v` list, normalizer branches, count-call-plus-gate), not one function edit. See Requirements/Architecture/Phase 1/Phase 2 below for the corrected mechanism.
2. **A2.5 stays default-on** inside `/release audit` — the new `--infer-template` entry point is *additional*, not a replacement, so plain `/release audit` keeps emitting the inference proposal exactly as today. No behavior change on this axis.
3. **A2→A2.5 interface**: A2's classification output stays the interface, **amended round 7 by decision 12 below**: after Phase 2 the extracted script emits structured JSON (classification fields only) and `SKILL.md` renders the human-readable text from it; A2.5 parses those JSON fields. No new persistence artifact (exact fields named in Phase 3 below).
4. **xhigh findings #9/#11 stay out of scope** for this plan, full stop — no follow-up-commit clause, even if the A2/A2.5 split makes a fix obvious mid-implementation.
5. **Phase sequencing**: a new Phase 1.5 lands the stable-anchor markers *before* any prose moves (trivially green), so Phases 2–3 move prose against already-stable anchors instead of racing Phase 4 to fix what they just broke.
7. **A2 characterization test / fixture-diff "before" state (added 2026-09-19, review-plan round 4 grill, accepted as recommended; scope completed 2026-09-19, round 5 fix).** Round 3 moved the A2 characterization test to Phase 1.5 to fix a sequencing defect, but round 4 found this created two new defects: (a) the test has no single comparable subject across the Phase 1.5 → Phase 2 boundary — pre-extraction A2 output is model-read prose, post-extraction is script stdout, different artifacts; (b) round 3's redefinition of the fixture-diff task (diff extracted-script stdout instead of LLM prose) means Phase 2 is the phase that *creates* those scripts, so there is no "before" script-stdout state to diff against. **Redecided: Phase 1.5 captures A2's classification *value*, not raw prose/transcript, as a committed golden fixture** (e.g. `tests/release/golden/a2-marker-absent.json`, recording the classification for the marker-absent, no-current-template case). Phase 1.5's own Findings entry records this capture as a **manual, human-verified step** (there is no pytest/bash harness that can execute the prose-driven, pre-extraction LLM path — round 5 fix: round 4's "Phase 1.5's own test asserts..." language wrongly implied an automated check existed for this side of the comparison; only the post-extraction, script-stdout side is machine-asserted). **Round 5 fix — decision 7's scope was previously A2-only, leaving every other extracted script (jq-gate validation, commit-precondition check, marker resolution) with no before-state**: Phase 1.5 commits one golden fixture **per extracted script, per fixture-repo case** (templated + untemplated), not just the A2 marker-absent case; the full golden set is what supplies Phase 2's/Phase 3's fixture-diff "before" state for every script, not only A2's. See Phase 1.5/Phase 2/Phase 3 below.
8. **A2.5 reference-file placement (added 2026-09-19, review-plan round 5 grill, accepted as recommended).** Phase 3 originally moved A2.5's inference prose into the same `references/template-subsystem.md` file as the template schema and jq-gate documentation, gated on the templated-branch trigger (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release) — but A2.5 must also run via the standalone `--infer-template` entry point on repos with **no** current template (that is A2.5's entire purpose per Context: proposing a template from release history when one was deliberately removed), so the templated-branch gate would leave `--infer-template` on an untemplated repo with no reference file to load. **Redecided: A2.5's inference prose splits into its own file, `references/audit-inference.md`, loaded whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of whether a template currently exists.** `references/template-subsystem.md` keeps only the template schema, jq-gate documentation, and dual-anchor marker resolution, and stays templated-branch-gated. This matches the `content-review` precedent's actual shape (two reference files, each with its own trigger) that round 3 already established as the real model, rather than round 3/4's continued one-file assumption. See Requirements/Architecture/Phase 3 below.
9. **Script input seam and fixture construction (added 2026-09-20, review-plan round 6 grill, accepted as recommended).** Round 5 specified fixtures as committed scratch git repos with commit/tag state that scripts run against; round 6 found that is unrealizable twice over: git stores a nested `.git` as a gitlink (repo convention is `git init` in mktemp at test time), and A2's real inputs are GitHub-side (`gh release list`/`gh release view --json body`, `git ls-remote --tags` peeled SHAs, release bodies carrying `release-template-sha` markers), none of which a local repo has. **Redecided: the extracted scripts are pure over injected local inputs** (release body text, release list JSON, peeled-commit SHA passed as files/argv/stdin); `SKILL.md` prose keeps the `gh`/`git ls-remote` fetches and pipes results in, and Phase 2's mapping table states which A2.5 fields come from script stdout vs the fetch — fetch results are script *inputs*, never outputs, so script stdout is the sole source of A2's *classification* fields, and A2.5's field-contract assertion covers only those. Fixtures are static input files under `tests/release/fixtures/` (a release body as a file, a release-list JSON, SHAs); where git state is unavoidable a deterministic builder in `tests/release/lib.sh` does `git init` in mktemp with pinned identity/dates. Pinned executables cross into scripts as pre-resolved absolute paths via documented env vars (`RELEASE_JQ`, `RELEASE_GIT` — `gh` is never used by a script, since fetches stay in `SKILL.md` prose), which the script re-checks (absolute, exists, executable) rather than re-resolving (`SKILL.md` Step 1b forbids an executable-selector environment as a *resolution* source). `RELEASE_JQ` is required only on code paths that invoke jq: Step 1b pins jq only on the templated branch, so the untemplated case of `read-release-template.sh` must succeed with it unset, fail-closed otherwise; the pytest suite's jq-gate subprocess tests override the same variables.
10. **Golden schema (added 2026-09-20, round 6 grill, accepted as recommended).** Phase 1.5 goldens pin a **value-level JSON projection** (the classification/decision fields), recorded manually from the pre-extraction prose path; Phase 2/3 assert a defined `jq -S` projection of each script's JSON stdout (decision 12) equals the golden — Phase 1.5 fixes the projection's key set, including `exit_code`, and Phase 2 asserts key-set equality, so a Phase 2 subset projection cannot weaken the golden — so output formatting may differ but decisions may not. This removes the requirement that Phase 1 pre-define byte-level stdout for scripts that do not yet exist. Goldens live in `tests/release/golden/`; the comparisons are cases in a Phase 2 bash suite under `tests/release/` wired into `ci:`.
11. **Region-length baseline rules (added 2026-09-20, round 6 grill, accepted as recommended).** `.release-region-length-baseline.tsv` stays append-only; Phase 2 and Phase 3 each append one row per region they shrink or relocate in the same commit; Phase 4 asserts only the **last row per `(region,file)`** against the 10%/20-byte tolerance, and a region relocated to a new `file` supersedes its earlier `(region, SKILL.md)` row. `exempt` (a reason string) is reserved for regions deliberately emptied; shrinks and relocations are recorded by appended rows, never exemptions. Phase 4's tolerance is measured against the **last row's** `byte_length` (each phase's append records the accepted post-change length), so unrecorded drift after the last append fails. The baseline side of each ancestry pin is a machine-readable `# captured-at: <sha>` header (tsv) or first-line comment (test-id baseline); the boundary side (Phase 1.5's and Phase 2's first commits) is recorded by each phase's last commit in `tests/parity/.release-baseline-meta.json` (`phase15_first_commit`, `phase2_first_commit`), which Phase 4 reads — not this plan's prose.
12. **A2 script emits JSON (added 2026-09-20, review-plan round 7 grill, accepted as recommended).** Decision 3's "human-readable text" interface conflicted with decision 10's `jq -S` projection, which presupposes JSON stdout. **Redecided: extracted scripts emit structured JSON (classification fields only); `SKILL.md` renders the human-readable text from it and A2.5 parses the JSON fields.** The A2→A2.5 field-contract assertion moves to Phase 3 (where `references/audit-inference.md` is created), reading the field list from a documented section of that file; Phase 2 documents the A2 script's JSON field schema in its mapping table.
13. **Baseline pins stay history pins, and fail hard in CI (added 2026-09-21, review-plan round 8 grill).** *Decision (grilled):* all three baselines (`.release-test-id-baseline.txt`, `.release-region-length-baseline.tsv`, `tests/release/golden/`) keep **commit-SHA ancestry pins**, not content hashes, and there is **no refresh recipe** — a refresh is precisely the silent rebaseline the pins exist to prevent. This rests on one stated constraint: **this branch's history is append-only from Phase 1.5's first commit until merge** (no rebase, amend, or squash; the repo already forbids squash-merge). Phase 1.5 adds `fetch-depth: 0` to `.github/workflows/ci.yml` so CI can resolve ancestry at all, and every ancestry check **fails hard, never skips with a warning**, when history is shallow or `tests/parity/.release-baseline-meta.json` is missing. Sunset after merge is an **explicit removal commit** deleting the meta file *and* the assertions together — never a runtime "skip if the meta file is absent" conditional, which would silently disable the gate everywhere including CI.
14. **Goldens are frozen and authoritative after Phase 1.5 (added 2026-09-21, round 8 grill).** *Decision (grilled):* Phase 1.5 records `golden_capture_commit` in `.release-baseline-meta.json` and commits the fixed key set as `tests/release/golden/schema.json`; the Phase 2 bash suite asserts `git diff --quiet <golden_capture_commit> -- tests/release/golden/` (byte-identical) and reads the key set from the schema file. **A Phase 2/3 disagreement with a golden is a script bug by definition** — never a reason to edit the golden. Capture fidelity is bought up front instead: the manual capture is derived independently by **two readers** (Claude, and Codex via `codex:rescue`) who must agree before the value is committed, and the golden's authority is that agreed reading of the prose at `golden_capture_commit`. A later correction is a disclosed, separately-reviewed re-capture commit that updates `golden_capture_commit`, not an in-phase edit.
15. **Hand-authored release scripts live in `lib/`, not `scripts/` (added 2026-09-21, round 8 grill).** *Decision (grilled):* in this repo `scripts/` under a skill means bundle-generated (`AGENTS.md:63-64`, `scripts/check-sync.sh:15-16`) and `lib/` means hand-authored, mirror-parity-enforced (`AGENTS.md`'s "### review-gauntlet `lib/` (authored, mirror-parity-enforced)"). Release's new scripts therefore land in `plugins/skein/skills/release/lib/` and `plugins/skein-codex/skills/release/lib/`; call-site anchors read `${CLAUDE_PLUGIN_ROOT}/skills/release/lib/<name>.sh` (Codex: `"$SKILL_DIR"/lib/<name>.sh`); `RELEASE_LIB_PARITY_FILES` enumerates over `lib/`; the test-override variable is **`PARITY_RELEASE_LIB_ROOT`** (matching `PARITY_GAUNTLET_LIB_ROOT`/`PARITY_BUNDLE_REF_ROOT`); lint globs list the **Claude mirror only**. §7's bundle-reference grep matches only `skills/<skill>/scripts/<x>.sh`, so it cannot see `lib/` — the §7 registration/exemption task is **dropped**, and Phase 2 instead adds one sentence to `AGENTS.md`'s path-resolution idiom section recording that `lib/` anchors are the hand-authored form.
16. **`--infer-template` topology, and A2.5's real guard (added 2026-09-21, round 8 grill).** *Decision (grilled):* `--infer-template` is defined topologically, not as "re-run A2.5 alone": it runs **A1 → A2 (the extracted script, with punch-list rendering suppressed) → A2.5**, re-entering A2 at its *existing* `SKILL.md` script call site by prose cross-reference (zero new invocation lines, zero new divergence pairs, zero seed extension — asserted by re-running Phase 2's gates unchanged), and emits only the inference proposal. It **bypasses** A2.5's template-absent gate so inference is reachable on templated repos too — the third named behavior change. A2.5's only guard is "do not propose a canonical shape A2 could not classify".
17. **Pinned-executable verification boundary (added 2026-09-21, round 8 grill).** *Decision (grilled):* option (b) — `SKILL.md`'s pinned-executable invariant is **amended** to name "launch of a mirror-parity-checked release `lib/` script with pre-verified executables passed by absolute path" as the verification boundary, so one script launch (not each `jq`/`git` call inside it) is the re-verification point. The script still re-checks each injected path (absolute, exists, executable, not a symlink) once before first use and fails closed with exit `2`. Phase 2 records this in its mapping table, retargets `test_release_step1b_bootstraps_pinned_context_before_its_first_launch` and `test_release_jq_pin_is_conditional_on_template_presence` to the amended boundary, and documents the env-var contract in `AGENTS.md`'s path-resolution idiom section.
18. **"Templated", defined (added 2026-09-21, round 8 grill).** *Decision (grilled):* "templated" means **Step 1b item 2's outcome** — `.release-template.json` present in the worktree **or** present at `TEMPLATE_HEAD_COMMIT` (`git ls-tree`) — not bare filesystem presence. The `ls-tree`/`ls-files`/`git diff --quiet`/mode outcomes are **injected argv inputs** of `read-release-template.sh`, never things the script probes. Phase 1.5's fixtures enumerate four presence states: (i) absent from worktree *and* `TEMPLATE_HEAD_COMMIT`; (ii) absent from worktree, present at `TEMPLATE_HEAD_COMMIT`; (iii) present untracked; (iv) present tracked but dirty vs HEAD. **`RELEASE_JQ` may be unset only in state (i)** — the untemplated case; every other state pins jq, and an unset `RELEASE_JQ` there is an environment failure.
19. **Fixture byte contract (added 2026-09-21, round 8 grill).** *Decision (grilled):* a release-body fixture is the **JSON-decoded UTF-8 bytes** exactly as `gh release view --json body | jq -r .body` emits, committed with `-text` in `tests/release/fixtures/.gitattributes` so no `autocrlf`/`text` normalization applies on any platform. LF and CRLF marker-bearing variants are **separate fixture cases**, each with its own golden, and each golden is what the prose path yields on that exact byte sequence (trailing newline included).
20. **Exit-code vocabulary (added 2026-09-21, round 8 grill).** *Decision (grilled):* every extracted script uses `0` = ok, `1` = validation failure (an input violated a gate), `2` = environment failure (injected executable missing/relative/non-executable, unreadable input). Fixing the vocabulary here is what lets Phase 1.5's goldens derive `exit_code` from a pre-defined prose-outcome → code mapping rather than from an execution that does not exist yet, and gives both mirrors and every negative-case test one shared set.
21. **Region-length rows group by `region` only (added 2026-09-21, round 8 grill).** *Decision (grilled):* Phase 4 reads the **last appended row per `region`**, regardless of the `file` column, so Phase 3's relocation into `references/` supersedes the earlier `(region, SKILL.md)` row without any exempt row. Decision 11's `(region,file)` grouping is superseded by this; `exempt` stays reserved for deliberately-emptied regions.
22. **`references/template-subsystem.md` is referenced exactly twice (added 2026-09-21, round 8 grill).** *Decision (grilled):* the file has two triggers, so it carries two references: one textually inside the Step-1b templated-branch region anchor, and one inside a new **A2 marker-bearing sub-region** anchor (added in Phase 1.5, since A2's dual-anchor marker resolution lives under `## Audit Mode`, not Step 1b). Phase 3 asserts exactly two, one per region. `references/audit-inference.md` keeps its single Audit-Mode-region reference.
23. **All Codex-mirror work is consolidated into one `codex:rescue`-driven Phase 3.5 (added 2026-09-21, user restructure request).** *Decision:* Phases 1.5, 2 and 3 touch the **Claude mirror and harness-neutral repo files only** (`plugins/skein/skills/release/**`, `tests/**`, `scripts/**`, `justfile`, `.pre-commit-config.yaml`, `.github/workflows/ci.yml`, `AGENTS.md`, `README.md`, docs). Every edit under `plugins/skein-codex/**` — Codex `SKILL.md` anchors, prose moves and call-site path anchors, Codex `lib/*.sh`, Codex `references/*.md` — moves into a single **Phase 3.5**, placed after Phase 3 and before Phase 4 so Phase 4's coverage and region-length checks see both mirrors in their final state. The **parity machinery itself stays in the earlier phases**, because every file that implements it (`scripts/check-prompt-parity.sh`, `tests/parity/test-applier-bundle-parity.sh`, `tests/parity/test-prompt-parity-extended.sh`, `justfile`, `.pre-commit-config.yaml`) is harness-neutral: Phase 2 defines both sides of each new divergence pair and the full `RELEASE_LIB_PARITY_FILES` mechanism, Phase 3 defines both `references/` byte-parity arms, and those definitions are the machine-readable specification Phase 3.5's Codex-mirror bytes must satisfy. What Phase 3.5 supplies is the Codex side's *content*, not its checks. Lint globs and pre-commit `files:` patterns list the Claude mirror only (grilled decision 15), so they need no Phase 3.5 change.

    **Reconciliation with `AGENTS.md`'s Codex-first rule.** The repo convention (`AGENTS.md`; `.claude/CLAUDE.md`; memory "Codex-mirror edits go through codex:rescue") is: a Codex-mirror edit is authored by `codex:rescue`, Codex half first, Claude half aligned in the **same commit**. Two properties of that rule are load-bearing and both are preserved here: (i) **Codex-mirror bytes are never authored by Claude** — Phase 3.5's worker is `codex:rescue` for every Codex file; (ii) **no mirror pair is left divergent at merge** — Phase 3.5 restores full parity before Phase 4 and before the branch merges. What changes is only the *within-commit ordering*: because Phase 3.5 runs after the Claude mirror is finished, the Claude mirror is the **reference** and Phase 3.5 mirrors it byte-for-byte **modulo the two sanctioned path anchors** (`${CLAUDE_PLUGIN_ROOT}` on Claude, `"$SKILL_DIR"` on Codex — never collapsed, never cross-written) and the pre-existing documented frontmatter/invocation-mode/execution-model divergences. This inversion is a **deliberate, disclosed departure** from the same-commit pairing, adopted because the alternative (a `codex:rescue` round-trip inside every one of Phase 1.5/2/3's many mirror-atomic commits) is what the user's restructure request removes; it is not a licence for Claude to write Codex-mirror files. Mirror-atomicity within Phase 3.5 itself still holds: each Phase 3.5 commit lands a Codex half together with the Codex-side parity-machinery change that edit needs.

    **Viability / fallback.** Phase 3.5 runs via `codex:rescue` when the Codex CLI is available. When it is not (the `codex-rescue` node-missing fallback documented in `AGENTS.md` and in this repo's memory), the phase falls back to a `conduct` worker that applies the hunks itself; that worker MUST then (a) byte-verify each Codex file against its Claude counterpart under the anchor-substitution rules (the same substitution `normalize_release_workflow()` performs), and (b) **disclose** in this phase's Findings entry that the Codex half was applied without Codex CLI review, naming which files. The disclosure is an acceptance criterion of the phase, not an optional note.

    **Parity during the lagging window.** The existing `CONDUCT_LAGGING_MIRROR_OK` fail-open does **not** cover any release plane — it is scoped to the per-skill `<skill>/<prompt-file>` loop (`scripts/check-prompt-parity.sh:120-143,235-260`), while release's mirror comparison lives in the separate "release SKILL.md workflow parity" block (`:265-452`), whose cardinality gates set `PARITY_DIFF=1` unconditionally (`:397-431`). So it is **not** reused. Instead, Phase 1.5's first task adds a release-scoped acknowledgment variable, `RELEASE_LAGGING_MIRROR_OK`, modeled line-for-line on `CONDUCT_LAGGING_MIRROR_OK`'s shape (comma/whitespace-separated plane names; exit zero **only** when every observed release-plane drift is enumerated; a stderr annotation `expected lagging-mirror drift: <plane> (RELEASE_LAGGING_MIRROR_OK)` on every acknowledged plane; any unenumerated drift still fails). It acknowledges exactly three planes and nothing else: `release-skill-md` (the normalized full-file `SKILL.md` workflow diff, the new per-call-site cardinality gates, **and the Codex-mirror parameter of `tests/parity/test_release_skill_contract.py`'s module-level `RELEASE_SKILLS` parametrization at `:239`, which would otherwise fail on every Claude-only anchor and prose move**), `release-lib` (`RELEASE_LIB_PARITY_FILES`' byte-identity and enumeration arms), `release-references` (the `references/` byte-parity arms). The pytest side is implemented as a **loud per-parameter `pytest.skip` naming the plane and the variable**, never a silent filter, and the tests are still *collected*, so Phase 4's `--collect-only` test-id superset check is unaffected. **These three checks are the ones deferred to Phase 3.5**; Phases 1.5–3 validate with them excluded (their Validation cmd sets the variable), and Phase 3.5's and Phase 4's Validation cmd is plain `just ci` with the variable **unset**, so full parity enforcement is what closes the window. Everything else in `just ci` — `check-sync`, lint, the pytest prose-contract suite, the `tests/release/` bash suite, `test-prompt-parity-extended.sh`'s synthetic seeded pairs (which seed both halves themselves and are therefore unaffected by the real Codex mirror lagging) — stays fully enforced in every phase.

## Related Work

- `docs/dev_plans/20260914-feature-release-repo-template.md` — the plan that introduced the template subsystem this refactor restructures. Its `/code-review xhigh --fix` run (2026-09-17) findings #1–#12, #14–#15 (finding #13 already fixed) are the concrete symptom list motivating this plan; see its Findings/changelog section for the full list. This plan does not re-litigate those findings' resolutions — it changes where the logic they point at lives, and Phases 2–3 below should close several of them as a side effect (tracked per-phase, not claimed as a batch fix). Findings #9 and #11 are explicitly excluded from that side-effect closure per the grilled decision above.
- `AGENTS.md` — mirror-editing convention is documented across its "### Path-resolution idiom (harness-divergent)" and "### review-gauntlet `lib/` (authored, mirror-parity-enforced)" subsections (there is no single top-level `## Mirrors` heading in `AGENTS.md` itself; the project's `.claude/CLAUDE.md` does carry one). Both govern the Codex-mirror-first edit order this plan follows throughout. `AGENTS.md` states no scoped rule limiting `codex:rescue` to "prose and path-anchor edits only" — that phrasing (removed from Requirements below, round 2 fix) overstated what the doc says; the actual, general rule is simply Codex-mirror-first-then-Claude-aligned for every edit to the Codex mirror, scripts included. **Per grilled decision 23 this plan inverts that ordering, deliberately and disclosed**: all Codex-mirror work is consolidated into Phase 3.5, authored by `codex:rescue` against the already-finished Claude mirror as the byte-for-byte reference (modulo the `${CLAUDE_PLUGIN_ROOT}` / `"$SKILL_DIR"` anchors, which are never collapsed). The rule's two load-bearing properties — Codex bytes authored by Codex, and no mirror left divergent at merge — are preserved; see decision 23 for the full reconciliation, the lagging-window parity handling, and the no-Codex-CLI fallback.
- `plugins/*/skills/review-gauntlet/lib/`, `tests/parity/test-applier-bundle-parity.sh` (`GAUNTLET_LIB_PARITY_FILES`) — the hand-authored-per-mirror precedent this plan's revised Phase 2 follows for `release`'s new scripts (redecided grilled decision 1, round 2). `scripts/lib/bundle-map.sh`, `scripts/bundle-appliers.sh`, `scripts/check-sync.sh` (the auto-fix-applier bundle machinery) are explicitly **not** touched by this plan — round 2 review established that machinery is scoped to skills with an auto-fix mode, which `release` does not have.

## Requirements

- `/release <version>` and `/release audit` behavior is unchanged for every untemplated repo (including skein's own repo) before and after this refactor, and the templated branch's documented behavior is preserved — either verbatim or via a named, reviewed equivalent when an assertion's *source of text* necessarily moves (SKILL.md prose → `scripts/` behavior or `references/` content). This is a structural move, not a behavior change, with three explicit, deliberate exceptions: (a) `/release audit --infer-template` is a new additional entry point (A2.5 stays default-on inside plain `/release audit` — grilled decision 2); (b) that entry point bypasses A2.5's template-absent gate, so inference also runs on templated repos (grilled decision 16); (c) the parity suite's assertion *targets* relocate alongside the prose they check (Phase 2/3 each ship a mapping table, see below) — the suite's *coverage* must not narrow, proven by a superset-of-test-ids check scoped to the pytest prose-contract file (Phase 4; see the round-2 fix on that check's scope below), not byte-for-byte prose equality.
- Each phase that moves an assertion's source of text (Phase 2, Phase 3) ships a **mapping table** as part of its own deliverable: old assertion (test name + line) → new assertion (script behavior test, or `references/`-content test). No assertion is deleted without a named replacement in the same commit. **Retarget**, for this table's purposes, means: the pytest test function's *name* and *test id* stay in `test_release_skill_contract.py` and keep asserting observable `/release` behavior (CLI output, file state), but its body's mechanism for producing/checking that behavior may call into the extracted script instead of re-deriving the check from `SKILL.md` prose text. A row is satisfied only when the new assertion still fails if the underlying behavior regresses. Per **round-3 fix** (this "still fails if behavior regresses" clause was flagged as unobservable prose, allowing a passthrough like `assert SCRIPT.exists()` to satisfy it): each mapping-table row MUST additionally record **evidence of the retargeted test actually failing** under a deliberate one-line mutation of the extracted script (e.g. flip a jq gate's comparison operator, capture the failing pytest output, then revert) — this evidence is committed alongside the mapping table itself, not merely asserted in prose.
- **Pytest-id baseline (round 3 fix — was ambiguous where/how this lives).** The pre-Phase-1.5 test-id set (captured in Phase 1, compared in Phase 4) is committed as a **data file**, not embedded in this plan's prose `## Findings` section: `tests/parity/.release-test-id-baseline.txt` (sorted, one pytest test-id per line, regenerated by a named `just` recipe, e.g. `just release-baseline-refresh`), with the plan's Findings section recording only the commit SHA it was captured at and a pointer to the file — never the list itself. Phase 1's Findings entry also records `git rev-parse HEAD` at capture time; Phase 4's superset check additionally asserts that commit — read from a machine-readable first-line `# captured-at: <sha>` comment in the baseline file itself, not this plan's prose (round 6 fix, grilled decision 11) — is a strict ancestor of Phase 1.5's first commit, so a `conduct --resume` or an out-of-order re-run of Phase 1 cannot silently rebaseline onto the already-mutated suite.
- **Fixture-repo diff, redefined (round 3 fix; before-state and repo-definition completed round 5).** The original "diff Step 4's composed confirmation output byte-for-byte before/after" task is not executable as specified: `/release` is a model-driven prose skill, and Step 4's confirmation text is LLM-generated and non-deterministic run to run — a byte-identical comparison can never reliably pass or fail for the reason intended. The fixture-diff task instead diffs the **extracted scripts' stdout and exit codes** (deterministic bash, not model output) against Phase 1.5's committed golden fixtures — **not** a live "before this phase's changes" capture (round 5 fix: Phase 2 is the phase that *first creates* the extracted scripts, so no live before-state can exist at that point; round 4's grilled decision 7 already established the golden value as the before-state, but round 3's original wording of the fixture-diff task itself was never updated to match, and round 4's own scope only captured a golden for the single A2 case — round 5 extends this to one golden per extracted script, per fixture-repo case, so every script has a real before-state to diff against). **The representative templated and untemplated fixture cases (static input files under `tests/release/fixtures/`, grilled decision 9) are named as a Phase 1.5 deliverable** (round 5 fix — no phase previously defined, created, or listed these repos, despite Phase 2/3 and Review Focus all referencing "a representative templated + untemplated fixture repo" as if it already existed). Verifying the *prose* composition around that output stays the pytest contract suite's job (via the mapping-table retargeting above), not the fixture-diff task's. **Phase 2 names the one concrete command** (the `tests/release/` bash suite, run via `just ci`); **Phase 3 re-runs that same command unchanged**; **Phase 1.5 supplies the goldens manually** and names no command of its own (see their task lists below).
- **`references/` files carry no path-anchor lines (added round 5 — architecture and Contradiction Pass findings).** Every new script-invocation line this refactor adds — the ones carrying `${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR` real anchors (grilled decision 6) — stays in `SKILL.md` prose, never in `references/template-subsystem.md` or `references/audit-inference.md` (grilled decision 8). Phase 3 moves only anchor-free descriptive/schema/inference prose into `references/`; this is what keeps the `references/` byte-parity arm (modeled on the existing `content-review/references` plain `diff -r` byte-identity block) satisfiable, since a `references/` file carrying a divergent per-mirror anchor line would fail that byte-identity check by construction, and `normalize_release_workflow()` is invoked only against the two `SKILL.md` paths — it does not, and is not being extended to, cover `references/` content.
- Deterministic template-read/validate/jq-gate logic currently re-specified in prose at the ~5 call sites identified in the xhigh review is extracted into scripts following the shape of `plugins/skein/skills/review-gauntlet/lib/` (a hand-authored, mirror-parity-enforced `lib/` — not `plugins/skein/skills/deep-review/scripts/lib/persist-common.sh`, which was this plan's earlier, incorrect citation for the pattern; the correct precedent files are enumerated in Phase 2 below). Per **redecided grilled decision 1** (round 2, above), these are authored separately in `plugins/skein/skills/release/lib/` and `plugins/skein-codex/skills/release/lib/`, byte-parity-enforced via a new `RELEASE_LIB_PARITY_FILES` array in `tests/parity/test-applier-bundle-parity.sh`. Per grilled decision 23, Phase 2 authors the **Claude** half and the Claude-side parity arm; `codex:rescue` authors the Codex `lib/` half and the Codex-side parity arm in **Phase 3.5**, mirroring the Claude scripts byte-for-byte (these scripts are anchor-free by grilled decision 9, so no substitution applies to them).
- **The template subsystem splits into two `references/` files, each with its own trigger (round 5 fix — grilled decision 8, superseding round 2/3/4's single-file assumption).** `references/template-subsystem.md` (schema definition, jq validation gate sequence *documentation* — the scripts now carry the logic — and Audit Mode's dual-anchor marker resolution) loads only on the templated branch (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release). `references/audit-inference.md` (A2.5's inference prose) loads whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of whether a template currently exists — A2.5 must be reachable on untemplated repos, since proposing a template against a repo that deliberately removed one is its entire purpose. Both follow the existing `content-review/references/` precedent, which round 3 established actually ships **two** files with a branching load rule (not one), so this two-file split matches that precedent's real shape rather than round 2/3/4's continued one-file model. There is no harness-enforced lazy-load mechanism for either file, so this is a model-discretion contract, not a hard guarantee: the untemplated path is verified to *reference* `references/template-subsystem.md` exactly twice — once inside the Step-1b templated-branch region and once inside the A2 marker-bearing sub-region (grilled decision 22) — and nowhere outside those two gated regions (see Review Focus below) — this is the load-bearing claim, not an absolute "must not pay context cost" guarantee, which no phase could actually gate on (round 2 fix). `references/audit-inference.md` has its own, separate exactly-once-referenced-on-audit-mode assertion (Phase 3).
- Audit Mode's A2.5 template-inference is split from A2's read-only drift classification into its own subcommand/step (`/release audit --infer-template`) so A2 never depends on A2.5's proposal state, closing the circular-coupling risk (A2.5 proposing a canonical template against a repo that deliberately removed one). A2 remains a pure read-only classifier whose emission is **the A2 script's JSON stdout** (grilled decision 12); the human-readable text `SKILL.md` renders from that JSON is unchanged in substance but is asserted nowhere (model discretion). A2.5 parses the JSON fields, not the rendered text. `references/audit-inference.md`'s A2.5 field list is split into two labelled subsections — **script-stdout fields** and **fetch-side fields** — and only the script-stdout subsection is covered by the Phase 3 field-contract assertion. Per round-2 fix as amended by decision 12 (round 7), this interface is pinned precisely: after Phase 2 extracts A2's matching logic into a script, that script's JSON stdout **is** "A2's existing output" for A2.5's purposes (`SKILL.md` renders the human-readable text from it) — the script's stdout contract (defined in Phase 2's mapping table) is the sole source of A2's *classification* fields A2.5 parses (fetch results are script inputs, not outputs), so there is exactly one artifact named "A2's output" from Phase 2 onward, not two. A2.5 stays default-on inside plain `/release audit` (grilled decision 2); `--infer-template` is an additional standalone entry point for re-running inference alone, not a replacement for the default behavior.
- `tests/parity/test_release_skill_contract.py`'s region-boundary helpers are re-anchored on stable, machine-readable markers (e.g. `<!-- skein:step N -->`-style HTML comments, invisible in rendered prose) instead of literal `### Step N: <title>` heading text **and** the other literal body-text anchors the suite currently uses (e.g. `"1. Read \`CHANGELOG.md\`"`, `"3. **Releases (list only)**"` — not all fragile anchors are headings). Round 2 found the actual scope is far larger than first estimated: the suite has ~142 `.index(`/`.find(` literal-text anchor call sites spread across ~60 `test_*` function bodies (round 3 clarification: the suite has 119 `test_*` functions total; 60 of them contain at least one literal-text anchor — "~60" was always the anchor-bearing subset, not a miscount of the 119 total, contra one round-3 lens's flag), not ~15 region-helper functions — Phase 1.5's scope is corrected below to match. `README.md` and `docs/dev_plans/20260712-feature-release-skill.md` also carry literal anchors the suite reads and are added to Phase 1.5's scope (round 2 fix). **Round 3 correction**: `README.md`'s anchor is not a `.index()`-style literal-text lookup at all — `test_release_skill_contract.py:316-329` selects the row via a `startswith("| release |")` table-row prefix match, then asserts exact cell equality; the HTML-comment-marker treatment above doesn't apply to a markdown table row. `README.md`'s anchor is instead stabilized by giving the `release` row a stable leading cell value the prefix match already depends on (documented as unchanged, not re-anchored) and adding it to the same rename-regression test's file list so a future column edit still fails loudly rather than silently. `docs/dev_plans/20260712-feature-release-skill.md`'s anchor is prose (compatible with the HTML-comment treatment) so it gets the standard marker treatment. This combined work is what closes the opaque-`ValueError` failure mode Phase 1.5 exists to prevent, ahead of Phase 3's `README.md` edit. Both mirrors end up carrying the same markers — the Claude mirror's in Phase 1.5, the Codex mirror's in Phase 3.5 (grilled decision 23). Per grilled decision 5, this markers-first work lands in **Phase 1.5**, before Phase 2 moves any prose — not in Phase 4, which now covers only the rename-resilience regression test and the coverage-narrowing check.
- **Mirror-atomicity, not phase-atomicity (round 4 fix — this line previously conflated the two; scope narrowed by grilled decision 23).** `AGENTS.md` requires only that a Codex-mirror edit and its Claude-mirror alignment land in the same commit (mirror-atomicity). Under decision 23 the two halves no longer share a commit: Phases 1.5–3 land the Claude half (plus the harness-neutral parity machinery, lint globs, pre-commit patterns and retargeted assertions that half needs) and **touch no file under `plugins/skein-codex/`**; Phase 3.5 lands every Codex half via `codex:rescue`, each Codex-file commit carrying the Codex-side parity-machinery change that edit needs. The invariant this plan holds instead is: **no mirror pair is divergent once Phase 3.5 completes**, enforced by Phase 3.5's plain-`just ci` Validation cmd with `RELEASE_LAGGING_MIRROR_OK` unset. It does **not** require that each `### Phase N:` section be exactly one commit (phase-atomicity) — a phase, especially Phase 1.5 given its now much larger scope (round 2/3/4 corrections), may land as multiple mirror-atomic commits. `scripts/check-prompt-parity.sh` must keep passing at the end of every commit (via `just ci`, see below), including against the new `lib/` and `references/` files. Per **round-4 fix** (correcting round 3's own miscount, superseding round 2's sentinel-token approach, redecided grilled decision 6 above): `normalize_release_workflow()`'s header forbids prefix-match/free-form-substitution rules ("deliberately forbidden here"), but **does** permit exact-full-line divergence pairs. Its actual machinery is **seven** cardinality gates (`check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1`, one `-ne 0`) guarding **four** contracts, of which only **two** (invocation-mode, execution-model) are the symmetric exact-full-line placeholder-pair shape — not "six pairs" as round 3 said; the other two are one-sided line deletions with no counterpart. Each sanctioned pair spans **four regions** of the file (these four ranges cover the whole divergence machinery, not each pair individually — round 5 fix, per the assumptions lens: the two symmetric pairs specifically occupy narrower sub-ranges within these — `:280-286` of the constants, `:316-319` of the `awk -v` list, `:346-361` of the branches, and `:388-395`+`:412-431` of the count-calls-plus-gates; the wider ranges below are supersets, so an implementer following them lands in the right place): shell constants (`:276-286`), the `awk -v` list (`:313-319`), normalizer branches (`:342-361`), and the `count_release_contract_line` calls plus `-ne 1` gates (`:382-431`). Each mirror's `SKILL.md` prose carries its own real anchor (`${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR`) at every new script-invocation call site — no sentinel token, which round 3 found has no expansion mechanism on either harness — and one new divergence pair per call site is added to `normalize_release_workflow()`, matching the 2-symmetric-pair/4-region template above, each still pinned to a per-mirror count of 1. **Per grilled decision 23 the pair machinery and the two `SKILL.md` anchors land in different phases**: Phase 2 writes the Claude `SKILL.md` anchor and the *complete* pair (both sides) in the harness-neutral `scripts/check-prompt-parity.sh`, which is thereby the machine-readable spec for the Codex line; Phase 3.5's `codex:rescue` pass writes that line into the Codex `SKILL.md`, satisfying the Codex-side gate and re-enabling the normalized full-file diff. Until then the whole `release-skill-md` plane is acknowledged via `RELEASE_LAGGING_MIRROR_OK` (not compared for equality between mirrors — a same-count-on-both-sides check would pass vacuously if both mirrors dropped the same anchor). This is release's `SKILL.md`'s first path-anchor seam. Round 3 also corrected an overclaim (release is not the repo's *only* byte-compared mirror pair — `rubric.md`, per-skill prompt pairs, `content-review/references`, and `review-gauntlet/lib/` are all byte-compared too), but round 3's narrower replacement claim ("release is the only pair compared via *normalized* full-file diff") is **also false**, per round 4: `fan-out`'s `normalize_fanout_prompt()`/`prompt_files_match()` (`check-prompt-parity.sh:165-190`) is a second normalized-full-file-diff mirror pair, and it uses the exact free-form-substitution class `normalize_release_workflow()`'s header calls "forbidden" — proving that restriction is function-local to `normalize_release_workflow()`, not repo-wide. The actually load-bearing point is narrower: *within `normalize_release_workflow()` specifically*, only exact-full-line pairs are sanctioned, and this plan's new anchors must follow that function-local rule. Phase 3 extends `check-prompt-parity.sh` with a `references/` byte-parity arm modeled on the existing `content-review/references` block (the arm is written in Phase 3 but stays inside the `release-references` acknowledged plane until Phase 3.5 creates the Codex-mirror `references/` files it compares against) — noting (round 3 fix) that block's actual precedent ships **two** references files with a branching load rule and a second skill reading them cross-skill, not the one-file/one-trigger model this plan assumes; Phase 3's three reference-count assertions (`template-subsystem.md` exactly twice, one per trigger region — grilled decision 22; `audit-inference.md` exactly once) are scoped to `release`'s own two-file case; the `content-review` precedent references each file twice (SKILL.md body plus a listing), so this count set is a deliberate, disclosed departure, not a copy of the precedent (round 6/8 fix).
- New scripts (`plugins/skein/skills/release/lib/*.sh` and the Codex-mirror equivalent, hand-authored per redecided grilled decision 1) are added to the `justfile`'s `lint-scripts` recipe globs and `.pre-commit-config.yaml`'s matching `files:` patterns in the same commit that introduces them — **Claude mirror only**, matching the `review-gauntlet/lib/` precedent (`justfile:115`, `.pre-commit-config.yaml:15` list only the Claude copy), with `RELEASE_LIB_PARITY_FILES` byte-parity covering the Codex copy (grilled decision 15). Script-level unit test coverage is authored as **bash test suites under `tests/<suite>/`, each with its own `justfile` recipe** — matching this repo's actual convention for canonical-script unit tests (`tests/reconciliation/test-reconciler-unit.sh`, `tests/lenses/test-persist-lens-result.sh`, `tests/gauntlet/test-finding-key.sh`) — **not** folded into `test_release_skill_contract.py` or counted toward its pytest test-id set (round 2 fix: this repo has no generic pytest-based script-unit-test convention, and folding them in would make Phase 4's coverage-narrowing check either non-comparable or silently scoped to the prose file only; see Phase 4). **Round 3 fix**: `justfile:164`'s `ci:` recipe is a hand-maintained dependency list (`ci: lint check-sync … pytest-tests`), not a glob — `lint-scripts`' globs lint the new suite but never *run* it. The new suite's `justfile` recipe MUST be appended to `ci:`'s dependency list in the same commit that introduces the suite (Phase 2), not merely created and left for Phase 4 to notice is missing. Additionally, the `RELEASE_LIB_PARITY_FILES` array added to `tests/parity/test-applier-bundle-parity.sh` (per redecided grilled decision 1 above) is modeled on the **full** `GAUNTLET_LIB_PARITY_FILES` precedent, not just its byte-identity loop: the precedent's enumeration half (`tests/parity/test-applier-bundle-parity.sh:151-210`) walks the union of both mirrors' `lib/` directories and fails on any basename not registered in the array — this is what stops a future script from silently escaping mirror-parity checking forever. Phase 2 reproduces both halves, not just the byte-identity loop this plan's earlier drafts cited alone. Per grilled decision 23 the array and both halves are **authored in Phase 2** but their Codex-mirror side only becomes satisfiable in **Phase 3.5**, when the Codex `lib/` files exist; until then the `release-lib` plane is acknowledged via `RELEASE_LAGGING_MIRROR_OK`, and Phase 3.5 is the phase that proves the arm green with the variable unset.
- **`-ne 1` cardinality pins require call-site-qualified distinct line text (round 5 fix — round 4's disposition claimed this was fixed but no phase task actually addressed it).** `count_release_contract_line` counts whitespace-trimmed exact full-line matches file-wide; a `-ne 1`/`-ne 0` gate fails the moment two call sites in the same `SKILL.md` emit the identical anchor line. Since this plan's premise (Context) is roughly 5 call sites collapsing onto shared scripts, at least two call sites would naturally emit the same invocation line unless each is given distinct, call-site-qualified text (e.g. embedding the script's purpose or a short discriminator in the invocation line itself, not just the bare `${CLAUDE_PLUGIN_ROOT}/skills/release/lib/<name>.sh` (Codex: `"$SKILL_DIR"/lib/<name>.sh`) form). Phase 1's inventory records each call site's exact, distinct line text; Phase 2's divergence-pair task uses that exact text, never a templated/interpolated line that could collide across sites.
- **Fail-open acknowledgment (round 5 addition).** Each new cardinality gate this refactor adds widens an existing fail-open property of `normalize_release_workflow()`: per `check-prompt-parity.sh:450-452`, any single tripped gate skips the entire normalized full-file mirror diff, not just the failing contract's check. This is accepted as existing, disclosed behavior inherited from the function as it stands today (not something this plan can fix without redesigning the function, which is out of scope) — noted here so a reviewer doesn't mistake the new gates as introducing this property, rather than merely adding two gates per new call-site pair (roughly ten in total per Phase 1's inventory) as further triggers for a pre-existing one.
- **Script input transport and the pinned-executable boundary (grilled decisions 17/9/18).** A script *launch* is the verification boundary for pinned executables; inside a script, `"$RELEASE_JQ"`/`"$RELEASE_GIT"` are re-checked (absolute, exists, executable, not a symlink) once before first use and reused thereafter, exiting `2` otherwise. Per-input transport is pinned: SHAs and enum flags (including Step 1b's `ls-tree`/`ls-files`/`git diff --quiet`/mode outcomes) arrive as **argv**; release bodies and release-list JSON arrive as **file paths the `SKILL.md` harness writes under the pinned `mktemp -d`** — never argv, never a heredoc; raw template bytes are **never injected** — the marker-resolution script runs `"$RELEASE_GIT" cat-file -t/-p <sha>` itself against a passed git dir, preserving `SKILL.md`'s content-addressed read. The `SKILL.md` prose that creates those harness files also owns deleting them.
- **Exit codes (grilled decision 20).** `0` ok, `1` validation failure, `2` environment failure — one vocabulary shared by both mirrors, every golden's `exit_code` field, and every negative-case test.
- **Harness and anchor assumptions, named rather than assumed.** `$SKILL_DIR` being exported by Codex and `${CLAUDE_PLUGIN_ROOT}` being template-substituted by Claude Code are **inherited harness assumptions**, grounded only in precedent (`plugins/skein-codex/skills/fan-out/SKILL.md:70-71`, which itself guards the export). Every new call site in both mirrors therefore carries the precedent's existence guard (`[ -x "${SKILL_DIR:?}/lib/<name>.sh" ] || { echo "<remedy>"; exit 1; }`, and the `${CLAUDE_PLUGIN_ROOT}` equivalent) so a mis-expanded anchor fails loudly with a remedy instead of being read by the model as "script absent, fall back to prose". Likewise the anchors are **not invisible to the model**: both harnesses feed `SKILL.md` as raw text, so anchor comments are loaded context and count toward the Objective's per-invocation byte cost. They are inert because they carry no instruction, their grammar (`<!-- skein:step-N -->`) is deliberately outside the `<!-- release-template-sha: ... -->` marker family A2's regexes match, and their bytes are included in Phase 1.5's region-length baseline.
- **Lagging-mirror acknowledgment in the Validation cmd (grilled decision 23).** Phases 1.5, 2 and 3 run `RELEASE_LAGGING_MIRROR_OK='release-skill-md release-lib release-references' just ci`; Phase 1 (no mirror edits), Phase 3.5 and Phase 4 run plain `just ci` with the variable unset. Setting the variable defers **only** those three release mirror-parity planes and annotates each on stderr; every other `just ci` gate stays enforced in every phase, and any drift outside the three enumerated planes still fails. Phase 1.5 creates the variable as its first task (see below) — no phase may use it before it exists.
- `just ci` (backgrounded per this repo's testing convention) is each phase's actual Validation cmd — not the narrower `pytest tests/parity/test_release_skill_contract.py` + `check-prompt-parity.sh` pairing alone, which is a strict subset and would let a `check-sync`/lint/other-pytest-file regression go unnoticed. The targeted pytest run stays as each phase's fast inner-loop Test command.

## Review Focus

- **No behavior drift**: the refactor must not change what a templated or untemplated `/release` run does (except the two named exceptions above), only where the logic lives and how much of it loads per invocation. Phase 2 and Phase 3 each include a task that diffs the extracted scripts' stdout/exit codes (not the LLM-composed prose around them — round 3 fix, see Requirements above) against Phase 1.5's goldens on the templated + untemplated fixture cases, with a named command in each phase's task list.
- **Progressive disclosure boundary correctness**: verify the untemplated path genuinely never reads `references/template-subsystem.md`, and that `references/audit-inference.md` is read only when Audit Mode actually runs (round 5 fix — two files, two triggers, per grilled decision 8). Since no harness-enforced lazy-load mechanism exists (only the `content-review` prose-instruction precedent — see Requirements above), this is verified via three static assertions (`template-subsystem.md` referenced exactly twice — one reference per trigger region, grilled decision 22; `audit-inference.md` exactly once inside the Audit Mode region — each textually inside its own trigger region of `SKILL.md`) plus one manual transcript check per file, run and recorded per the protocol Phase 3 names, not an automated harness-level guarantee.
- **Mirror parity**: because Phases 1.5–3 leave the Codex mirror untouched by design (grilled decision 23), the three release parity planes are acknowledged, not silently skipped, in those phases; **Phase 3.5 is the gate that proves them** — it must end with plain `just ci` green and `RELEASE_LAGGING_MIRROR_OK` unset, and a reviewer should check that no acknowledgment leaked past it (Phase 3.5 and Phase 4 both declare plain `just ci`). `scripts/check-prompt-parity.sh` and `test_release_skill_contract.py` both continue to assert what they assert today or a named, reviewed equivalent (per the mapping-table requirement above); confirm the new test-anchor mechanism (Phase 1.5) and the retargeted assertions (Phase 2/3) don't silently narrow what's covered while making the suite pass — Phase 4's coverage-narrowing check (superset-of-test-ids over `test_release_skill_contract.py` only, per the round-2 fix above; the new bash script-unit suites are validated separately by their own `just` recipes, not folded into this comparison) is the actual gate for this. This requires a captured pre-Phase-1.5 baseline test-id set (added to Phase 1, round 2 fix) since Phase 4 cannot compare against a baseline nothing recorded.
- **A2/A2.5 split correctness**: after the split, confirm A2's classification for a marker-absent, no-current-template repo doesn't change (this is exactly finding #9/#11's territory from the xhigh review, and per the grilled decision above, any fix to that territory is explicitly out of scope here — this Review Focus item is about *not regressing* it, not fixing it). Phase 1.5 captures the golden pre-split classification value for this exact case (round 3/4/5 fix — moved here from Phase 3, extended to a golden-fixture comparison — see grilled decision 7 and Phase 1.5/2/3 below), so the split's effect on it is measurable against a committed baseline rather than asserted from memory.

## Architecture & Call Flow

This refactor touches six independently-executing components (round 2 adds component 6, the drift-guard plane, previously omitted); their contracts:

1. **Claude/Codex `SKILL.md` mirrors** (`plugins/skein/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/SKILL.md`) — the orchestration layer. Post-refactor, each invokes the extracted scripts (below) for deterministic work and reads `references/template-subsystem.md` only when the templated branch is reached and `references/audit-inference.md` only when Audit Mode runs (round 6 fix — stale single-file wording). Both mirrors currently carry zero `${CLAUDE_PLUGIN_ROOT}`/`$SKILL_DIR` path-anchor lines (verified); the new script-invocation call sites this refactor adds are the *first* per-harness path-addressing seam in this pair, addressed via real per-mirror anchors plus one new exact-full-line divergence pair per call site in `normalize_release_workflow()` (round 3 redecided grilled decision 6 — superseding round 2's sentinel-token idea, which had no expansion mechanism). **Phasing (grilled decision 23):** the Claude mirror is edited in Phases 1.5–3 and becomes the reference; the Codex mirror is brought up to it in Phase 3.5 by `codex:rescue`, byte-for-byte modulo the two path anchors and the pre-existing documented frontmatter/invocation-mode/execution-model divergences.
2. **Hand-authored per-mirror scripts** (`plugins/skein/skills/release/lib/*.sh`, `plugins/skein-codex/skills/release/lib/*.sh` — redecided grilled decision 1, round 2) — pure over injected local inputs (files/argv/stdin — grilled decision 9) with exit code and stdout out; the pinned `jq`/`git` executables arrive via documented env vars (`RELEASE_JQ`, `RELEASE_GIT`), fail-closed — **`gh` is never used by a script** (grilled decision 9): `gh` fetches stay in `SKILL.md` prose and their results are injected as script inputs; the script *launch* is the pinned-executable verification boundary (grilled decision 17); authored separately per mirror (Claude half in Phase 2, Codex half by `codex:rescue` in Phase 3.5 — grilled decision 23, superseding the Codex-first-same-commit ordering) and byte-parity-checked via `RELEASE_LIB_PARITY_FILES` in `tests/parity/test-applier-bundle-parity.sh` (component 6). Each script's contract (arguments, exit codes, stdout shape) is documented in Phase 2 as part of the mapping table, so `SKILL.md` prose has a concrete interface to call rather than re-deriving the logic.
3. **`references/template-subsystem.md` and `references/audit-inference.md`** (round 5 fix — two files, not one; grilled decision 8) — `template-subsystem.md` (schema, jq-gate documentation, dual-anchor marker resolution) is read by `SKILL.md` only on the templated branch (repo has `.release-template.json`, or Audit Mode reaches a marker-bearing release). `audit-inference.md` (A2.5's inference prose) is read whenever Audit Mode runs `--infer-template` or default `/release audit`, independent of template presence — so `--infer-template` on an untemplated repo still has a reference file to load. Both trigger conditions and both readers are named explicitly in Phase 3; both are model-discretion reads (see Requirements above), not harness-enforced gates. Neither file carries a path-anchor line (Requirements above) — every script-invocation call site with a real per-mirror anchor stays in `SKILL.md`. The Claude-mirror pair of files is created in Phase 3; the Codex-mirror pair is created by `codex:rescue` in Phase 3.5 as byte-identical copies (no anchors to substitute), which is what makes the plain `diff -r` arm satisfiable.
4. **A2 → A2.5 handoff** — after Phase 2 extracts A2's matching logic into a script, that script's JSON stdout (documented in Phase 2's mapping table; decision 12) is "A2's existing output" for A2.5's purposes — the single pinned medium for A2's classification fields (round 2 fix; see Requirements above). No new persistence artifact.
5. **Test planes, kept separate** (round 2 fix) — `tests/parity/test_release_skill_contract.py` continues to assert observable `/release` behavior (pytest, prose-contract), retargeted per the mapping tables but not expanded to cover script internals. New **script-level unit coverage lives in bash suites under `tests/<suite>/`**, each with its own `justfile` recipe, matching this repo's existing convention (`tests/reconciliation/`, `tests/lenses/`, `tests/gauntlet/`) — not folded into the pytest file or its test-id set.
6. **Drift-guard plane** (round 2 addition — previously omitted from this section despite Phase 2 editing files in it) — `tests/parity/test-applier-bundle-parity.sh`'s new `RELEASE_LIB_PARITY_FILES` array is release's mirror-parity check for component 2, modeled on `GAUNTLET_LIB_PARITY_FILES`. This plane carries the three release mirror-parity checks deferred during the lagging-mirror window (`release-skill-md`, `release-lib`, `release-references` — grilled decision 23): they are authored in Phases 2–3, acknowledged via `RELEASE_LAGGING_MIRROR_OK` there, and first enforced unacknowledged in Phase 3.5. `scripts/lib/bundle-map.sh`, `scripts/bundle-appliers.sh`, `scripts/check-sync.sh` (the auto-fix-applier bundle/drift machinery) are explicitly out of scope for `release` — that machinery governs skills with an auto-fix mode, which `release` does not have (redecided grilled decision 1).

## Implementation Checklist

### Phase 1: Explore precedent and inventory the ~10 duplicate-logic call sites

**Impl files:** `docs/dev_plans/20260917-refactor-release-skill-structure.md` (Findings section — the inventory itself is a committed deliverable, not left in agent context only), `tests/parity/.release-test-id-baseline.txt` (new, the captured baseline data file), `justfile` (new `release-baseline-refresh` recipe) — round 5 fix, these two artifacts were created by this phase's own tasks below but omitted from this list
**Test files:** none
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py --collect-only -q`
**Validation cmd:** `just ci`
**Goal:** a concrete, line-numbered inventory of every call site re-specifying template-read/jq-gate/marker-resolution logic in both mirrors, committed to this plan's Findings section so a dropped session or `conduct --resume` doesn't lose it, plus the confirmed harness mechanism for lazy-loaded `references/` files (the `content-review/references/content-guidelines.md` precedent, already cited above — confirm no stronger mechanism exists before assuming this one), plus the pre-Phase-1.5 pytest test-id baseline Phase 4 needs (round 2 fix — Phase 4 previously had no baseline to compare against).

- Grep both mirrors for every prose reference to the jq gate sequence, template schema validation, and marker (`release-template-sha`) resolution; tabulate call site, line range, and which of the xhigh findings (if any) it corresponds to. Commit this table into this plan's `## Findings` section. **For each call site, also record the exact, call-site-distinct invocation line text Phase 2 will write** (round 5 fix — `normalize_release_workflow()`'s `-ne 1`/`-ne 0` cardinality gates fail the moment two call sites emit the identical full line; a bare `${CLAUDE_PLUGIN_ROOT}/skills/release/lib/<name>.sh` (Codex: `"$SKILL_DIR"/lib/<name>.sh`) invocation repeated at multiple call sites would collide, so each site's line must carry a distinct discriminator, e.g. naming the calling step) — this table entry is what Phase 2's divergence-pair task and path-anchor writes use verbatim.
- Confirm the `content-review/references/` precedent is in fact the only conditional/lazy-load mechanism in this codebase (no `scripts/`, `tests/`, or justfile gate observes or enforces it) — record this explicitly so Phase 3's verification approach isn't silently assuming a stronger guarantee than exists.
- Confirm `scripts/check-prompt-parity.sh`'s current scope: it walks `rubric.md`, `*-prompt.md` (maxdepth 1), and per-skill normalizer rules — it does not yet cover `lib/` or arbitrary `references/` subtrees for `release`. Confirm the `content-review/references` block (lines ~563-577) is the copyable precedent for Phase 3's extension. **Round 4 correction** (this bullet still described the superseded round-2 sentinel-token approach verbatim, contradicting redecided grilled decision 6 — four lenses independently caught this stale instruction surviving into round 3's own task list): `normalize_release_workflow()`'s existing divergence machinery is **seven** cardinality gates (`scripts/check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1` and one `-ne 0`) guarding **four** contracts, of which only **two** are the symmetric exact-full-line placeholder-pair shape (invocation-mode, execution-model) Phase 2's new path anchors will follow; the other two are one-sided line deletions with no counterpart and are not the template. Each sanctioned pair spans **four regions** of the file, not one function: the verbatim-line shell constants (`:276-286`), the `awk -v` substitution list (`:313-319`), the normalizer branches (`:342-361`), and the `count_release_contract_line` calls plus their `-ne 1` gates (`:382-431`). Confirm this exact shape before Phase 2 — it replaces round 2's sentinel-token instruction and round 3's "six pairs, one function" miscount, both wrong.
- Confirm the corrected script-distribution precedent (redecided grilled decision 1, round 2): `plugins/*/skills/review-gauntlet/lib/`'s file set and `GAUNTLET_LIB_PARITY_FILES` (`tests/parity/test-applier-bundle-parity.sh:138-147`, the byte-identity loop) **and** its enumeration half (`:151-210`, the union-over-both-mirrors guard against a silently-unregistered future script — round 3 fix, see Requirements above), including the anchor-divergent-exclusion carve-out for `gauntlet-common.sh`, as the exact template for Phase 2's `RELEASE_LIB_PARITY_FILES`. This replaces the original inventory task of confirming `scripts/lib/bundle-map.sh`'s `BUNDLE_SKILLS`/`bundle_applier_for` shape, which is no longer relevant — `release` does not register there.
- Confirm `normalize_release_workflow()`'s exact existing cardinality-contract count and shape (round 4 fix — the plan previously miscounted this as "four", then "five", then round 3's own "six pairs"): **seven** cardinality gates (`check-prompt-parity.sh:397,402,407,412,417,422,427` — six `-ne 1`, one `-ne 0`) guarding **four** contracts, of which only **two** (invocation-mode, execution-model) are symmetric exact-full-line placeholder pairs, each spanning four file regions (constants, `awk -v` list, normalizer branches, count-call-plus-gate). This 2-symmetric-pair/4-region shape — not "six pairs, one function" — is the template Phase 2's new path-anchor divergence pair(s) follow (redecided grilled decision 6).
- Run `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py --collect-only -q` against the pre-Phase-1.5 tree; commit the resulting sorted test-id list, filtered to `::` lines (`--collect-only -q` also prints a blank line and an `N tests collected in Xs` summary that would break the superset check) and prefixed with a `# captured-at: <sha>` header line the check skips (round 6 fix) to `tests/parity/.release-test-id-baseline.txt` (a data file, not this plan's prose — round 3 fix) via a new `just release-baseline-refresh` recipe, and record the capture commit's `git rev-parse HEAD` plus a pointer to the file in this plan's Findings section (round 2/3 fix — Phase 4's superset check needs both a machine-readable baseline and a tree-state pin so a re-run can't silently rebaseline onto the mutated suite).
- Grep `README.md` and `docs/dev_plans/20260712-feature-release-skill.md` for literal-text anchors the suite also locates (round 2 finding); confirm which mechanism applies to each — `README.md`'s is a table-row prefix/cell-equality match, not a `.index()` literal lookup (round 3 finding, see Requirements above) — and add the applicable treatment to Phase 1.5's scope below.
- Confirm the exact count of `test_*` functions (119 total) vs. the subset containing at least one literal-text anchor (60) in `tests/parity/test_release_skill_contract.py`, so Phase 1.5's scope bullet states both numbers unambiguously (119 is exact; the anchor-bearing count depends on the counting rule — a round-6 recount gave 63 functions / 140 literal `.index(`/`.find(` sites — so state the rule used and treat those figures as approximate) (round 3 fix — a round-3 lens misread "~60" as a miscount of the 119 total).

### Phase 1.5: Land stable test anchors before any prose moves

**Impl files:** `plugins/skein/skills/release/SKILL.md` (Claude mirror only — the Codex mirror's anchors move to Phase 3.5 per grilled decision 23), `scripts/check-prompt-parity.sh` (new `RELEASE_LAGGING_MIRROR_OK` acknowledgment), `tests/parity/test-applier-bundle-parity.sh` (same acknowledgment honoured on the `release-lib` plane), `tests/parity/test_release_skill_contract.py`, `README.md`, `docs/dev_plans/20260712-feature-release-skill.md` (round 2 addition — both carry literal anchors the suite also reads; see Phase 1), `tests/release/golden/*.json` (new — one golden fixture per extracted script per fixture case, round 5 fix), `tests/release/fixtures/` (new — static injected-input files per case, grilled decision 9), `tests/release/lib.sh` (new — deterministic git-state builder, only where git state is unavoidable), `justfile` (`release-baseline-check` recipe, registered in `ci:` and `parity-tests`), `tests/parity/.release-baseline-meta.json` (new — boundary SHAs plus `golden_capture_commit`), `tests/parity/.release-region-length-baseline.tsv` (new), `tests/release/golden/schema.json` (new — the frozen golden key set, grilled decision 14), `tests/release/fixtures/.gitattributes` (new — `-text`, grilled decision 19), `.github/workflows/ci.yml` (`fetch-depth: 0`, grilled decision 13), `docs/dev_plans/20260917-refactor-release-skill-structure.md` (Findings section — mapping table, first-commit-SHA record, round 5 fix: these were previously omitted from the Impl-files list despite being this phase's own deliverables)
**Test files:** `tests/parity/test_release_skill_contract.py`
**Test command:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md' uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md release-lib release-references' just ci`
**Goal:** stable, machine-readable anchors exist at every region boundary the suite currently locates via literal heading or body text, in the **Claude mirror** (the Codex mirror's identical anchors land in Phase 3.5 — grilled decision 23), with zero prose movement — trivially green, so Phases 2–3 move prose against already-stable anchors instead of racing to fix what they just broke (grilled decision 5). Round 3 fix: this phase now also ships its own mapping table and in-phase coverage gate (it is the largest test-mutating phase and was previously the only one without either), and performs the **manual golden capture of each script's pre-extraction classification value** (moved here from Phase 3 so it reflects genuinely pre-extraction behavior — there is no automated test on that side of the comparison).

- **First task — create the lagging-mirror acknowledgment (grilled decision 23).** Add `RELEASE_LAGGING_MIRROR_OK` to `scripts/check-prompt-parity.sh`, modeled line-for-line on `CONDUCT_LAGGING_MIRROR_OK`'s existing shape (`:109-143`, `:235-260`): comma/whitespace-separated plane names, acknowledged drift annotated on stderr as `expected lagging-mirror drift: <plane> (RELEASE_LAGGING_MIRROR_OK)`, `PARITY_DIFF` left unset **only** when every observed release-plane drift is enumerated, and any unenumerated drift still failing. Exactly three plane names are recognised — `release-skill-md`, `release-lib`, `release-references` — and an unrecognised name is itself an error, so a typo cannot silently widen the window. Honour the same variable on the `release-lib` plane in `tests/parity/test-applier-bundle-parity.sh`, and on the `release-skill-md` plane in `tests/parity/test_release_skill_contract.py`: when `release-skill-md` is enumerated, the Codex-mirror parameter of the module-level `RELEASE_SKILLS` parametrization (`:239`) raises a **loud `pytest.skip` naming the plane and the variable** (tests are still collected, so the Phase 4 test-id superset check is unaffected); unset, both mirrors run as today. This phase's Test command therefore also runs with the variable set, matching its Validation cmd. Add a self-test per plane (acknowledged drift → exit 0 with the annotation; unacknowledged drift → non-zero; unrecognised plane name → non-zero) so the acknowledgment cannot be vacuous, and a self-test asserting the variable **unset** restores the pre-existing hard-fail behaviour on all three planes. The variable is explicitly temporary: Phase 4's post-merge sunset commit (see below) removes it together with the other scaffolding.
- Add `<!-- skein:step-N -->`-style anchors at each region boundary currently reached via `text.index("### Step N: ...")` or literal body-text lookups (e.g. `"1. Read \`CHANGELOG.md\`"`, `"3. **Releases (list only)**"`, `"2. Resolve and validate"`) — both the heading anchors and the body-text anchors the assumptions lens flagged as not-headings. The anchor grammar must not resemble the `<!-- release-template-sha: ... -->` marker family A2's regexes match (no `<!-- ...-sha:` shape), and the anchor bytes are counted into this phase's region-length baseline, since the model sees them as raw text (Requirements above).
- **Scope correction (round 2):** the suite has ~142 `.index(`/`.find(` literal-text anchor call sites spread across ~60 `test_*` function bodies, not ~15 region-helper functions as originally estimated — only 4 helper functions are `_`-prefixed shared helpers today; the rest are inline literal lookups inside individual test bodies. This phase's actual task is: (a) promote every inline literal-text lookup into a small number of shared, anchor-keyed region helpers (this *creates* the ~15-helper structure the original estimate assumed already existed), then (b) retarget all ~60 call sites onto those helpers. Budget this phase accordingly — it is the largest phase in this plan by line count touched, not a small mechanical pass.
- Inconsistent heading spellings noted in review (some region helpers use truncated vs. full heading text) are normalized as part of this pass.
- Add the HTML-comment marker treatment to the literal-text anchor in `docs/dev_plans/20260712-feature-release-skill.md` (round 2 addition, see Phase 1 — this file's anchor is prose, compatible with the standard treatment). For `README.md` (round 3 correction — its anchor is a `startswith("| release |")` table-row prefix match plus exact-cell equality, not a `.index()` literal lookup): leave the row's leading cell value unchanged (it is the prefix match's own anchor) and add `README.md` to the rename-resilience regression test's file list below, so a future column edit to the `release` row still fails loudly instead of silently passing.
- Add an anchor for the "templated-branch region" of `SKILL.md` that Phase 3's progressive-disclosure static assertion (Review Focus) needs — round 3 fix: no phase previously created this anchor, so that assertion had no region to check "textually inside" against. This phase adds the region boundary anchor; Phase 3 adds the assertion that reads it (one of the two `template-subsystem.md` references, grilled decision 22).
- **Also add the Audit Mode region anchor here** (round 6 fix — Phase 3's `references/audit-inference.md` exactly-once assertion needs an Audit Mode region anchor; adding it in Phase 3, after this phase froze the byte-length baseline and rename test, would split a recorded region and leave the anchor uncovered by the rename test. Anchors are inert, so add it now and record its region in the baseline). **Also add the A2 marker-bearing sub-region anchor** inside `## Audit Mode` (grilled decision 22), which Phase 3's second `references/template-subsystem.md` reference assertion reads — A2's dual-anchor marker resolution lives there, not in Step 1b.
- **Create the representative templated and untemplated fixture inputs** (round 5 fix, redone round 6 — grilled decision 9: round 5 specified committed scratch git repos, which git cannot store as nested `.git` and which cannot carry the GitHub-side inputs A2 actually consumes): commit **static input files** under `tests/release/fixtures/templated/` (a `.release-template.json`, a release-list JSON, at least one marker-bearing release body as a text file, and the peeled-commit SHAs) and `tests/release/fixtures/untemplated/` (skein's own repo shape — no template file, no markers). Where a script genuinely needs git state, `tests/release/lib.sh` builds it with `git init` in a `mktemp -d` using pinned author/committer identity and dates, matching the repo's fixture convention (`tests/lenses/`, `tests/reconciliation/`, `tests/parity/test-anchor-path-with-spaces.sh`). No `tests/**/.git` is ever committed. **Fixture byte contract (grilled decision 19):** release bodies are the JSON-decoded UTF-8 bytes `gh release view --json body | jq -r .body` emits, committed with `-text` in `tests/release/fixtures/.gitattributes`, with LF and CRLF marker-bearing variants as separate cases. **Template-presence states (grilled decision 18):** the fixture set covers all four states — absent from worktree and `TEMPLATE_HEAD_COMMIT`; absent from worktree but present at `TEMPLATE_HEAD_COMMIT`; present untracked; present tracked but dirty — passed to scripts as argv enum flags, with `RELEASE_JQ` unset only in the first. **Per-gate invalid inputs:** commit `tests/release/fixtures/invalid/<gate>.json` for each of the five jq gates (empty/shape/enum/unknown-key/duplicate-key), reusing the raw JSON fixtures already in `test_release_skill_contract.py:1134`'s `_template_fixtures()`. **Builder determinism (round 8 fix — the cited convention pins none of this):** `tests/release/lib.sh` sets `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1`, fixed `GIT_AUTHOR_*`/`GIT_COMMITTER_*` name/email/date, and runs `git init -q --object-format=sha1 -b main -c commit.gpgsign=false`, with a self-test asserting two runs yield the same HEAD SHA; it also commits the template blob in the templated fixture so A2's marker SHA resolves via `git cat-file`.
- **Manual golden capture of every extracted script's pre-extraction classification value (moved here from Phase 3, extended to every script — not an automated test)** (round 3 fix — sequencing and Contradiction Pass findings: Phase 2 ports A2's matching logic into a script one commit before Phase 3's characterization test was previously scheduled to capture a "pre-split" baseline, so that baseline was already post-mutation; round 5 fix — round 4's golden-fixture scope covered only the A2 marker-absent case, leaving every other extracted script with no before-state for Phase 2/3's fixture-diff). **Capture each extracted script's pre-extraction classification/output *value*, not raw prose/transcript, as one committed golden fixture per script per fixture-repo case** (round 4 fix — grilled decision 7 — extended round 5): run the marker-absent, no-current-template audit path (and the equivalent pre-extraction path for the jq-gate validation and commit-precondition checks) against the pre-Phase-2 (and pre-Phase-1.5-prose-move, since this phase moves no logic) `SKILL.md`, on both fixture cases above — the prose path does live `gh`/`git ls-remote` fetches, so the human performing this manual capture supplies the fixture files (release list, release body, peeled SHA) as the mocked fetch results, so golden and Phase 2 comparison run over identical inputs; each golden carries a fixed key set including `exit_code` (grilled decision 10) and a script/case pair a golden does not apply to is listed `n/a` in the mapping table, and commit each result as `tests/release/golden/<script>-<case>.json`, each a **value-level JSON projection** of the decision fields (grilled decision 10; a `jq -S`-normalised object, not raw stdout, since the scripts and their stdout shape do not exist until Phase 2) (e.g. `tests/release/golden/a2-marker-absent.json`, `tests/release/golden/jq-gate-templated.json`, `tests/release/golden/jq-gate-untemplated.json`). **`tests/release/golden/a2-templated.json` is a required deliverable, not an example** (round 8 fix — the marker-absent case exercises none of A2's real classification): captured from the marker-bearing templated fixture body, it must exercise at least one `ok` row, one `drifted` row, and one `template-marker-unresolvable` row. **Every `n/a` cell in the mapping table carries a reason string**; a bare `n/a` is not accepted. Each golden's `exit_code` is derived from grilled decision 20's prose-outcome → code mapping (`0` ok / `1` validation failure / `2` environment failure), not from an execution. The fixed key set is committed as `tests/release/golden/schema.json` and `golden_capture_commit` is recorded in `tests/parity/.release-baseline-meta.json` (grilled decision 14), so Phase 2 reads the key set from the schema file and asserts the goldens are byte-identical to that commit. **This capture is recorded in this phase's Findings entry as a manual, two-reader-verified step, not an automated pytest/bash assertion** (grilled decision 14 — the golden's authority is an *agreed reading of the prose at `golden_capture_commit`*, not observed behavior, since `SKILL.md` has no substitution seam and driving it over injected fixtures means instructing the model to deviate from the prose being characterized; the Findings entry records, per golden, the exact operator prompt, the fixture files used, both readers' derivations — Claude and Codex via `codex:rescue` — and their agreement; after this phase goldens are frozen and a Phase 2/3 disagreement is a script bug) (round 5 fix — round 4's "add a test in this phase asserting the prose-driven path produces that golden value" named no runner, because the prose-driven path is an LLM reading `SKILL.md`, which no pytest/bash harness in this repo can execute; the automated half of this comparison lives entirely in Phase 2/3, which assert the extracted script's stdout against the committed golden value). Phase 2's Validation gate then asserts each extracted script's stdout produces the *same* golden value for its case, on both fixture repos — this is what supplies Phase 2's/Phase 3's fixture-diff "before" state for every script, not only A2's.
- **Record each anchor-bounded region's byte length, with a file column and an exemption column** (round 4 fix — Phase 4's per-region byte-length assertion needs a real Phase 1.5 recording task, and `.release-test-id-baseline.txt`'s pytest-id-only format can't hold these rows; round 5 fix — the file needs a `file` column so Phase 4 can resolve a region after Phase 3 relocates it into `references/`, and an `exempt` column with a machine-readable reason string so Phase 4's tolerance check can read shrink-exemptions directly instead of parsing Phase 2/3's prose mapping tables) into a new committed data file, `tests/parity/.release-region-length-baseline.tsv` (columns: `region`, `file`, `byte_length`, `exempt` — empty string when not exempt), with a `# captured-at: <sha>` header (and a Findings pointer) — Phase 4 asserts against this file and its recorded commit is a strict ancestor of Phase 2's first commit and of Phase 4's tree, mirroring the pytest-id baseline's anti-rebaseline pin above. Anchor-comment bytes are included in each region's recorded length (Requirements above). **Phase 2 and Phase 3 append rows for regions they shrink or relocate, and Phase 4 reads the last row per `region` regardless of `file` (grilled decision 21, superseding decision 11's `(region,file)` grouping — otherwise Phase 3's relocation leaves an empty `(region, SKILL.md)` row that fails by construction); Phase 3 only appends new rows for regions it relocates into `references/` (with the new file's path in the `file` column); it never edits an existing Phase-1.5-recorded row in place** (round 5 fix — a cross-finding conflict between "append" and "edit" proposals; append is the safe direction since it preserves Phase 1.5's original capture as the audit trail).
- **Record this phase's first commit SHA** (round 5 fix — Phase 4's superset-of-test-ids ancestry check needs Phase 1.5's first commit as the boundary point, but no phase previously recorded it) in `tests/parity/.release-baseline-meta.json` as `phase15_first_commit` — alongside `golden_capture_commit` (grilled decision 14) — written by this phase's last commit, machine-readable (grilled decision 11), and pointed to from this phase's Findings entry, alongside the mapping table and golden-fixture capture-commit records above.
- This phase's own scope now includes the round-2/round-3 corrected sizing (bullet above) — because it both creates and populates the ~15 region-helper functions, ships its **own mapping table** (old inline `.index()`/`.find()` call site → new anchor-keyed helper call) and its own in-phase coverage gate (round 3 fix — Phase 1.5 was previously the largest test-mutating phase with neither, an omission the Requirements mapping-table rule was scoped past by only naming "Phase 2, Phase 3"). **This phase's mapping-table mutation evidence is anchor-scoped, not script-scoped** (round 5 fix — Requirements' mutation-evidence rule is worded over "the extracted script," which does not exist yet in this no-logic-moves phase): each row's evidence is a deliberate one-line mutation of the anchor comment itself (e.g. rename `<!-- skein:step-3 -->` to `<!-- skein:step-3x -->` in a scratch copy), captured failing pytest output, then reverted — proving the retargeted call site actually depends on the anchor rather than passing vacuously via a stale fallback. The coverage gate is a concrete command created **in this phase** (`just release-baseline-check`, registered in `ci:` and `parity-tests` in this phase — round 7 fix, comparing the filtered `--collect-only -q` output against `tests/parity/.release-test-id-baseline.txt`; Phase 4 adds only the ancestry pin — round 6 fix), run here too: at the end of this phase, `--collect-only -q`'s output must be a superset of `tests/parity/.release-test-id-baseline.txt`, checked immediately rather than left solely to Phase 4.
- Add a scratch-copy rename regression test, parameterized over all three anchored files (`SKILL.md` scratch copy, `README.md`, `docs/dev_plans/20260712-feature-release-skill.md` — round 3 fix, was scoped to `SKILL.md` alone): rename a heading (or, for `README.md`, edit a non-anchor cell) in a scratch copy, assert the suite still locates the region correctly via the anchor (not the now-stale heading text). **Mechanism (round 8 fix — the suite's tests are parametrized over the module-level `RELEASE_SKILLS` path list at `test_release_skill_contract.py:239`, so there is no way to point them at a scratch copy):** the anchor-keyed region helpers take `text: str`, not a path; the rename test calls each helper on the renamed scratch *text* and asserts the returned region equals the region from the original text. This phase's mapping table lists every helper name, and the rename test is parameterized over all of them, not a sample. For `README.md` the observable is a named `AssertionError` from the exact cell-equality check (`test_release_skill_contract.py:316-329` — an edited cell fails by design), not a silent pass and not a bare `StopIteration`; specify and assert that (round 6 fix).
- No jq-gate, schema, or template logic moves in this phase — `SKILL.md` prose content is otherwise byte-identical before/after except for the inserted anchor comments and any heading-spelling normalization from the bullet above; the latter is a deliberate, disclosed exception to "byte-identical except anchors," not an oversight (round 2 fix — the prior draft claimed a stronger invariant than this phase's own heading-normalization task delivers).
- **Add `fetch-depth: 0` to `.github/workflows/ci.yml`'s `actions/checkout@v4` step** (round 8 fix — the default is depth-1, so every ancestry pin would take the shallow-clone path and the anti-rebaseline gate would be a permanent no-op on the only enforcing plane). Per grilled decision 13, the ancestry checks then **fail hard** on a shallow clone or a missing `.release-baseline-meta.json` rather than warning, and this branch's history is append-only from this phase's first commit until merge (no rebase/amend/squash; no refresh recipe exists by design).
- **Internal task order for this phase (round 5 fix — this phase's outputs are order-dependent but no order was pinned, and it may span multiple mirror-atomic commits per the Requirements atomicity split):** (1) fixture cases (static inputs + `tests/release/lib.sh` builder), (2) anchors + heading normalization, (3) golden-fixture capture (needs the pre-anchor-move `SKILL.md` for the "before" side, so capture before or in the same commit as the anchor insertion — anchors don't move logic, so ordering relative to (2) is flexible, but must precede Phase 2), (4) byte-length baseline recording (needs the anchors from (2) to exist), (5) mapping table + in-phase coverage gate + scratch-copy rename regression test (needs (2)-(4) complete to test against). The `RELEASE_LAGGING_MIRROR_OK` task above precedes all five, since step (2)'s Claude-only anchor insertion is the first thing that trips the `release-skill-md` plane. No commit in this ordering touches `plugins/skein-codex/` (grilled decision 23).

### Phase 2: Extract scripts/ (deterministic logic)

**Impl files:** `plugins/skein/skills/release/lib/read-release-template.sh` (+ siblings per Phase 1's inventory — **Claude mirror only**; the Codex `lib/` half moves to Phase 3.5 per grilled decision 23), `tests/parity/test-applier-bundle-parity.sh` (new `RELEASE_LIB_PARITY_FILES` array, modeled on `GAUNTLET_LIB_PARITY_FILES`), `scripts/check-prompt-parity.sh` (each new exact-full-line divergence pair in `normalize_release_workflow()`, both sides, per-call-site, per redecided grilled decision 6 — not a sentinel-token mechanism, round 2's superseded idea; harness-neutral, so it also serves as the spec for the Codex `SKILL.md` line Phase 3.5 writes), `justfile` (`lint-scripts` globs), `.pre-commit-config.yaml` (matching `files:` patterns), `tests/parity/.release-region-length-baseline.tsv` (append rows for shrunk regions), `tests/parity/.release-baseline-meta.json` (`phase2_first_commit` — round 8 fix, the task below named it but the list omitted it), `tests/parity/test-prompt-parity-extended.sh` (seed extension for the new gates), `tests/release/` (new bash suite and golden comparisons), `AGENTS.md` (one sentence recording `lib/` anchors as the hand-authored form, grilled decision 15, plus the `RELEASE_JQ`/`RELEASE_GIT` env-var contract per grilled decision 17), `plugins/skein/skills/release/SKILL.md`
**Test files:** `tests/parity/test_release_skill_contract.py` (retargeted per the mapping table below), new script-level unit tests as bash suites under `tests/<suite>/` (round 2 fix — not pytest, see Requirements/Architecture above)
**Test command:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md' uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md release-lib release-references' just ci`
**Goal:** every call site from the Phase 1 inventory calls the same hand-authored, mirror-parity-checked script instead of re-deriving the logic in prose; `SKILL.md` shrinks by roughly the amount of prose extracted; behavior is unchanged; the suite's coverage of the extracted logic moves to script-level tests with an explicit mapping table, not silently dropped.

- Port the jq gate sequence (empty/shape/enum/unknown-key/duplicate-key checks, per `SKILL.md`'s existing `## Canonical Format` schema section) into `read-release-template.sh`, following `plugins/skein/skills/review-gauntlet/lib/gate-bounded.sh`'s validation-helper pattern (round 2 fix — the plan's prior citation, `deep-review/scripts/persist-common.sh`'s `persist_validate_json_shape`, does not exist at that path; the actual file is `plugins/skein/skills/deep-review/scripts/lib/persist-common.sh`, but `review-gauntlet/lib/` is the correct structural precedent to follow now that scripts are hand-authored per mirror, not bundled). Assert this script's projected JSON stdout and exit code for both the templated and untemplated fixture case equal Phase 1.5's committed `tests/release/golden/jq-gate-<case>.json` values exactly (round 5 fix — extending grilled decision 7's golden-value comparison, previously scoped to A2 only, to every extracted script).
- Port the commit-precondition check (tracked, matches HEAD) and marker resolution (dual-anchor scheme, Audit Mode's A2 matching) into sibling scripts in the **Claude** mirror's `lib/` directory (the Codex mirror's hand-authored copies land in Phase 3.5 — grilled decision 23). Immediately after, assert the extracted script's projected JSON stdout for the marker-absent, no-current-template case equals Phase 1.5's committed `tests/release/golden/a2-marker-absent.json` value exactly (round 4 fix — grilled decision 7: this replaces "re-run Phase 1.5's test," since the artifact shape changes from prose-transcript to script-stdout across this boundary; the golden value, captured in Phase 1.5, is what stays constant and also supplies this phase's fixture-diff "before" state, below), and likewise assert the commit-precondition script's projected stdout against its own Phase 1.5 golden value for both fixture cases.
- Add `tests/parity/test-applier-bundle-parity.sh`'s `RELEASE_LIB_PARITY_FILES` array (the new scripts' filenames) and **all four** mechanisms of the `GAUNTLET_LIB_PARITY_FILES` precedent (round 4 fix — round 3's "both halves" was itself an incomplete copy: only 2 of 4 actual precedent mechanisms were captured): the byte-identity loop (modeled on lines ~138-147), the enumeration half (modeled on lines ~151-210, which walks the union of both mirrors' `lib/` directories and fails on any basename not registered in the array), the precedent's `PARITY_GAUNTLET_LIB_ROOT` test-override fixture (lets tests point the parity check at a scratch `lib/` tree instead of the real one, so parity-check behavior itself is testable), and its **G9 self-test** (a test that deliberately adds an unregistered file to the scratch tree and asserts the enumeration guard actually fails — proving the guard isn't vacuous). If any new script legitimately resolves `${CLAUDE_PLUGIN_ROOT}` vs `$SKILL_DIR`, exclude it via the same documented anchor-divergent-exclusion pattern used for `gauntlet-common.sh`. **All four mechanisms are authored here, but the Codex mirror's `lib/` does not exist until Phase 3.5, so the `release-lib` plane runs acknowledged (`RELEASE_LAGGING_MIRROR_OK`) in this phase and Phase 3; the self-tests above, which use the `PARITY_RELEASE_LIB_ROOT` scratch-tree override, are **not** acknowledged and must pass here — they exercise synthetic trees containing both halves, so they are unaffected by the real Codex mirror lagging (grilled decision 23).** **Extend the existing self-tests' recursion-skip guards to include the new `PARITY_RELEASE_LIB_ROOT` override variable (round 8 fix — named to match the precedent's `PARITY_<section>_ROOT` family, not the deviating `RELEASE_LIB_PARITY_ROOT`) alongside the existing ones, `PARITY_GAUNTLET_LIB_ROOT` and `PARITY_BUNDLE_REF_ROOT` (round 6 fix — the plan previously named a nonexistent `GAUNTLET_LIB_PARITY_ROOT`; the actual variable is `PARITY_GAUNTLET_LIB_ROOT`, guarded alone at `:221` and with the others at `:314`)** (round 5 fix — the precedent's own convention is that every override variable appears in every self-test's skip guard, e.g. `PARITY_GAUNTLET_LIB_ROOT` alone at `test-applier-bundle-parity.sh:221` and with `PARITY_BUNDLE_REF_ROOT` at `:314`; omitting the new variable from those existing guards would re-spawn the existing fixture runs when release's own fixture is exercised). **Section 7's bundle-reference check is out of scope (round 8 fix — grilled decision 15): its grep matches only `skills/<skill>/scripts/<x>.sh`, which cannot match release's `lib/` anchors on either mirror, so the previously-planned §7 registration/exemption task is dropped rather than worked around — this also removes the workaround whose stated remedy (`register it in scripts/lib/bundle-map.sh`) is exactly what decision 1 forbids.** The Codex-side guard is the byte-parity arm above.
- Write the **Claude** mirror's real path anchor (`${CLAUDE_PLUGIN_ROOT}`) directly into its `SKILL.md` prose at each new script-invocation call site, and add the **complete** new exact-full-line divergence pair per call site to `normalize_release_workflow()` in `scripts/check-prompt-parity.sh` — both sides' verbatim constants, both `awk -v` entries, both normalizer branches, and both `count_release_contract_line` calls plus their `-ne 1` gates — the same 2-symmetric-pair/4-region shape as its existing sanctioned pairs (round 4 correction: not "six pairs" as round 3 said — see Context grilled decision 6), each pinned to a per-mirror occurrence count of exactly 1. `scripts/check-prompt-parity.sh` is a **harness-neutral repo file**, so defining the expected Codex line there is not a Codex-mirror edit; it is the machine-readable spec Phase 3.5's `codex:rescue` pass must satisfy, and the Codex `SKILL.md` bytes themselves are written only there (grilled decision 23). The two anchors are never collapsed to a shared token. **Until Phase 3.5 the real Codex mirror carries none of these lines, so its `-ne 1` gate trips and the `release-skill-md` plane runs acknowledged via `RELEASE_LAGGING_MIRROR_OK` in this phase and Phase 3.** **Each call site carries the precedent's existence guard** (the `${CLAUDE_PLUGIN_ROOT}` form here; `[ -x "${SKILL_DIR:?}/lib/<name>.sh" ] || { echo "<remedy>"; exit 1; }` on Codex in Phase 3.5 — Requirements above), so an unset or mis-expanded anchor fails loudly instead of reading as "script absent, fall back to prose". **Also extend `tests/parity/test-prompt-parity-extended.sh`'s `seed_release_pair_with_documented_divergence` (line ~129, used by 9 tests including `test_release_documented_harness_differences_are_normalized`, which requires exit 0) with the new anchor lines on both seeded halves — the seed is synthetic, so it carries the Codex line from this phase onward and is not affected by the real Codex mirror lagging — and add self-test cases per new gate: passes with the anchor present once, fails on a missing anchor, fails on a duplicated anchor** (round 6 fix — the new `-ne 1` gates set `PARITY_DIFF=1` and would otherwise break the seeded synthetic pair, with no self-test of the gates themselves) (round 3 redecided grilled decision 6, superseding round 2's sentinel-token approach, which had no expansion mechanism on either harness).
- Rewrite each Phase-1-inventoried call site in the **Claude** `SKILL.md` to invoke the relevant script and interpret its output/exit code, removing the duplicated prose. Phase 3.5 applies the identical rewrite to the Codex mirror (grilled decision 23).
- Write the Phase 2 mapping table (old assertion name/line on `SKILL.md` prose → new script-behavior test) as this phase's own deliverable, in this plan's Findings section, **with evidence of each retargeted test failing under a deliberate mutation of the extracted script** (round 3 fix, see Requirements above); retarget each listed assertion in `tests/parity/test_release_skill_contract.py` in the same commit, per the "retarget" definition in Requirements above.
- Add script-level unit test coverage for every new script as a bash suite under `tests/<suite>/` (e.g. `tests/release/test-read-release-template.sh`) with its own `justfile` recipe, matching `tests/reconciliation/`/`tests/lenses/`/`tests/gauntlet/`'s existing convention — **and append that recipe to `justfile`'s `ci:` dependency list in this same commit** (round 3 fix — `ci:` is a hand-maintained list, not a glob; `lint-scripts`' globs lint the suite but never run it, so without this the phase's own `just ci` Validation cmd goes green having never executed the new coverage).
- **Golden comparison, against Phase 1.5's golden values, not a live before/after capture** (round 5 fix; reworked round 6 — grilled decisions 9/10: no live before-state can exist since this phase first creates the scripts, and the goldens are value-level projections). These comparisons are **cases in a Phase 2 bash suite under `tests/release/`**, wired into `ci:` per the bullet above, so `just ci` is the plane that runs them and Phase 3's re-run is simply `just ci`. For each extracted script and each case under `tests/release/fixtures/{templated,untemplated}/`: feed the static inputs to the script, then assert the `jq -S` projection of its stdout, and its exit code, equal the corresponding `tests/release/golden/<script>-<case>.json` value exactly. Each script's projection is documented in this phase's mapping table. **The suite runs each case against the Claude mirror's `lib/` only, with `RELEASE_LIB_PARITY_FILES` byte-parity as the transitive guarantee for the Codex copy** (round 8 fix — pick one explicitly); the release anchor-divergent-exclusion list **must be empty**, since grilled decision 9 makes the scripts pure over injected inputs with pre-resolved executables and therefore anchor-free. **Freeze check (grilled decision 14):** the suite also asserts `git diff --quiet <golden_capture_commit> -- tests/release/golden/` and reads the key set from `tests/release/golden/schema.json`, so a golden edited in this phase fails rather than silently redefining the before-state.
- **Negative cases and the exit-code contract** (round 8 fix — golden coverage was one happy path per script per case, with the fail-closed paths the plan itself specifies untested): the `tests/release/` suite adds, per script, a case per jq gate over `tests/release/fixtures/invalid/<gate>.json` (expect exit `1` plus the named classification), the four injected-executable failures (`RELEASE_JQ`/`RELEASE_GIT` unset, relative, missing, non-executable — expect exit `2`), the SHA-256 hard-stop, and the **`RELEASE_JQ`-unset success case on the untemplated path** (state (i) of grilled decision 18, expect exit `0`). Exit codes come from grilled decision 20's vocabulary, which Requirements pins.
- **Permanent self-test that the golden comparison can fail** (round 8 fix — modeled on `test-applier-bundle-parity.sh:211-242`'s G9, which is a permanent self-test rather than a one-time transcript): a case that runs the comparison against a tampered copy of a golden (one changed field, one changed `exit_code`, and one dropped key) and asserts non-zero exit naming the differing key — so a later projection change (e.g. `jq -S` over an empty object on both sides) cannot make every comparison pass vacuously.
- **Amend `SKILL.md`'s pinned-executable invariant to name the script-launch boundary** (grilled decision 17, **Claude mirror here; Codex mirror in Phase 3.5**): the invariant gains "launch of a mirror-parity-checked release `lib/` script with pre-verified executables passed by absolute path" as a verification boundary, so a single launch re-verifies rather than each in-script `jq`/`git` call. Retarget `test_release_step1b_bootstraps_pinned_context_before_its_first_launch` (`test_release_skill_contract.py:2630`) and `test_release_jq_pin_is_conditional_on_template_presence` (`:2658`) to the amended boundary in the same commit, with mapping-table rows and mutation evidence, and record the boundary and the env-var contract in `AGENTS.md`.
- Add new scripts to `justfile`'s `lint-scripts` globs and `.pre-commit-config.yaml`'s matching `files:` patterns in this same commit.
- **This phase touches no file under `plugins/skein-codex/`** (grilled decision 23). Every Codex `SKILL.md` and `lib/` edit this phase's Claude-side work implies is carried out by `codex:rescue` in Phase 3.5, against the Claude mirror finished here as the byte-for-byte reference modulo path anchors. Record in this phase's Findings entry the exact list of Claude-side artifacts Phase 3.5 must mirror (each new `lib/*.sh` filename, each call-site line with its Claude anchor and the paired Codex line text from `check-prompt-parity.sh`, and the amended pinned-executable invariant paragraph), so Phase 3.5 has a checklist rather than a diff hunt.
- **Append `.release-region-length-baseline.tsv` rows for every region this phase shrinks** (round 6 fix — Phase 2 removes duplicated prose from anchored regions well past the 10%/20-byte tolerance and previously had no task recording it; Phase 4 asserts only the last row per `(region,file)`, grilled decision 11), in the same commit as each shrink; set `exempt` only for a region deliberately emptied.
- **Record this phase's first commit SHA** as `phase2_first_commit` in `tests/parity/.release-baseline-meta.json` (written by this phase's last commit; add that file to this phase's Impl files) — Phase 4's `.tsv` ancestry pin targets it (round 7 fix — no phase previously recorded it).
- **Document the A2 script's JSON field schema in this phase's mapping table** (round 7 fix — decision 12; the A2→A2.5 field-contract *assertion* moves to Phase 3, because `references/audit-inference.md`, the file that names the fields A2.5 parses, is created there and this phase cannot assert against a file that does not exist yet): the schema lists each classification field the script emits under two labelled subsections — **script-stdout fields** and **fetch-side fields** — using the identical labels `references/audit-inference.md` uses in Phase 3, so Phase 3's field-contract assertion can parse only the script-stdout subsection without failing by construction on fetch-sourced fields (round 8 fix).

### Phase 3: Progressive disclosure split + A2/A2.5 separation

**Impl files:** `plugins/skein/skills/release/references/template-subsystem.md`, `plugins/skein/skills/release/references/audit-inference.md` (new, round 5 — grilled decision 8), `plugins/skein/skills/release/SKILL.md` (Claude mirror only — the Codex mirror's `references/` files and `SKILL.md` changes move to Phase 3.5 per grilled decision 23), `scripts/check-prompt-parity.sh` (`references/` byte-parity arm, two files — harness-neutral, written here, first enforced unacknowledged in Phase 3.5), `tests/parity/.release-region-length-baseline.tsv` (append rows for relocated regions), `tests/release/` (A2→A2.5 field-contract assertion), `docs/skills_architecture/20260522-design-claude-skills-architecture.md` (invocation-mode catalogue entry for `--infer-template`), release skill's frontmatter/README subcommand listing
**Test files:** `tests/parity/test_release_skill_contract.py` (retargeted per the mapping table below; the marker-absent/no-current-template golden was captured in Phase 1.5 (manual, value-level — round 7 fix, previously described as a moved automated test); this phase re-runs Phase 2's golden comparison via `just ci`)
**Test command:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md' uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `RELEASE_LAGGING_MIRROR_OK='release-skill-md release-lib release-references' just ci`
**Goal:** an untemplated `/release <version>` cut is verified, via the static assertion and manual transcript check below (not an absolute harness-enforced guarantee — round 5 fix, matching the hedge already stated in Requirements/Review Focus), to not read `references/template-subsystem.md`; A2.5 is reachable both as part of `/release audit`'s default run (grilled decision 2) and standalone via `--infer-template` on templated **and untemplated** repos alike — the flag runs A1 → A2 (script, punch-list rendering suppressed) → A2.5 and bypasses A2.5's template-absent gate (grilled decision 16), and its inference prose lives in the separately-triggered `references/audit-inference.md` (grilled decision 8) rather than being gated on the templated branch; A2.5 consumes **the A2 script's JSON stdout** (grilled decisions 3/12), not a new artifact, and the human-readable text `SKILL.md` renders from it is unasserted.

- Move the template schema definition, jq-gate-sequence *documentation* (the scripts now carry the logic; this is the remaining prose description), and Audit Mode's dual-anchor marker resolution into `references/template-subsystem.md` (**Claude mirror; the Codex copies land in Phase 3.5** — grilled decision 23), read by `SKILL.md` only on the templated branch per the `content-review` precedent named in Phase 1. **Move A2.5's inference prose into a separate `references/audit-inference.md` (Claude mirror, round 5 fix — grilled decision 8), read by `SKILL.md` whenever Audit Mode runs `--infer-template` or default `/release audit`**, independent of template presence — this closes the contradiction where A2.5's prose would otherwise be unreachable from an untemplated repo's `--infer-template` invocation. Neither `references/` file carries a path-anchor line (Requirements above); the script-invocation call sites with real per-mirror anchors stay in `SKILL.md`, so `scripts/check-prompt-parity.sh`'s `references/` byte-parity arm (below) can stay a plain byte-identity check.
- Add `/release audit --infer-template` as an **additional** standalone entry point defined topologically per grilled decision 16: it runs **A1 → A2 (the extracted script, punch-list rendering suppressed) → A2.5**, emitting only the inference proposal, and it **bypasses** A2.5's template-absent gate (the third named behavior change). A2.5 keeps running by default inside plain `/release audit` (grilled decision 2). **It re-enters A2 at that step's existing script call site by prose cross-reference — it adds no new invocation line, so no new divergence pair, no `test-prompt-parity-extended.sh` seed extension, and no Phase 1 inventory entry are needed; this is asserted by re-running Phase 2's cardinality gates unchanged** (round 8 fix — a second invocation line would trip the per-call-site `-ne 1` gate). Name the exact fields A2.5 parses from the A2 script's stdout, in `references/audit-inference.md`'s two labelled subsections (**script-stdout fields**, **fetch-side fields**), as part of this task's deliverable.
- Update the doc/frontmatter/README entries for the new `--infer-template` flag (`docs/skills_architecture/20260522-design-claude-skills-architecture.md` line ~36 prose and the README `release` row). Round 6 fix: `test_invocation_mode_count_matches_release_catalogue` (`test_release_skill_contract.py:871-880`) asserts only skill-count strings ("2 of 14/15 skills … clear both") and pins no subcommand or flag list, so `--infer-template` changes nothing it checks — add a named test in `tests/parity/test_release_skill_contract.py` that pins the `release` subcommand/flag list as listed in `SKILL.md` frontmatter/usage and the README `release` row (assert `--infer-template` appears in both), so the new entry point cannot pass vacuously.
- Assert the marker-absent, no-current-template audit path still equals Phase 1.5's `tests/release/golden/a2-marker-absent.json` value (projected JSON stdout) after this phase's A2/A2.5 split (round 4 fix — grilled decision 7: comparison is against the golden value, not a re-run of Phase 1.5's own test mechanism). Per Context above, do not fix xhigh findings #9/#11 in this phase even if the split makes a fix obvious — log it separately.
- **Assert the A2→A2.5 field contract** (moved here from Phase 2, round 7 fix): a bash-suite assertion that every field name in the **script-stdout fields** subsection of `references/audit-inference.md`'s A2.5 field list (the fetch-side subsection is explicitly out of scope, since fetch results are script inputs, never outputs — grilled decision 9) appears in the A2 script's projected JSON stdout and goldens (the assertion parses that section's field list; a rename on either side fails), with a mapping-table row carrying mutation evidence (rename a field in the script, capture the failure, revert).
- Add the progressive-disclosure boundary check named in Review Focus: **three** static assertions (round 8 fix — `template-subsystem.md` has two triggers, so it carries two references, grilled decision 22) — `references/template-subsystem.md` is referenced exactly twice, once textually inside the templated-branch region anchor and once inside the A2 marker-bearing sub-region anchor, both added in Phase 1.5, and nowhere else; `references/audit-inference.md` is referenced exactly once, textually inside the Audit Mode region anchor added in Phase 1.5 (round 6 fix) — plus a manual transcript check for each file. **Transcript-check protocol (round 8 fix — previously no command, observable, or record was named, so it was not an acceptance criterion):** run untemplated `/release <version>` against skein's own repo for `template-subsystem.md`, and `/release audit` against the untemplated fixture for `audit-inference.md`, both in Claude Code; the observable is **zero file-read tool calls on the gated file across the full transcript**; the record committed to this plan's Findings section is the transcript path (or its SHA-256), the date, the harness, and the verifying human. Apply the same recording rule to Phase 1.5's manual golden capture (model/harness, exact mocked-fetch inputs, both readers).
- **Assert decision 2's and decision 16's invariants statically** (round 8 fix — neither had a test path; the only new test pins the subcommand/flag list): (1) inside the Audit Mode region anchor, the default-run sequence still contains the A2.5 step marker and that marker is **not** textually inside the `--infer-template` sub-region; (2) the `references/audit-inference.md` reference is **not** inside the templated-branch region anchor. Both get rename-regression and mutation-evidence rows like any other assertion. Note (round 3 fix, now literally satisfied rather than merely noted): the `content-review/references` precedent this models really does ship two references files with a branching load rule — this plan's two-file split now matches that shape directly, rather than approximating a single-file case scoped narrower than the precedent.
- Extend `scripts/check-prompt-parity.sh` with a `references/` byte-parity arm — **two arms, one per new file** (round 5 fix) — modeled on the existing `content-review/references` block (lines ~563-577), so both new `references/` files are mirror-drift-guarded from the moment the Codex copies exist — they are not covered by any existing check otherwise. Since neither file carries a path anchor (per the task above), a plain `diff -r` byte-identity check suffices for both, with no normalizer needed. The arms are written here; because the Codex-mirror copies do not exist until Phase 3.5, the `release-references` plane runs acknowledged in this phase and is first enforced unacknowledged in Phase 3.5 (grilled decision 23).
- **Carry forward Phase 1.5's region-helper anchors for content moving into `references/`, by appending new rows, never editing Phase 1.5's** (round 4 addition, append-vs-edit resolved round 5 — a cross-finding conflict between two lens proposals): when this phase moves anchored `SKILL.md` content into `references/template-subsystem.md` or `references/audit-inference.md`, re-point the corresponding anchor-keyed test helpers at the new `references/` file location, **add both `references/` files to the Phase 1.5 rename-regression test's parameter list in the same commit** (round 8 fix — otherwise a heading rename inside `references/` regresses silently, since that test was parameterized over `SKILL.md`/`README.md`/the 20260712 plan only), and **append** a new row to `.release-region-length-baseline.tsv` for each relocated region (same `region` name, new `file` value, current byte length, empty `exempt` unless also shrunk) rather than mutating the row Phase 1.5 recorded — Phase 1.5's original row stays as the audit trail of the pre-move state. Record the retarget in this phase's mapping table like any other relocated assertion.
- Write the Phase 3 mapping table (old assertion name/line on `SKILL.md` prose → new `references/`-content test) as this phase's own deliverable, with evidence of each retargeted test failing under a deliberate content mutation (round 3 fix, per Requirements above); retarget each listed assertion in `tests/parity/test_release_skill_contract.py` in the same commit.
- **This phase does not touch the extracted scripts** (round 5 fix — round 3's fixture-diff task, copied verbatim from Phase 2, was vacuous here: Phase 3 is a pure prose/references move plus the A2.5 entry-point addition, and touches no script file per the Impl-files list above). Instead, this phase's no-behavior-drift evidence is: (a) the mapping-table mutation evidence above, and (b) re-running Phase 2's fixture-diff commands unchanged post-Phase-3 and confirming they still pass — since this phase changes where prose lives, not what any script does, a passing re-run of Phase 2's existing golden-value comparisons is sufficient; no new fixture-diff command is needed.
- **Record the refactor's size result in this phase's Findings entry** (round 8 fix — the Objective's headline goal had no measurable acceptance target): `wc -c` of the **Claude** `SKILL.md` and its two `references/` files after this phase, compared against the pre-refactor 243,730 bytes and round 2's ~195K estimate. A post-refactor `SKILL.md` at or above the pre-refactor size fails this phase. Phase 3.5 records the same measurement for the Codex mirror and asserts the two mirrors' totals match.
- **This phase touches no file under `plugins/skein-codex/`** (grilled decision 23). Record in this phase's Findings entry the Claude-side artifacts Phase 3.5 must mirror: both new `references/` filenames and their content, the `SKILL.md` regions removed, and the three reference-count assertions' expected placement, so Phase 3.5 works from a checklist.

### Phase 3.5: Codex mirror alignment via codex:rescue

**Impl files:** `plugins/skein-codex/skills/release/SKILL.md`, `plugins/skein-codex/skills/release/lib/read-release-template.sh` (+ siblings, matching Phase 2's Claude set), `plugins/skein-codex/skills/release/references/template-subsystem.md`, `plugins/skein-codex/skills/release/references/audit-inference.md`, `docs/dev_plans/20260917-refactor-release-skill-structure.md` (Findings — the Phase 3.5 alignment record and, if the fallback path is taken, the disclosure)
**Test files:** `tests/parity/test_release_skill_contract.py`
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci`
**Goal:** the Codex mirror carries every change Phases 1.5–3 made to the Claude mirror — anchors, region markers, heading normalizations, script-invocation call sites with `"$SKILL_DIR"` anchors and existence guards, the amended pinned-executable invariant, `lib/*.sh`, and both `references/` files — byte-for-byte identical to the Claude mirror modulo the two path anchors and the pre-existing documented frontmatter/invocation-mode/execution-model divergences, so that plain `just ci` passes with `RELEASE_LAGGING_MIRROR_OK` **unset** and all three deferred parity planes (`release-skill-md`, `release-lib`, `release-references`) are enforced for the first time.

- **Worker: `codex:rescue`** (grilled decision 23). Every byte written under `plugins/skein-codex/` in this phase is authored by the Codex rescue subagent, per `AGENTS.md`'s mirror convention and this repo's memory note. Claude's role is to supply the brief (the Phase 2 and Phase 3 Findings checklists plus the Claude-mirror files as the reference), then verify.
- **Viability / fallback.** Run this phase via `codex:rescue` when the Codex CLI is available (`codex:setup` confirms readiness). If it is not — the documented `codex-rescue` node-missing fallback — fall back to a `conduct` worker that applies the hunks itself, then (a) byte-verifies each Codex file against its Claude counterpart under the sanctioned anchor substitutions (the same substitution `normalize_release_workflow()` performs) and the plain `diff -r` for `lib/` and `references/`, and (b) **discloses** in this phase's Findings entry that the Codex half was applied without Codex CLI review, naming every file. The disclosure is an acceptance criterion of this phase.
- Mirror Phase 1.5's anchor work: add the identical `<!-- skein:step-N -->` region anchors, the templated-branch region anchor, the Audit Mode region anchor, the A2 marker-bearing sub-region anchor, and the identical heading-spelling normalizations.
- Mirror Phase 2's extraction: hand-author `plugins/skein-codex/skills/release/lib/*.sh` byte-identical to the Claude copies (these scripts are anchor-free by grilled decision 9, so `RELEASE_LIB_PARITY_FILES`' byte-identity arm applies with an **empty** exclusion list), rewrite each inventoried call site to invoke them with the `"$SKILL_DIR"` anchor and the `[ -x "${SKILL_DIR:?}/lib/<name>.sh" ] || { echo "<remedy>"; exit 1; }` existence guard, using the exact Codex line text already pinned by `check-prompt-parity.sh`'s divergence-pair constants, and mirror the amended pinned-executable invariant paragraph. **Never collapse the anchors**: `${CLAUDE_PLUGIN_ROOT}` must not appear in the Codex mirror and `"$SKILL_DIR"` must not appear in the Claude mirror.
- Mirror Phase 3's split: create `plugins/skein-codex/skills/release/references/template-subsystem.md` and `audit-inference.md` byte-identical to the Claude copies (neither carries a path anchor, per Requirements), remove the relocated regions from the Codex `SKILL.md`, and mirror the `--infer-template` entry point, its two reference-count placements, and the doc/frontmatter/usage listing changes.
- **Turn the window off and prove it:** run `just ci` with `RELEASE_LAGGING_MIRROR_OK` unset and confirm zero `expected lagging-mirror drift:` lines on stderr — a remaining annotation means the window is still open and this phase is not done. Confirm each of the three planes specifically: the normalized `SKILL.md` diff is clean and every new per-call-site cardinality gate reports exactly 1 on both mirrors; `RELEASE_LIB_PARITY_FILES`' byte-identity and enumeration arms pass over the real trees; both `references/` `diff -r` arms pass.
- Re-run Phase 2's golden comparison and the full pytest prose-contract suite with the variable unset, so the Codex-mirror parameter of `RELEASE_SKILLS` actually executes rather than skipping. No golden is edited here (grilled decision 14) — a disagreement is a mirror bug.
- Record in this phase's Findings entry: `wc -c` of the Codex `SKILL.md` and its two `references/` files, asserted equal to Phase 3's recorded Claude-mirror measurements; the verbatim `normalize_release_workflow()` divergence pairs now satisfied; and the codex:rescue session reference (or the fallback disclosure above).
- This phase adds **no** new parity machinery, `justfile` recipe, lint glob or pre-commit pattern — all of it was authored in Phases 1.5–3 against harness-neutral files, and lint/pre-commit deliberately list the Claude mirror only (grilled decision 15).

### Phase 4: Coverage-narrowing check and rename-resilience regression

**Impl files:** `tests/parity/test_release_skill_contract.py`, `justfile` (`parity-tests` registration, round 6 fix)
**Test files:** `tests/parity/test_release_skill_contract.py`
**Test command:** `uv run --with pytest python -m pytest tests/parity/test_release_skill_contract.py -v`
**Validation cmd:** `just ci`
**Goal:** prove the suite's *coverage* did not narrow across Phases 1.5–3.5 (both mirrors are in their final state by then, since Phase 3.5 precedes this phase — grilled decision 23; this phase runs with `RELEASE_LAGGING_MIRROR_OK` unset, so any still-open window fails here too), using two complementary checks compatible with the refactor's own goal of shrinking `SKILL.md`: a **coverage-preservation superset check** over the pytest test-id baseline, plus a **per-region byte-length tolerance measured against the last appended baseline row** — the latter does not fail on intentional shrinkage precisely because Phases 2/3 append a row recording each accepted post-change length, so the tolerance is always read against the most recently accepted state (round 8 fix — the Goal previously rejected the tolerance the third bullet below adds).

- **Extend** (Phase 1.5 created `just release-baseline-check`; this phase adds only the ancestry pin) the superset-of-test-ids check scoped to `test_release_skill_contract.py` only (round 2 fix — the new script-level unit tests are bash suites, which emit no pytest ids, so they cannot be spanned by this check without breaking the repo's bash-suite convention; see Requirements/Architecture above): the post-refactor collected pytest test-id set must be a superset of `tests/parity/.release-test-id-baseline.txt` (round 3 fix — a committed data file, not a list embedded in Phase 1's Findings prose; see Requirements above), and the baseline's capture commit (read from its `# captured-at: <sha>` header, not this plan's Findings prose) must be a strict ancestor of `phase15_first_commit` read from `tests/parity/.release-baseline-meta.json`. **A shallow clone, or a missing `.release-baseline-meta.json`, is a hard failure, never a skip** (round 8 fix, grilled decision 13 — Phase 1.5 adds `fetch-depth: 0` to `ci.yml` so CI can resolve ancestry; leaving this as "skip with a warning" made the only anti-rebaseline gate a permanent no-op on the only enforcing plane). The same hard-failure rule applies to the `.tsv` and `golden_capture_commit` pins below — proving no prose-contract assertion was dropped without a named replacement, and that the baseline itself wasn't silently recaptured onto an already-mutated tree.
- Add a bash-suite **enumeration** check, modeled on `tests/parity/test-applier-bundle-parity.sh`'s enumeration half (round 3 fix — "confirm every suite is registered and passing" previously named no mechanism): walk `tests/release/test-*.sh` (excluding the `tests/release/lib.sh` builder, which is a helper, not a suite), and for each suite assert a `justfile` recipe invokes it and that recipe is a `ci:` dependency (two-hop: suite → recipe → `ci:`, since `ci:` lists recipes, not files), failing loudly on any unregistered suite rather than silently skipping it. **Register this enumeration check in the hand-maintained `parity-tests` recipe (`justfile:27-43` — round 8 fix, the recipe's last line is `tests/auto-fix/test-review-plan-marker-write.sh` at :44, so the pytest/bash body runs to :43) and list `justfile` among this phase's Impl files** (round 6 fix — the check is bash-shaped but Phase 4's Impl files named only the pytest file, the same hole round 3 closed for `ci:`). This is the script-coverage half of the proof, now actually enforced rather than merely confirmed by inspection.
- Add a per-region byte-length assertion (round 3 fix — the Goal's "non-empty, not silently shrunk to a vacuous substring match" claim previously had no implementing task; **round 4 fix**: `.release-test-id-baseline.txt` is a sorted pytest-test-id list and cannot hold byte-length rows, and no Phase 1.5 task recorded them; **round 5 fix**: the tolerance and exemption mechanism were both still undefined even after round 4's separate-file fix). Phase 1.5 records each anchor-bounded region's byte length into a **separate** committed data file, `tests/parity/.release-region-length-baseline.tsv` (columns: `region`, `file`, `byte_length`, `exempt` — round 5 fix, added `file` and `exempt` columns), with a `# captured-at: <sha>` header. This phase asserts, for the **last appended row per `region`** (regardless of `file` — grilled decision 21, round 8 fix: grouping by `(region,file)` would still evaluate the now-empty `(region, SKILL.md)` row after Phase 3 relocates a region, failing by construction) whose `exempt` column is empty, the current byte length of `region` in that row's `file` is non-empty and within **10% or 20 bytes (whichever is larger) of that last row's `byte_length`** (round 5 fix — a concrete tolerance, not "a documented tolerance" with no number); rows with a non-empty `exempt` value are skipped, read directly from the `.tsv` (round 5 fix — not parsed out of Phase 2/3's prose mapping tables, which have no exemption field). This phase also asserts the `.tsv`'s `# captured-at:` commit is a strict ancestor of `phase2_first_commit` (from `tests/parity/.release-baseline-meta.json`) — the same anti-rebaseline pin as the pytest-id baseline above (round 6 fix: the `.tsv` capture commit is a strict ancestor of **Phase 2's first commit and Phase 4's tree** — not of Phase 1.5's first commit, which is impossible since the `.tsv` is recorded in Phase 1.5's step (4), after steps (1)-(2) commit; and the assertion reads the **last row per `(region,file)`** since Phase 2/3 append rows for shrunk/relocated regions — grilled decision 11), since Phase 3 appends rows to this same file and an out-of-order re-run must not silently recapture it. **Also assert `golden_capture_commit` is a strict ancestor of `phase2_first_commit`** (grilled decision 14), giving the goldens the same anti-rebaseline pin as the other two baselines.
- **Post-merge sunset is an explicit removal commit, not a runtime conditional** (round 8 fix, grilled decision 13): once this branch merges, a separate, reviewed commit deletes `tests/parity/.release-baseline-meta.json` **and** the three ancestry assertions together, leaving only the content checks (test-id superset, region-length tolerance, golden freeze). **The same commit removes `RELEASE_LAGGING_MIRROR_OK` and its self-tests entirely** (grilled decision 23) — the window closed in Phase 3.5, and leaving the variable in place would let a future Codex-mirror lag be acknowledged indefinitely without a plan backing it. Making the pins conditional on the meta file existing is explicitly rejected — deleting the file would then silently disable the gate everywhere, including CI.
- The Phase 1.5 scratch-copy rename regression test already proves anchor robustness in isolation; this phase's coverage check is the complementary proof that the anchors still bound the *same logical regions* after Phases 2–3.5's real prose movement, **in both mirrors** — the region-length and superset checks run over the Codex mirror too, which is why this phase follows Phase 3.5 rather than Phase 3.
- **Assert the lagging window is closed:** `RELEASE_LAGGING_MIRROR_OK` is unset for this phase's Test command and Validation cmd, and no `justfile` recipe, CI workflow or pre-commit hook sets it — grep for it and fail on any occurrence outside `scripts/check-prompt-parity.sh`, `tests/parity/test-applier-bundle-parity.sh`, `tests/parity/test_release_skill_contract.py`, their self-tests, and this plan.
- Confirm this phase's own checks pass cleanly given Phases 1.5–3.5 have already landed the anchor and prose changes they depend on — by construction, since Phase 1.5 now runs first, this phase adds no new anchor-stability risk, only the coverage-narrowing proof.

<!-- reviewed: 2026-09-21 @ d28732fa58e0cb42f68a639a53bdc51e971e5ecd -->

## Findings

(populated during implementation/review — do not pre-fill; Phase 1's committed inventory and Phase 2/3's mapping tables land here as they're produced)

### Phase 1 inventory (captured at base 7da1021a0260ce326111943f812f3bab0a2e9e0f)

**Line-number convention.** Lines are for `plugins/skein/skills/release/SKILL.md` (Claude). The Codex mirror `plugins/skein-codex/skills/release/SKILL.md` is **Claude + 1** at every site below (verified by matching heading lines: Step 1b 74 vs 73, A2 305 vs 304, A2.5 356 vs 355, A4 415 vs 414); only line 440/441 (Execution Model) diverges in content. Numbers are as of the base SHA and will drift as Phase 1.5+ edits land.

**Call sites re-specifying template-read / jq-gate / marker-resolution logic** (10 sites x 2 mirrors). "Phase 2 invocation line" is the exact, call-site-distinct line Phase 2 writes (Claude form shown; Codex form replaces the anchor with `"$SKILL_DIR"/lib/<name>.sh`). Script names are proposals (grilled decisions 15/20: hand-authored `lib/`, exit `0/1/2`); the distinct `--site <id>` value is the per-site discriminator that keeps `normalize_release_workflow()`'s `-ne 1` cardinality gates satisfied. Sites marked "prose-only" carry no new invocation line (they cross-reference another site) and add no divergence pair.

| # | Lines | Logic re-specified | xhigh finding | Phase 2 invocation line |
|---|-------|--------------------|---------------|-------------------------|
| 1 | 37-55 (`### Repo Template Override`) | schema, closed key set, defaults | #2/#5 (schema self-contradictions) | prose-only (moves to `references/template-subsystem.md`, Phase 3) |
| 2 | 79-81 (Step 1b items 1-3) | jq pin conditional, presence oracle (worktree or `TEMPLATE_HEAD_COMMIT`), commit precondition (`ls-files`, `git diff --quiet`, tracked mode) | #6, #14 | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/read-release-template.sh" --site step1b-precondition` |
| 3 | 82-94 (Step 1b item 4) | committed-object read + the jq gate sequence: `jq empty`, `-se 'length == 1'`, `type == "object"`, duplicate-key gate, unknown-key gate, per-field enum, `whats_new` type, `excluded_sections` shape (lines 83-90) | #2, #5, #11 | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/read-release-template.sh" --site step1b-validate` |
| 4 | 95 (Step 1b item 5) | record active template, defaults, unset sentinel, blob identity | #6 | prose-only (consumes site 3 JSON) |
| 5 | 160-164 (Step 3 recovery) | marker strictly valid + bound to template blob before stripped-candidate; recovery-template resolution | #9, #11 (out of scope, decision 4) | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/resolve-template-marker.sh" --site step3-recovery` |
| 6 | 171 (Step 3 item 3) | append `release-template-sha` marker from confirmed identity | #5 | prose-only |
| 7 | 194 (Step 5) | re-verify template identity before tag message | #6, #14 | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/read-release-template.sh" --site step5-reverify` |
| 8 | 245, 247 (Step 6) | re-verify identity before notes file; marker already composed | #6, #14 | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/read-release-template.sh" --site step6-reverify` |
| 9 | 329-349 (Step A2) | classification-source resolution: marker search (strict/loose/shape-only patterns), dual-anchor binding (origin peeled-commit at 333, current-HEAD at 334), tracked-mode gate (331), `template-marker-unresolvable` spellings (343-348) | #9, #11 (out of scope), #2 | `"${CLAUDE_PLUGIN_ROOT}/skills/release/lib/resolve-template-marker.sh" --site a2-classify` |
| 10 | 357-373 (Step A2.5) | fresh HEAD resolution, template-absent gate (359), candidate set over A2's resolved classification source (361-363) | A2->A2.5 dependency (Context item 2) | `--infer-template` re-enters A2 site 9 by prose cross-reference (decision 16): zero new invocation lines |

Sites 2, 3, 7, 8 share one script with four distinct `--site` values (four divergence pairs); sites 5, 9 share a second script with two (two pairs). Total: **six** new call-site-distinct invocation lines per mirror, hence six new Phase 2 divergence pairs (fewer than the ~10 near-duplicate prose copies, because sites 1, 4, 6, 10 are prose-only). Other template mentions not counted as logic re-specification: 184-186 (Step 4 confirmation line-item), 297 (A1 tags), 314 (table cell), 391/398-403 (A3 report and Proposed Template rendering).

**Lazy-load mechanism (confirmed).** The only conditional/lazy-load mechanism in the codebase is the `content-review` `references/` precedent: `plugins/skein/skills/content-review/SKILL.md:90-91` ("read `references/content-guidelines.md` (relative to this skill's directory)") and the index at `:234-235`; `content-draft/SKILL.md` also references `references/`. It is **pure prose instruction to the model**: no harness loader, no frontmatter field, no script, test, or justfile gate observes or enforces that a reference is read. Only enforcement present is byte-parity of the files themselves (`scripts/check-prompt-parity.sh:563-577`, `diff -r` of the two `references/` dirs) and one tracked-file existence spot check (`tests/plugin/test_history_and_assets.sh:104-107`). No stronger mechanism exists; Phase 3's verification must not assume a guarantee that a reference is actually loaded, only that the trigger prose is present in the right region.

**`scripts/check-prompt-parity.sh` scope (confirmed).** Release block is `:265-452`. Walks `rubric.md` and `*-prompt.md` (maxdepth 1) plus per-skill normalizers; it does not cover `release/lib/` or `release/references/`. `content-review/references` block (`:563-577`, `diff -r` of two dirs, sets `PARITY_DIFF=1`) is the copyable precedent for Phase 3. `normalize_release_workflow()` divergence machinery: **seven** cardinality gates at `:397,402,407,412,417,422,427` (six `-ne 1`, one `-ne 0` at `:402`), four contracts, two symmetric exact-full-line placeholder pairs (invocation-mode, execution-model). Four regions per pair: verbatim-line constants `:276-286`; `awk -v` list `:313-319`; normalizer branches `:342-361` (replaced by `__HARNESS_INVOCATION_MODE__` / `__HARNESS_EXECUTION_MODEL__`); `count_release_contract_line` calls `:382-395` plus their gates `:397-431`. Shape confirmed as stated in this plan's Phase 1 bullets.

**Script-distribution precedent (confirmed).** `plugins/skein/skills/review-gauntlet/lib/` holds 6 files (`convergence-ledger.sh gate-bounded.sh gauntlet-common.sh ledger-promote.jq run-gate.sh state-path-guard.sh`). `tests/parity/test-applier-bundle-parity.sh`: `GAUNTLET_LIB_PARITY_FILES` at `:138` (byte-identity loop `:138-149`), enumeration half `:151-210` (`GAUNTLET_LIB_ANCHOR_DIVERGENT=(gauntlet-common.sh)` at `:160`; `PARITY_GAUNTLET_LIB_ROOT` override; union-of-both-mirrors `find` at `:205-209` over `*.sh` and `*.jq`). Template for `RELEASE_LIB_PARITY_FILES`. Minor plan discrepancy: the plan's `:138-147` byte-identity range is `:138-149` at this SHA.

**Pytest counts (rule stated).** `tests/parity/test_release_skill_contract.py`: **119** `def test_*` (exact); **60** of them contain at least one `.index(`/`.find(` call (AST walk over each test function's source; 157 total `.index(`/`.find(` occurrences file-wide, including helpers, vs the round-6 figure of 140 by a different rule) so treat 60 / ~140-157 as approximate. Collected test ids: **236** (parametrized over both mirrors via `RELEASE_SKILLS`, defined `:28`; the `@pytest.mark.parametrize("skill_path", RELEASE_SKILLS)` usage begins at `:238`, not `:239`). Region helpers: `_template_region` `:891`, `_a2_region` `:1511`, `_step3_region` `:1995`.

**README / release-plan anchors (Phase 1.5 scope).** `README.md`: `test_readme_describes_persisted_release_highlights_and_summaries` (`:315-336`) locates the `| release |` **table-row prefix** (`line.startswith("| release |")`), splits cells, asserts cell-equality on the two contract cells plus substring assertions on the row: a row/cell match, not a `.index()` literal lookup. `docs/dev_plans/20260712-feature-release-skill.md`: `test_completed_release_plan_records_the_shipped_contract` (`:358-`) uses `text.index("## Requirements")` / `text.index("## Implementation Checklist", ...)` and `requirements.index("2. Resolve and validate")` / `"\n3. "` literal lookups: a heading/numbered-item `.index()` mechanism that raises `ValueError` on rename. Phase 1.5 treatments: README needs no marker (row-prefix contract); the release-plan test needs a stable-anchor treatment for its two headings and numbered item.

**Pytest test-id baseline (pre-Phase-1.5).** Committed at `tests/parity/.release-test-id-baseline.txt` (236 sorted `::` ids, first line `# captured-at: 7da1021a0260ce326111943f812f3bab0a2e9e0f`), produced by `just release-baseline-refresh`. The recipe is **create-only** (refuses to overwrite an existing file): decision 13 says a refresh is the silent rebaseline the pin prevents, while this phase's task requires a recipe, so it is a capture recipe despite its name. No CI wiring yet (Phase 4 consumes it).

### review-plan disposition (2026-09-17)

Full 44-finding review-plan report (13 Critical, 22 Important, 9 Minor, 12 Contradiction) is persisted at `.review-plan/latest-claude.json` from that run. Disposition:

- **5 genuine design decisions**, grilled and accepted as recommended (see "Grilled decisions" under Context above): script distribution model (bundled), A2.5 default-on, A2→A2.5 interface (existing output, unchanged), xhigh #9/#11 out of scope, Phase 1.5 sequencing insertion.
- **All other findings** (Missing Task, Testing Gap, Risk, Constraint, Ambiguity, Sequencing, Nonexistent Reference, and the remaining Contradiction findings that were downstream of the 5 decisions above) had a single clear fix and are incorporated directly into the Requirements/Review Focus/Architecture & Call Flow sections and the phase checklists above — not left as open findings.

### review-plan disposition, round 2 (2026-09-18)

Re-review of the revised plan (run_id `20260918T143631Z`, plan_hash at review time `0dd0ea149ebc9056bcda40f156138463c6815357`), persisted at `.review-plan/latest-claude.json`. 44 findings (8 Critical, 21 Important, 15 Minor). Disposition:

- **Re-grilled decision 1** (script distribution model): four of the 8 Critical findings (2 Contradiction, 1 Constraint, 1 Hidden dependency, 1 Environmental Fact) all converged on the same root cause — `BUNDLE_SKILLS` is the auto-fix-applier pipeline roster, not a generic bundler, and decision 1's "no hand-authored-per-mirror precedent" rationale was false (`review-gauntlet/lib/` and `fan-out.sh` both are precedents). Re-grilled via `AskUserQuestion`; accepted recommendation: switch to hand-authored per-mirror `scripts/`, `review-gauntlet/lib/` pattern with a new `RELEASE_LIB_PARITY_FILES` byte-parity check — see the revised Context/Requirements/Architecture/Phase 2 sections above.
- **1 Critical Nonexistent Reference**: `deep-review/scripts/persist-common.sh` doesn't exist at the cited path (`deep-review/scripts/lib/persist-common.sh` is correct, but no longer the operative precedent once decision 1 was redecided) — fixed by pointing Phase 2 at `review-gauntlet/lib/` instead.
- **2 Critical findings on the superset-of-test-ids check** (Testability + Testing Gap): the check was unenforceable as specified — no captured baseline, ambiguous "retarget" semantics, and (per two Important-severity Contradiction findings) an impossible ask to span both pytest and the bash script-suite convention at once. Fixed directly (standard fix, not grill-eligible — a testing-mechanism definition gap, not an architecture judgment call): "retarget" is now defined in Requirements, a baseline-capture task is added to Phase 1, and the coverage check is scoped to the pytest prose-contract file only, with bash-suite registration as a separate proof.
- **Remaining Important/Minor findings** (component-list omissions in Architecture & Call Flow, the `normalize_release_workflow()` forbidden-substitution-class conflict, Phase 1.5's ~15-helper vs ~142-call-site/~60-test-body underscoping, the A2→A2.5 medium ambiguity post-Phase-2, the unfounded `codex:rescue` scoping claim, the absolute "must not pay context cost at all" Requirements contradiction, and the missing README.md/20260712-plan anchor files) each had a single clear fix and are incorporated directly into the sections and phases above — not left open.
- **Not fixed, logged as accepted framing (Minor, informational):** the architecture lens noted the progressive-disclosure split moves ~20% of `SKILL.md`'s bytes (the template-only regions), leaving the dominant untemplated-path prose (~102K of ~247K) untouched, so the post-refactor file (~195K estimated) stays roughly 2x the next-largest skein skill rather than fully matching the Objective's "matches every other complex skein skill" framing. This plan does not expand scope to also compress untemplated-path prose — noted here as a disclosed gap between the Objective's framing and this plan's actual reach, not a defect requiring a phase.

### review-plan disposition, round 3 (2026-09-19)

Re-review of the round-2-revised plan (run_id `20260918T171659Z`, plan_hash at review time `2632cb37f2c3c7c413a70f7dd2604ea88ae48626`), persisted at `.review-plan/latest-claude.json`. 46 findings (9 Critical, 26 Important, 11 Minor) after the sequential Contradiction Pass. Disposition:

- **Re-grilled decision 6** (new — path-anchor bridging mechanism): round 2's sentinel-token idea (`__RELEASE_SCRIPT_ROOT__`) turned out to have no expansion mechanism on either harness, and the premise for inventing it (that `normalize_release_workflow()` forbids all path-anchor divergence handling) was itself wrong — it only forbids prefix-match/free-form substitution, and already permits exact-full-line divergence pairs. Re-grilled via `AskUserQuestion`; accepted recommendation: each mirror's `SKILL.md` prose carries its own real anchor, and `normalize_release_workflow()` gains one new exact-full-line divergence pair per call site — the same shape as its existing six sanctioned pairs (also corrected: the plan previously miscounted this set as "four" and separately "five"). See the revised Context/Requirements/Architecture/Phase 2 sections above.
- **Standard fixes applied directly** (all judged mechanism/definition gaps, not architecture judgment calls): the `RELEASE_LIB_PARITY_FILES` citation was missing the `GAUNTLET_LIB_PARITY_FILES` precedent's enumeration half (now both halves are in scope); new bash script-unit suites were never wired into `justfile`'s `ci:` dependency list (now an explicit Phase 2 task, plus a Phase 4 enumeration check as backstop); the A2 characterization test was scheduled a phase too late relative to Phase 2's extraction (moved to Phase 1.5, with Phase 2/3 re-running it as a regression check); the "no-behavior-drift" fixture-diff task specified an impossible byte-identical comparison of non-deterministic LLM-generated prose (redefined to diff the extracted scripts' deterministic stdout/exit codes instead); the pytest-id baseline had no machine-readable format or tree-state pin (now a committed data file plus a recorded capture-commit ancestry check); the "retarget" definition's load-bearing clause was unobservable prose (now requires committed evidence of a deliberate-mutation test failure per mapping-table row); `README.md`'s anchor mechanism was misidentified as a `.index()` literal lookup when it's actually a table-row prefix/cell-equality match (corrected, given its own treatment); the "templated-branch region" anchor Phase 3's static assertion depends on was never created by any phase (now added in Phase 1.5); Phase 1.5 was the largest test-mutating phase with no mapping table or in-phase coverage gate (now has both); Phase 4's "same logical regions, non-empty" claim had no implementing task (now a per-region byte-length assertion); and three factual overclaims were corrected (release is not the repo's *only* byte-compared mirror pair, only its only *normalized*-comparison pair; the content-review `references/` precedent actually ships two files with a branching load rule, not one; "~60 test bodies" was a correct anchor-bearing subset of 119 total functions, not a miscount needing to be "119").
- **Rejected finding**: the codebase-claims lens's claim that Phase 1.5's scope should be corrected from "~60" to "119" was itself incorrect — the assumptions lens and the Contradiction Pass both verified 60 is the count of anchor-*bearing* test bodies out of 119 total, a different (and correctly stated) quantity. The plan now states both numbers explicitly to remove the ambiguity that caused the misread, without changing the substance.

### review-plan disposition, round 4 (2026-09-19)

Re-review of the round-3-revised plan (run_id `20260919T091715Z`, plan_hash at review time `0b954ac7620136f7193fca959bf64426cd6dd2f5`), persisted at `.review-plan/latest-claude.json`. 66 findings (18 Critical) after the sequential Contradiction Pass, converging on roughly 7 distinct root issues (many Critical findings were independent lenses catching the same defect). Disposition:

- **Re-grilled decision 7** (new — A2 characterization test / fixture-diff "before" state): round 3's fix of moving the characterization test to Phase 1.5 created a new defect — no comparable artifact across the Phase 1.5 → Phase 2 boundary (prose vs. script stdout), and round 3's redefined fixture-diff task had no "before" state since Phase 2 is what creates the scripts being diffed. Re-grilled via `AskUserQuestion`; accepted recommendation: Phase 1.5 records A2's classification *value* as a committed golden fixture; Phase 1.5's test asserts the prose path produces it; Phase 2's test asserts the extracted script's stdout produces the same value, which also supplies Phase 2's fixture-diff "before" state. See the revised Context/Phase 1.5/Phase 2/Phase 3 sections above.
- **Root cause: round 3's own "fix" of the divergence-pair template was itself a third miscount.** Four lenses plus the Contradiction Pass independently found `normalize_release_workflow()`'s actual machinery is seven cardinality gates guarding four contracts, of which only two are the symmetric exact-full-line pair shape (not "six pairs, one function" as round 3 said, after round 2's "four" and round 3's intermediate "five") — each sanctioned pair spans four file regions, not one function edit. This was a standard mechanism-description fix (not grill-eligible — a verifiable code-shape claim, not an architecture judgment call), applied throughout Context/Requirements/Architecture/Phase 1/Phase 2 above. A related overclaim was also corrected: round 3's "release is the only *normalized*-comparison mirror pair" is false — `fan-out`'s prompt-pair normalizer is a second one, using the exact free-form-substitution class `normalize_release_workflow()`'s header calls forbidden (proving that restriction is function-local, not repo-wide).
- **Standard fixes applied directly** (mechanism/definition gaps, not architecture judgment calls): mirror-atomicity (required by `AGENTS.md`) was conflated with phase-atomicity (not required) in Requirements — split apart, noting Phase 1.5's now-large scope as the reason this matters; the `RELEASE_LIB_PARITY_FILES` citation was still missing 2 of 4 actual `GAUNTLET_LIB_PARITY_FILES` precedent mechanisms (`PARITY_GAUNTLET_LIB_ROOT` test-override fixture, G9 self-test proving the enumeration guard isn't vacuous — now all four are in scope); Phase 4's per-region byte-length assertion had no real recording mechanism (`.release-test-id-baseline.txt` can't hold byte-length rows) — Phase 1.5 now records to a separate `tests/parity/.release-region-length-baseline.tsv`; no phase re-anchored Phase 1.5's region helpers when Phase 3 moves their content into `references/` — added as a Phase 3 task.
- **Remaining Important/Minor findings** (the `-ne 1` pin's same-invocation-line-twice collision risk and its full-mirror-diff-skip side effect, the `ci:` recipe's two-hop bash-suite-registration resolution path, mutation-evidence schema looseness, mapping-table-completeness gate wording) each had a single clear fix and are incorporated directly into the phase checklists above — not left open.
- **Observed pattern, flagged to the user rather than silently repeated a fifth time**: every round so far (2, 3, 4) has found new Critical issues in the immediately-preceding round's own fixes, most often the same divergence-pair count being re-miscounted. Recommend a round 5 before treating this plan as converged, given round 4's fixes touch the same mechanism round 3 got wrong.

### review-plan disposition, round 5 (2026-09-19)

Re-review of the round-4-revised plan (run_id `20260919T134438Z`, plan_hash at review time `8f5c1800599412418d8fa3a4937ce279a28a9c77`), persisted at `.review-plan/latest-claude.json`. 48 findings (14 Critical, 20 Important, 14 Minor) after the sequential Contradiction Pass. Disposition:

- **The divergence-pair mechanism claim is now confirmed correct — first round where it survives verification.** All five lenses independently re-verified `normalize_release_workflow()`'s shape (7 gates / 4 contracts / 2 symmetric pairs / 4 regions, per round 4's Context decision 6) directly against `scripts/check-prompt-parity.sh`, and the `fan-out` second-normalized-pair claim, and every other previously-contested line-number/count claim (119/60/142 test-anchor counts, `GAUNTLET_LIB_PARITY_FILES`'s four mechanisms including `PARITY_GAUNTLET_LIB_ROOT` and G9). Zero findings this round dispute any of that. The assumptions lens flagged one Minor imprecision (the four file-region ranges are machinery-wide, not pair-specific — the two symmetric pairs occupy narrower sub-ranges) and one Minor inconsistency (three different `SKILL.md` byte/line-count figures in the plan's own prose) — both noted, not yet fixed (low-risk, superset ranges).
- **Root cause: round 4's disposition claimed several fixes were "incorporated directly into the phase checklists" that were never actually written into any checklist bullet — only described at the Context/Requirements level.** This is a different failure mode than rounds 2-4's repeated re-miscounting: the *content* of round 4's decisions (golden-value comparison, the `-ne 1` collision risk, the `.tsv` recording mechanism) was correct, but the disposition's claim that it had been propagated into every phase's task list was false for at least four items (Phase 2's fixture-diff bullet still specified the discredited before/after live capture; the `-ne 1` collision fix was never given a concrete mechanism in any phase task; the byte-length exemption format was still undefined; the mutation-evidence schema was never adapted for Phase 1.5's anchor-only retargeting). All four are fixed directly this round (standard fixes — verifiable propagation gaps, not architecture judgment calls), throughout Phase 1/1.5/2/3/4 above.
- **Re-grilled decision 8** (new — A2.5 reference-file placement): Phase 3 originally placed A2.5's inference prose inside the same templated-branch-gated `references/template-subsystem.md` as the template schema, but A2.5 must also run via `--infer-template` on untemplated repos — the templated-branch gate would leave that entry point with no reference file to load on exactly the repos it's most needed for. Re-grilled via `AskUserQuestion`; accepted recommendation: split A2.5's prose into its own `references/audit-inference.md`, gated on Audit Mode running at all rather than on template presence — matching the `content-review` precedent's real two-file/branching-trigger shape. See the revised Context/Requirements/Architecture/Phase 3 sections above.
- **New architectural finding, fixed directly (standard fix — a Requirements-level invariant, not itself a judgment call once decision 8 resolved the file split): `references/` files must carry no path-anchor lines.** Phase 3's planned `references/` byte-parity arm is plain byte-identity (modeled on `content-review/references`, no normalizer); if a `references/` file carried one of Phase 2's real per-mirror anchors, that byte-identity check would fail by construction, and removing the line from `SKILL.md` would also drop Phase 2's per-call-site divergence-pair count from 1 to 0, breaking that gate too. Resolved by stating the invariant explicitly: script-invocation lines with real anchors always stay in `SKILL.md`; only anchor-free descriptive/schema/inference prose moves to `references/`.
- **Fixture-diff scope completed (standard fix): round 4's golden-value approach (decision 7) covered only the single A2 marker-absent case, leaving every other extracted script (jq-gate validation, commit-precondition check) with no before-state for Phase 2/3's byte-diff task — the exact defect class decision 7 was supposed to close, recurring in the part of its scope round 4 didn't extend.** Fixed by committing one golden fixture per extracted script per fixture-repo case in Phase 1.5, and rewriting Phase 2's fixture-diff bullet (which had never been updated from round 3's original "capture before this phase's changes" wording — an impossible ask once Phase 2 is understood as the phase that creates the scripts) to diff against those golden values instead of a live before/after capture. The representative templated/untemplated fixture repos themselves — referenced by Review Focus and Phase 2/3 since round 1 but never created by any phase — are now a named Phase 1.5 deliverable.
- **Remaining Important findings, fixed directly** (mechanism/definition gaps): Phase 1.5's internal task order pinned (fixture repos → anchors → golden capture → byte-length recording → mapping table/coverage gate/rename test); Phase 1's contradictory `n/a` Test/Validation commands corrected to match its own justfile-recipe-creating task; Phase 1/1.5's Impl-files lists extended to include the golden fixtures, fixture repos, baseline files, and mapping-table/first-commit-SHA Findings entries those phases' own tasks create; the region-length baseline given a `file` column (so Phase 4 can resolve a relocated region) and an anti-rebaseline ancestry pin matching the pytest-id baseline's; Phase 3's append-vs-edit conflict on that baseline resolved as append-only; Phase 3's vacuous fixture-diff task (it touches no script) replaced with "re-run Phase 2's existing golden comparisons and confirm they still pass"; the fail-open "any tripped gate skips the entire normalized diff" property disclosed as accepted existing behavior, not something this plan introduces or must fix; the bundle-reference enumeration check (section 7) and the G9/`PARITY_GAUNTLET_LIB_ROOT` self-tests' recursion-skip guards both given explicit Phase 2 tasks so release's new lines don't silently trip either mechanism.
- **Not yet independently re-verified**: this round's fixes have not themselves been through a fresh lens pass. Given round 5 found zero new disputes of round 4's core mechanism claim (a first), but did find a new failure mode (claimed-vs-actual checklist propagation) affecting roughly a third of round 4's disposition, a round 6 is recommended before treating this plan as converged — though the bar for round 6 should be narrower: confirm this round's checklist edits actually landed as described, rather than re-deriving the underlying mechanism facts from scratch again.

### review-plan disposition, round 6 (2026-09-20)

Re-review of the round-5-revised plan against post-#43 `main` (run_id `20260920T081434Z`, plan_hash at review time `c98f4fd45361aed776895bf6b40f5ae1b2aace1f`), persisted at `.review-plan/latest-claude.json`. 36 findings (2 Critical, 17 Important, 17 Minor) after the Contradiction Pass (10 findings). All round-5 mechanism claims (7 gates/4 contracts/2 pairs/4 regions, GAUNTLET_LIB precedent, line numbers and byte counts) re-verified against current `main` with no drift; every defect was in the fixture/golden/baseline test machinery round 5 introduced. Disposition:

- **Grilled decision 9** (new): scripts are pure over injected local inputs; fixtures are static files plus a deterministic `tests/release/lib.sh` builder — committed nested git repos are not storable and could never carry A2's GitHub-side inputs. Pinned executables cross via env vars.
- **Grilled decision 10** (new): goldens are value-level JSON projections in `tests/release/golden/`, compared in a Phase 2 bash suite wired into `ci:`.
- **Grilled decision 11** (new): `.release-region-length-baseline.tsv` stays append-only, Phase 2/3 append rows, Phase 4 reads the last row per `(region,file)`; ancestry pins read `# captured-at:` headers, and the tsv pin targets Phase 2's first commit (the earlier Phase-1.5-first-commit rule was unsatisfiable).
- **Standard fixes applied directly**: Claude anchor form corrected to `${CLAUDE_PLUGIN_ROOT}/skills/release/scripts/<name>.sh` (Codex `"$SKILL_DIR"/scripts/<name>.sh`); `--collect-only -q` filter to `::` lines; nonexistent `GAUNTLET_LIB_PARITY_ROOT` corrected to `PARITY_GAUNTLET_LIB_ROOT` with all three guards; §7's Claude-only grep disclosed; Phase 2 now extends `test-prompt-parity-extended.sh`'s seed and self-tests the new gates; Phase 2 appends `.tsv` rows and asserts the A2→A2.5 field contract; stale no-op `pytest`→`uv run` task deleted; Audit Mode anchor moved to Phase 1.5; Phase 1.5 coverage gate named (`just release-baseline-check`); README rename-regression observable specified; `--infer-template` catalogue test retargeted to a test that pins the subcommand list; Phase 4 registers its enumeration check in `parity-tests`; stale single-file wording removed; anchor-count figures marked approximate.
- **Not yet independently re-verified**: these fixes have not been through a fresh lens pass. Rounds 2-6 each found defects in the preceding round's own fixes; recommend a narrow round 7 confirming propagation only before conduct.

### review-plan disposition, round 7 (2026-09-20)

Narrow confirmation round on the round-6 edits (run_id `20260920T181224Z`, plan_hash at review time `ed7ff6323651ec9cfaebd9774ec3176daf9f1a76`), persisted at `.review-plan/latest-claude.json`. 30 findings (0 Critical, 14 Important, 16 Minor) after the Contradiction Pass (7 findings); the codebase-claims lens verified 38 references with none wrong (its records were written on its behalf from its clean reply — no per-section progress). All defects were propagation gaps or seams in round 6's own additions. Disposition:

- **Grilled decision 12** (new): extracted scripts emit structured JSON (classification fields only); `SKILL.md` renders human text; decision 3 amended. This resolves the JSON-vs-human-text tension with decision 10's `jq -S` projection and the A2.5 field-source contradiction (fetch results are inputs, not outputs).
- **Standard fixes applied directly**: A2→A2.5 field-contract assertion moved from Phase 2 to Phase 3 (where `references/audit-inference.md` is created); boundary SHAs recorded machine-readably in `tests/parity/.release-baseline-meta.json` (`phase15_first_commit`, `phase2_first_commit`), with Phase 2 gaining the task; `.tsv` tolerance measured against the last row, `exempt` narrowed to deliberately-emptied regions; `release-baseline-check` registered in `ci:`/`parity-tests` in Phase 1.5; Phase 4 enumeration narrowed to `tests/release/test-*.sh` with the two-hop suite→recipe→`ci:` check and `justfile:27-40`; Phase 3 Impl files gain the `.tsv` and the `tests/release/` assertion; golden key set (incl. `exit_code`) fixed in Phase 1.5; manual golden-capture procedure specified (human supplies fixture files as mocked fetch results); `RELEASE_JQ` required only where jq runs, `gh` dropped from the script-side env set; subcommand-list-pinning test given a target file; stale raw-stdout, "fixture repo", moved-characterization-test, and superseded ancestry wording removed.
- **Not yet independently re-verified**: as with every prior round, these fixes have not been through a fresh lens pass. Rounds 2-7 each found defects in the previous round's fixes, though round 7's were all Important/Minor propagation, with no Critical findings and no mechanism-level errors — the trend is converging.

### Phase 1.5 stable anchors, mapping table, golden capture (implementer, base 7983f45)

**Boundary SHAs.** `tests/parity/.release-baseline-meta.json` carries `phase15_first_commit` and `golden_capture_commit` as the literal placeholders `__PHASE15_FIRST_COMMIT_PLACEHOLDER__` / `__GOLDEN_CAPTURE_COMMIT_PLACEHOLDER__` (neither commit exists until the conductor's boundary commit; the conductor fills both with that commit's SHA in this phase's last commit). `phase2_first_commit` is `null` until Phase 2. `tests/parity/.release-region-length-baseline.tsv` `# captured-at:` is the base SHA `7983f45dd758b2c763ab000d9a7934a2f05b26b9` (prose byte-identical to it; measured with the Phase 1.5 anchors applied).

**Anchors (Claude mirror only).** 25 `<!-- skein:NAME -->` comment lines in `plugins/skein/skills/release/SKILL.md`; grammar contains no `-sha:` so it cannot match A2's marker regexes. Nesting: `step-1b-templated-branch` / `-end` bracket Step 1b items 4 (grilled decision 22's templated-branch region); `a2-marker-bearing` / `-end` bracket the "Marker present, exactly one match" bullet inside `## Audit Mode` (the A2 sub-region). Audit Mode region anchor is `audit-mode`. Heading spellings were NOT edited (normalization is achieved by retargeting every truncated/full heading literal onto one anchor; SKILL.md prose is byte-identical apart from the inserted anchor lines). Release dev-plan (`docs/dev_plans/20260712-feature-release-skill.md`) carries four `plan-*` anchors. README needs no marker (row-prefix contract) and is covered by the rename test's cell-edit case.

**Mapping table (old literal lookup -> anchor-keyed call).** 90 call sites rewritten mechanically plus 2 by hand (`\n\n### Step 5`, `\n\n### Step 2` guards, now `_anchor_at(..., "step-5"/"step-2")`); the shared primitives are `_anchor_at`, `_plan_anchor_at`, `_region_between`, with `_template_region`, `_a2_region`, `_step3_region` retargeted onto them. Deviation from the plan's "~15 named helpers": one anchor-keyed primitive plus the three pre-existing region helpers, because the inline lookups were single boundary probes, not regions. Mid-region substring probes (e.g. `- \`title_format\` —`, `Try the unmodified body first`, ~65 of the 157 literals) are deliberately NOT anchored: they are content assertions inside an already-anchored region, not region boundaries. Mutation evidence is automated rather than a captured transcript: `test_release_skill_helper_fails_loudly_when_its_anchor_is_mutated[<name>]` renames each anchor comment and asserts a named `AssertionError`.

| Anchor | Replaces literal |
|---|---|
| `<!-- skein:step-1 -->` | ### Step 1: Resolve... |
| `<!-- skein:step-1b -->` | ### Step 1b |
| `<!-- skein:step-2 -->` | ### Step 2 (full/truncated/bare spellings) |
| `<!-- skein:step-3 -->` | ### Step 3 |
| `<!-- skein:step-4 -->` | ### Step 4 |
| `<!-- skein:step-5 -->` | ### Step 5 |
| `<!-- skein:step-6 -->` | ### Step 6 |
| `<!-- skein:audit-mode -->` | ## Audit Mode |
| `<!-- skein:single-version-mode -->` | ## Single-Version Mode |
| `<!-- skein:canonical-format -->` | ## Canonical Format |
| `<!-- skein:step-a1 -->` | ### Step A1 |
| `<!-- skein:step-a2 -->` | ### Step A2 |
| `<!-- skein:step-a2-5 -->` | ### Step A2.5 |
| `<!-- skein:step-a3 -->` | ### Step A3 |
| `<!-- skein:step-a4 -->` | ### Step A4 |
| `<!-- skein:step-1-item-1 -->` | 1. Read `CHANGELOG.md` |
| `<!-- skein:step-1-data-boundary -->` | Treat `CHANGELOG.md` ... untrusted data |
| `<!-- skein:a1-tags -->` | 1. **Tags** |
| `<!-- skein:a1-changelog -->` | 2. **CHANGELOG versions** |
| `<!-- skein:a1-releases -->` | 3. **Releases (list only)** |
| `<!-- skein:a1-normalize -->` | 4. **Normalize and classify names...** |
| `<!-- skein:plan-requirements -->` | ## Requirements (release plan) |
| `<!-- skein:plan-implementation-checklist -->` | ## Implementation Checklist (release plan) |
| `<!-- skein:plan-requirement-2 -->` | 2. Resolve and validate (release plan) |
| `<!-- skein:plan-requirement-3 -->` | \n3. (release plan) |

| Test / helper function | Old literal lookup -> new anchor-keyed call |
|---|---|
| `test_release_treats_changelog_as_untrusted_data_only` | _anchor_at(step-1), _anchor_at(step-1-item-1), _anchor_at(step-1-data-boundary) |
| `test_release_audit_inventory_is_bounded_and_fails_closed` | _anchor_at(a1-releases), _anchor_at(a1-normalize) |
| `test_completed_release_plan_records_the_shipped_contract` | _plan_anchor_at(plan-requirements), _plan_anchor_at(plan-implementation-checklist), _plan_anchor_at(plan-requirement-2), _plan_anchor_at(plan-requirement-3) |
| `test_release_rejects_non_default_remote_ports_before_gh` | _anchor_at(step-2) |
| `test_release_forbids_transport_environment_overrides` | _anchor_at(step-2), _anchor_at(step-3) |
| `test_release_url_diagnostics_never_expose_raw_or_ambiguous_urls` | _anchor_at(step-2), _anchor_at(step-3) |
| `test_release_isolated_transport_uses_absolute_empty_child_hooks_path` | _anchor_at(step-2), _anchor_at(step-3) |
| `test_release_revalidates_complete_destination_immediately_before_tag_push` | _anchor_at(step-5), _anchor_at(step-6) |
| `test_release_locks_repo_identity_and_uses_immutable_step6_remote_url` | _anchor_at(step-2), _anchor_at(step-3), _anchor_at(step-6), _anchor_at(audit-mode) |
| `test_release_rechecks_immutable_release_identity` | _anchor_at(step-3), _anchor_at(step-4), _anchor_at(step-6), _anchor_at(audit-mode) |
| `test_release_audit_uses_one_immutable_remote_url_snapshot` | _anchor_at(audit-mode) |
| `test_release_pushes_pinned_tag_object_instead_of_mutable_local_ref` | _anchor_at(step-4), _anchor_at(step-5), _anchor_at(step-6) |
| `test_release_direct_mode_guards_prefixed_and_bare_names_before_mutation` | _anchor_at(step-3), _anchor_at(step-5) |
| `test_release_audit_classifies_nonstandard_tags_consistently` | _anchor_at(a1-tags), _anchor_at(a1-changelog) |
| `_template_region` | _anchor_at(step-1), _anchor_at(step-2) |
| `test_release_template_schema_defines_all_four_fields` | _anchor_at(canonical-format), _anchor_at(single-version-mode) |
| `test_release_template_validation_fails_closed_on_all_gates` | _anchor_at(step-2), _anchor_at(step-3) |
| `test_release_template_step4_confirmation_names_active_fields` | _anchor_at(step-4), _anchor_at(step-5) |
| `test_release_template_identity_check_is_separate_from_payload_hash` | _anchor_at(step-3), _anchor_at(step-4), _anchor_at(step-6), _anchor_at(audit-mode) |
| `test_release_template_marker_aware_recovery_strips_marker_and_applies_exclusions` | _anchor_at(step-3), _anchor_at(step-4), _anchor_at(step-a2), _anchor_at(step-a3) |
| `test_release_template_marker_uses_head_blob_not_working_tree_hash` | _anchor_at(step-6), _anchor_at(audit-mode) |
| `test_release_template_marker_is_composed_in_step3_not_deferred_to_step6` | _anchor_at(step-3), _anchor_at(step-4), _anchor_at(step-6), _anchor_at(audit-mode) |
| `_a2_region` | _anchor_at(step-a2), _anchor_at(step-a3) |
| `_dry_run_search_region` | _anchor_at(step-a2), _anchor_at(step-a4) |
| `test_release_audit_dry_run_is_audit_mode_only_and_sequenced_after_a2` | _anchor_at(single-version-mode), _anchor_at(audit-mode), _anchor_at(step-a1), _anchor_at(step-a2) |
| `test_release_marker_strip_requires_strict_bound_marker` | _anchor_at(step-3), _anchor_at(step-4) |
| `test_release_step5_tag_message_respects_bare_title_format` | _anchor_at(step-5), _anchor_at(step-6) |
| `test_release_step4_override_recomposes_through_step3_item3` | _anchor_at(step-4), _anchor_at(step-5) |
| `_step3_region` | _anchor_at(step-3), _anchor_at(step-4) |
| `test_release_whats_new_default_is_documented_as_unset_sentinel` | _anchor_at(step-2) |
| `test_release_template_line_item_documents_unset_whats_new_display` | _anchor_at(step-5) |
| `test_release_excluded_sections_empty_body_predicate_matches` | _anchor_at(canonical-format), _anchor_at(single-version-mode) |
| `test_release_excluded_sections_scoped_out_of_no_free_text_claim` | _anchor_at(canonical-format), _anchor_at(single-version-mode) |
| `test_release_template_head_is_resolved_once_per_check` | _anchor_at(step-5), _anchor_at(step-6), _anchor_at(audit-mode) |
| `test_release_audit_a1_peeled_identity_defect_is_scoped_per_tag` | _anchor_at(step-a1), _anchor_at(step-a2) |
| `test_release_single_head_resolution_rule_enumerates_every_site` | _anchor_at(step-a2-5) |
| `test_release_step1b_bootstraps_pinned_context_before_its_first_launch` | _anchor_at(step-1b) |
| `test_release_excluded_sections_gate_rejects_del_byte` | _anchor_at(canonical-format), _anchor_at(single-version-mode) |
| `test_release_step6_prev_drift_recomposes_trailer_not_snapshot` | _anchor_at(step-6) |
| `test_release_step6_prev_drift_restarts_all_identities_before_confirmation` | _anchor_at(step-6) |
| `test_release_audit_a1_3_names_all_five_per_candidate_json_fields` | _anchor_at(step-a1), _anchor_at(step-a2) |
| `test_release_audit_a4_exhaustive_non_fixable_list_is_actually_exhaustive` | _anchor_at(step-a4) |

**In-phase coverage gate.** `just release-baseline-check` (registered in `ci:` and `parity-tests`) asserts collected ids are a superset of `tests/parity/.release-test-id-baseline.txt` and hard-fails on a missing baseline or meta file. Rename regression tests: `test_release_skill_helpers_survive_heading_and_item_renames`, `test_release_plan_helpers_survive_heading_renames`, `test_readme_release_row_edit_fails_with_a_named_assertion_error` (Claude mirror and text-based; Codex lands in Phase 3.5).

**Lagging-mirror acknowledgment.** `RELEASE_LAGGING_MIRROR_OK` (planes `release-skill-md`, `release-lib`, `release-references`; unrecognised name is an error) implemented in `scripts/check-prompt-parity.sh` (skill-md + references), `tests/parity/test-applier-bundle-parity.sh` (lib, with a `PARITY_RELEASE_LIB_ROOT` test seam) and `tests/parity/test_release_skill_contract.py` (loud `pytest.skip` of the Codex parameter, plus the one cross-mirror test). Self-tests: `tests/parity/test-release-lagging-mirror.sh` (per plane: acknowledged -> exit 0 + annotation; unacknowledged -> non-zero; unrecognised name -> non-zero; unset -> hard-fail). Deviation: this new file is outside the Impl-files list.

**Manual golden capture (NOT an automated assertion; single reader so far).** Goldens live in `tests/release/golden/` with the frozen key set in `schema.json` (`case, decision, exit_code, failed_gate, rows, script, site`); `test-golden-schema.sh` enforces the key set. Operator prompt used for every golden (a static reading, since SKILL.md has no substitution seam): "Read `plugins/skein/skills/release/SKILL.md` at the base SHA; for the named site and fixture case under `tests/release/fixtures/`, state the decision the prose mandates and map it to exit 0/1/2 per decision 20." Reader 1 (Claude, this implementer) derived every value below from the cited prose; **reader 2 (Codex via `codex:rescue`) has NOT yet derived them, so per decision 14 the goldens are not yet authoritative and must not be frozen until that agreement is recorded.** No values were fabricated from execution; each is a derivation:

| Golden | Fixture inputs | Derivation (SKILL.md prose) | exit |
|---|---|---|---|
| jq-gate-templated | templated/.release-template.json (valid, tracked, clean) | Step 1b item 4: every gate passes -> template active | 0 |
| jq-gate-untemplated | untemplated (state i) | item 2: absent from worktree and HEAD -> no-op, jq never resolved | 0 |
| jq-gate-templated-jq-unset | templated, RELEASE_JQ unset | item 1: unresolvable jq on the present branch -> stop | 2 |
| jq-gate-invalid-{empty,shape,enum,unknown-key,duplicate-key} | invalid/{jq-empty,shape,enum,unknown-key,duplicate-key}.json | item 4 gate failure -> hard stop, failed_gate = gate name (`jq-empty`, `object-type`, `enum`, `unknown-key`, `duplicate-key`) | 1 |
| commit-precondition-templated / -untemplated | templated clean / untemplated | item 3 passes / item 2 no-op | 0 |
| commit-precondition-state-ii | presence/state-ii.json | committed-then-deleted falls to item 3 -> tracked-but-dirty | 1 |
| commit-precondition-state-iii | presence/state-iii.json | untracked fails item 3 | 1 |
| commit-precondition-state-iv | presence/state-iv.json | tracked-but-dirty fails item 3 | 1 |
| a2-marker-absent | untemplated bodies, no marker, no current template | A2 marker-absent branch -> canonical shape; both versions match section + compare-line rule -> ok, ok | 0 |
| a2-templated | templated bodies v1.0.0/v1.1.0/v1.2.0 | v1.0.0 marker = template blob sha, binds via HEAD anchor, body == section -> ok; v1.1.0 marker binds, body differs -> drifted; v1.2.0 marker 40 zeros, both anchors resolve to a different blob -> template-marker-unresolvable, note "anchor resolved but SHA mismatched" | 0 |

**Mapping-table n/a cells (each with a reason).** read-release-template `step5-reverify` / `step6-reverify` x {templated, untemplated}: n/a, the re-verify sites re-run the identical item 2/3/4 sequence against one fixed HEAD, so the static fixtures yield the step1b-* decisions already pinned above and add no distinct decision. resolve-template-marker `step3-recovery` x both cases: n/a, needs a re-sync fixture (existing release body being recovered) that this phase's mandated fixture set does not contain; a later golden would be a disclosed re-capture. `a2-marker-absent` with a *templated* current template: n/a, not mandated (only marker-absent/no-current-template and the marker-bearing set are). CRLF marker-bearing body (`templated/bodies/v1.0.0.crlf.md`): committed for the byte contract but **no golden**, because the prose never defines whether a trailing CR belongs to the "physical line" the marker regexes match, so any expected value would be a guess; needs a human decision. Tracked-mode (`120000`/`160000`) precondition failures have no fixture or golden yet.

**Fixtures.** `tests/release/fixtures/{templated,untemplated,invalid,presence}` (static inputs, `.gitattributes` `-text`), `tests/release/lib.sh` (deterministic builder; `test-lib.sh` asserts identical HEAD across two builds and that the marker blob resolves via `git cat-file`). The CI workflow now checks out with `fetch-depth: 0`.

### Phase 2 script extraction, mapping table, Phase 3.5 checklist (implementer, base 1fb8a99)

**Scripts (Claude mirror `plugins/skein/skills/release/lib/`; Codex copies land in Phase 3.5).** `release-common.sh` (sourced helpers: `release_require_exe`, `release_emit`, `release_validate_gates`), `read-release-template.sh` (sites `step1b-precondition`, `step1b-validate`, `step5-reverify`, `step6-reverify`), `resolve-template-marker.sh` (sites `a2-classify`, `step3-recovery`). All three are registered in `RELEASE_LIB_PARITY_FILES`; the anchor-divergent exclusion is empty (scripts are anchor-free; the directory is byte-identical across mirrors). The plan's inventory named two scripts; a third sourced helper (`release-common.sh`) holds the shared gate sequence and exe re-check so the two entry points do not fork it.

**Contract.** Stdout: one JSON object with the frozen golden key set `{case,decision,exit_code,failed_gate,rows,script,site}`; exit `0` ok, `1` validation failure, `2` environment failure (decision 20). `read-release-template.sh` args: `--site --worktree {absent|present-untracked|present-tracked-clean|present-tracked-dirty|present-not-regular} --head-commit {present|absent} [--mode M] [--head-sha SHA]`, committed template bytes on stdin (validate/reverify sites); `RELEASE_JQ` required only when a template is present. `resolve-template-marker.sh` args: `a2-classify`: `--repo --release-list --bodies-dir --peeled --changelog --web-base-url`; `step3-recovery`: `--repo --tag --body-file --peeled`; needs `RELEASE_JQ` and `RELEASE_GIT` (read-only git over the injected `--repo`). Body for tag T is `<bodies-dir>/T.md`, else `T.lf.md`.

**A2 script JSON schema (decision 12).** *Script-stdout fields* (the only fields A2.5 may parse): `case` (`templated` when any marker or current template participated, else `untemplated`), `decision` (`classified`), `exit_code`, `failed_gate`, `rows[]` of `{version, status in {ok, drifted, template-marker-unresolvable}, note}` (note is the punch-list spelling, e.g. `anchor resolved but SHA mismatched`), `script`, `site`. *Fetch-side fields* (script inputs, never outputs): release `name`/`body`/`isDraft`/`isPrerelease`/`databaseId`, origin peeled-commit SHAs, `WEB_BASE_URL`. Not covered by the script: the T/R/C table, `drafted`/`prereleased`, `local-only-tag`, `release-without-tag`, `missing-*`, `legacy-bare-tag` (those stay prose; only the marker/anchor resolution plus `ok`/`drifted` moved). `step3-recovery` emits only `marker-bound`, `marker-absent-current-template`, `marker-absent-canonical`, `template-marker-unresolvable`.

**Golden comparison.** `tests/release/test-scripts.sh` (`just release-script-tests`, wired into `ci:`) runs every Phase 1.5 golden against the Claude `lib/`, compares the `jq -S` projection key-by-key plus the process exit status, checks `git diff --quiet <golden_capture_commit> -- tests/release/golden/`, and carries the permanent tamper self-test (changed field, changed `exit_code`, dropped key, each asserted non-zero naming the key). All 15 goldens with fixture inputs reproduce exactly. Negative cases: `RELEASE_JQ`/`RELEASE_GIT` unset/relative/missing/non-executable/symlink (exit 2), SHA-256 hard stop, untemplated `RELEASE_JQ`-unset success on all four read sites, `step3-recovery` bound/canonical/unresolvable. The CRLF marker-bearing body and tracked-mode `120000`/`160000` remain unpinned (no golden; decision needed).

**Mapping table (old SKILL.md-prose assertion -> new script-behaviour test).** Mutation evidence is automated: `test_release_lib_gate_tests_fail_under_deliberate_mutation[<gate>]` neuters each gate in a scratch copy of `lib/` and asserts the retargeted fixture verdict flips.

| Old assertion (prose-extracted jq commands / region text) | New test |
|---|---|
| `test_release_template_validation_fails_closed_on_all_gates` (region regexes for the three ported gates, unknown/duplicate/enum) | same test: asserts the script call site and all eight `failed_gate` names are in the region; gate behaviour asserted by the fixture tests below |
| `test_release_duplicate_key_gate_matches_persist_common_edge_cases` (extracted duplicate-key jq) | same name, drives `read-release-template.sh` and compares its duplicate-key verdict with persist-common |
| `test_release_template_jq_gates_fail_closed_on_fixtures` | same name, `_release_lib_validate` over every `_template_fixtures()` case |
| `test_release_template_jq_gates_reject_null_and_false_not_just_wrong_string[*]` | same name, script rejects each fixture |
| `test_release_template_jq_gates_match_across_mirrors` (compared extracted jq commands) | same name, compares lib bytes across mirrors (acknowledged/skipped until Phase 3.5) |
| `test_release_excluded_sections_bound_is_characters_not_bytes` | same name: prose count relaxed to the schema table, doc comment and `(length <= 200)` asserted in `release-common.sh`, 196/197-character behaviour cases |
| `test_release_excluded_sections_gate_rejects_del_byte` | same name: DEL class asserted in `release-common.sh`, DEL fixture rejected with `excluded-sections-shape` |
| `test_release_step1b_bootstraps_pinned_context_before_its_first_launch` | same name + script-launch boundary sentence and `RELEASE_JQ` unset -> exit 2 (decision 17) |
| `test_release_jq_pin_is_conditional_on_template_presence` | same name + untemplated `RELEASE_JQ`-unset -> `absent-noop` exit 0 (decision 18) |
| new | `test_release_lib_gate_tests_fail_under_deliberate_mutation[8 gates]` |

**Shape.** SKILL.md grew net (~+0.7 KB: 6 invocation lines, ~5.2 KB of gate-bullet prose removed from Step 1b item 4, plus glue prose and the amended invariant): the step-1b-templated-branch region shrank 10510 -> 6852 bytes, every other touched region grew by its invocation line and glue. The remaining duplicated prose (items 2/3, A2's anchor/limitations prose) is still pinned by ~60 pytest assertions and was left in place rather than retargeted this pass; it is a Phase 3 candidate. `.release-region-length-baseline.tsv` rows appended for every changed region.

**Divergence pairs.** Six new exact-full-line pairs in `scripts/check-prompt-parity.sh` (`RELEASE_CALL_SITES`, generated by `release_call_line claude|codex <site>`; placeholders `__RELEASE_CALL_<SITE>__`; per-mirror `-ne 1` gates; a Codex-side miss is annotated when `release-skill-md` is acknowledged). Deviation from the plan's "written out per region": generated from one template via a map file rather than 6 x 4 hand-written constants, so the two sides cannot drift. Self-tests in `tests/parity/test-prompt-parity-extended.sh` (`test_release_script_call_line_gates`: per site x mirror, present passes / missing fails / duplicated fails); the seed carries both sides' lines via the same generator.

**Phase 3.5 checklist (Codex mirror must mirror, byte-for-byte modulo anchors).** (1) Copy `lib/release-common.sh`, `lib/read-release-template.sh`, `lib/resolve-template-marker.sh` byte-identically. (2) Six call-site lines, Codex form from `release_call_line codex <site>` (guard `[ -x "${SKILL_DIR:?}/lib/<name>.sh" ]`, call `"$SKILL_DIR"/lib/<name>.sh`) at: Step 1b before the templated-branch anchor (`step1b-precondition`), inside the templated-branch region after item 4's transport paragraph (`step1b-validate`), after the Step 3 "Try the unmodified body first" paragraph (`step3-recovery`), after Step 5's and Step 6's template-identity re-verify paragraphs (`step5-reverify`, `step6-reverify`), and at Step A2 before the marker-bearing anchor (`a2-classify`, column 0). (3) The Step 1b item 4 gate-bullet replacement paragraph, the item 2/3 script-computation lead-in, the Step 3/5/6/A2 lead-in sentences, and the amended pinned-executable invariant sentence ("**Script-launch verification boundary:** ...") appended to the Step 2 item 1 invariant paragraph. The Claude `SKILL.md` diff at this phase's commit is the reference.


### Phase 3 progressive disclosure split, mapping table, Phase 3.5 checklist (implementer, base 15194c6)

**Moved (Claude mirror only).** `references/template-subsystem.md` (three anchored sections, `ref-template-schema`, `ref-template-read-gates`, `ref-template-a2-anchors`): the `### Repo Template Override` schema section (was inside Canonical Format), Step 1b item 4's committed-object read + shell-transport rule + gate documentation + the `excluded_sections` treat-as-data paragraph, and Audit Step A2's marker-present dual-anchor bullets/gates/limitations (previously between the `a2-marker-bearing` anchors). `references/audit-inference.md` (anchor `ref-audit-inference`): Step A2.5's whole inference prose, verbatim, plus new "Entry points and the template-absent gate" and "A2 to A2.5 field contract" sections (`### Script-stdout fields`: `case`, `exit_code`, `rows[]`, `rows[].version`, `rows[].status`; `### Fetch-side fields`: `name`, `body`, `isDraft`, `isPrerelease`, `databaseId`, origin peeled SHAs, `WEB_BASE_URL`). Neither file carries a path anchor. SKILL.md keeps: item 4's hard-stop paragraph and `step1b-validate` call line, the A2 marker-bearing bullet stub (one pointer), the A2.5 heading + one-paragraph stub (one pointer), a Canonical Format stub (names no reference file), and new anchors `infer-template-entry`/`-end` (the `--infer-template` sub-region, no script call line: it re-enters A2 by prose), `audit-mode-end`, a "Default `/release audit` run sequence" sentence, a Usage bullet and `argument-hint: "[X.Y.Z|latest|unreleased|audit [--infer-template]]"`.

**Design choice (deviation from "retarget each assertion by hand").** ~100 existing assertions read `skill_path.read_text()`. Rather than rewriting each to name a reference file, `_release_text(skill_path)` returns SKILL.md with each reference section spliced in at its pointer (position of the original prose; `_REFERENCE_SPLICES`). Every retargeted assertion therefore reads the moved prose from `references/` and is mutation-tested against it; where prose lives is pinned separately by the raw-text boundary tests below. Codex mirror: `_release_text` returns raw text when no `references/` dir exists (its parameters are skipped under `release-skill-md` until Phase 3.5).

| Old assertion (SKILL.md prose) | New location of the pinned prose | Mutation evidence (automated) |
|---|---|---|
| `test_release_template_schema_defines_all_four_fields` (+ the excluded_sections/whats_new schema tests over the Canonical Format region) | `template-subsystem.md` `ref-template-schema` | `_REFERENCE_MUTATIONS`: `"compare_line_label"` renamed in the reference -> AssertionError |
| `test_release_raw_template_bytes_have_an_explicit_shell_transport_rule` (+ `_template_region` tests over item 4) | `ref-template-read-gates` | "never through a heredoc" mutated -> AssertionError |
| `test_release_audit_a2_documents_dual_anchor_limitations` (+ `_a2_region` marker-anchor tests) | `ref-template-a2-anchors` | "clone-dependent"/"clone-completeness" mutated -> AssertionError |
| `test_release_audit_a2_5_candidate_set_is_defined_mechanically` (+ all `_dry_run_search_region` A2.5 tests) | `audit-inference.md` | "Step A2 actually resolved a classification source" mutated -> AssertionError |
| new: 3 reference-count assertions | raw SKILL.md: template ref named exactly twice (once in `step-1b-templated-branch`, once in `a2-marker-bearing`), inference ref exactly once (inside `audit-mode`/`step-a2-5`), neither elsewhere | anchor-mutation raises named AssertionError (existing parametrized anchor test covers the new anchors) |
| new: decision 2/16 invariants | `step-a2-5` marker inside `audit-mode`, outside `infer-template-entry`; default-run sentence names Step A2.5; inference ref not inside templated-branch / marker-bearing / canonical-format regions | same anchor-mutation test |
| new: `test_release_infer_template_is_listed_in_frontmatter_usage_and_readme` | frontmatter argument-hint, Usage bullet, README `release` row | n/a (string presence) |
| new: `test_release_reference_helpers_survive_heading_renames[both files]` | reference anchors survive heading renames; a mutated anchor raises a named AssertionError | built in |
| new: A2->A2.5 field contract | `tests/release/test-a2-a25-contract.sh` (in `just release-script-tests`): documented fields must appear in live script stdout (templated + untemplated fixtures) and both A2 goldens | automated self-test: `rows[].status` renamed in a scratch script copy, then in a scratch reference copy; each makes the contract fail |

**Marker-absent golden / Phase 2 fixture diff.** `tests/release/test-scripts.sh` (unchanged) still reproduces `a2-marker-absent.json` and every other golden; this phase touched no script file.

**Size result.** Claude `SKILL.md` 213,442 bytes vs pre-refactor 243,730 (-12.4%) and Phase 2's 245,577; round 2's ~195K estimate was NOT reached (213K). `references/template-subsystem.md` 19,599 + `references/audit-inference.md` 19,697 = 252,738 total (+3.7% vs pre-refactor: three headers, the field-contract and entry-point prose are new). SKILL.md is below the pre-refactor size, so this phase's size gate passes; an untemplated `/release <version>` cut no longer loads either reference (~39 KB).

**Region baselines.** `.release-region-length-baseline.tsv` has appended rows (Phase 1.5/2 rows untouched) for every changed region: shrunk SKILL.md rows marked `relocated-to-references`, the relocated reference rows under the same region name, and the new anchors.

**Parity script.** `scripts/check-prompt-parity.sh`: the single `references/` directory diff was replaced by one arm per file (`template-subsystem.md`, `audit-inference.md`), byte identity via `cmp`, a file missing on either side is drift; acknowledged by `release-references` until Phase 3.5.

**NOT DONE, needs a human.** The manual transcript checks require a live Claude Code session and are not recorded (no transcript path/SHA-256/date/verifier): (1) untemplated `/release <version>` against skein's own repo must show zero file reads of `template-subsystem.md` (and of `audit-inference.md`); (2) `/release audit` against the untemplated fixture must show zero reads of `template-subsystem.md`, and its read of `audit-inference.md` must occur only at Step A2.5. Note the plan's protocol names `audit-inference.md` as gated for the audit run, but by grilled decision 2 A2.5 runs in the default audit, so that file is read there by design; the plan text should say the gated-off case for it is single-version cuts. Phase 1.5's reader-2 (Codex) golden agreement is also still open.

**Deviations.** Also touched (outside the Impl list, required): `tests/parity/test-release-lagging-mirror.sh` (fake root now mirrors Claude's `references/` into Codex so each plane test isolates one plane) and the `references/` arm additionally compares the two directories' file sets so a stray file is drift.

**Phase 3.5 checklist (Codex mirror must mirror; anchors differ, everything else byte-identical).** (1) Copy `references/template-subsystem.md` and `references/audit-inference.md` byte-identically (the parity arms then enforce unacknowledged). (2) Apply the same SKILL.md edits: Canonical Format stub (no reference name), Step 1b item 4 stub paragraph (names `references/template-subsystem.md`, keeps the `step1b-validate` call line and hard-stop paragraph, drops the transport/values paragraphs), A2 marker-bearing bullet stub (names it once), A2.5 heading + stub (names `references/audit-inference.md` once), `infer-template-entry`/`-end` sub-region, default-run sentence, Usage bullet, `audit-mode-end` anchor before `## Worked Example`, and the new argument-hint. (3) Expected reference counts: template ref exactly 2 (branch region, marker-bearing region), inference ref exactly 1 (inside Audit Mode). (4) Extend the boundary/decision-2/16/flag-list tests to the Codex path and drop `release-references` from the acknowledgment. The Claude `SKILL.md` diff at this phase's commit is the reference.
